# OpenHands certification coding task

This disposable repository contains one intentionally incomplete function.
The governed worker must implement `slugger.core.slugify`, run the existing
pytest suite, and return a real diff.  The task has no network or credential
requirements; the live certification harness supplies the workspace.
