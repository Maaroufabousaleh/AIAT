# Capability Registry Scaffold

This directory exists to satisfy the Phase 0 scaffold requirement for a
capability registry module.

Runtime protocol models live in:
- `mas_core.protocols.capability`

Runtime utility surface lives in:
- `mas_core.capabilities`

