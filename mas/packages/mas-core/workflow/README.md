# Workflow Controller Scaffold

This folder exists to match the architecture checklist path `packages/mas-core/workflow/`.

Runtime Python code lives under:
- `packages/mas-core/mas_core/workflow/`

That package now includes the deterministic Phase 4b scaffold from org architecture section 11.2:
- transition table
- controller transition validator
- watchdog timing helpers
