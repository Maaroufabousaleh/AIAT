# ── office_cio — CIO office ───────────────────────────────────────────────────
# MAS role: c_suite
# Technical feasibility, tech-stack assessment, integration analysis.
# Hiring board role: integration and adapter reviewer.

team_id: office_cio

admin:
  agent_id: cio
  role: c_suite
  class: CSuiteAgent
  display_name: "Chief Information Officer"
  system_prompt_file: prompts/cio.md
  budget_defaults:
    max_llm_calls: 30
    max_tool_calls: 15
    max_cost_usd: 1.00
  tools:
    - capability.search
    - web_search

workers:
  - agent_id: tech_analyst
    role: worker
    class: WorkerAgent
    display_name: "Tech Analyst"
    min_instances: 0
    max_instances: 2
    budget_defaults:
      max_llm_calls: 20
      max_tool_calls: 10
      max_cost_usd: 0.50
    tools:
      - capability.search
      - web_search

  - agent_id: tool_interface_auditor
    role: worker
    class: WorkerAgent
    display_name: "Tool Interface Auditor"
    min_instances: 0
    max_instances: 1
    budget_defaults:
      max_llm_calls: 20
      max_tool_calls: 12
      max_cost_usd: 0.40
    tools:
      - capability.search
      - capability.list_workers
      - web_search
      - blob.upload

  - agent_id: adapter_certifier
    role: worker
    class: WorkerAgent
    display_name: "Adapter Certifier"
    min_instances: 0
    max_instances: 1
    budget_defaults:
      max_llm_calls: 20
      max_tool_calls: 12
      max_cost_usd: 0.40
    tools:
      - capability.search
      - capability.list_workers
      - web_search
      - blob.upload
