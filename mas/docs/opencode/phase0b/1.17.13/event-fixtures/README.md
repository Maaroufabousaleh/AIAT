# OpenCode event fixtures

This directory is reserved for sanitized event payloads captured by a future
fully approved live certification. The current Phase 0B run only proves the
initial SSE connection and keeps the report pending; no runtime payloads or
credentials are stored here.
