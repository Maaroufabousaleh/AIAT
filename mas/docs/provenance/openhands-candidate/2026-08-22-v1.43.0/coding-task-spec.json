{
  "schema_version": "aiat.openhands-certification-coding-task.v1",
  "task_id": "deterministic-slugify-v1",
  "fixture": "mas/scripts/fixtures/openhands-coding-task",
  "prompt": "Implement slugger.core.slugify in the disposable repository. It must lowercase text, transliterate accented letters to ASCII, replace every run of non-alphanumeric characters with one hyphen, trim hyphens, return an empty string for empty/separator-only input, leave the tests unchanged, run python -m pytest -q, and report the real changed files.",
  "test_command": "python -m pytest -q",
  "expected_changed_paths": ["slugger/core.py"],
  "forbidden_changed_paths": ["tests/test_slugger.py", "pyproject.toml"],
  "network_required": false,
  "credentials_required": false,
  "prose_only_is_insufficient": true,
  "payloads_retained": false
}
