# AIAT Current Release Ledger

**Run date:** 2026-08-18
**Base revision:** `5b6e13a` (latest reviewed implementation/evidence revision)
**Working-tree state:** dirty; this ledger is a P0 progress ledger, not a production release certificate  
**Decision:** **NO-RELEASE / P0 INCOMPLETE**

The latest grouped verification at this revision (2026-08-18) confirms the
policy-backed network boundary matrix, the durable local Postgres flow-instance
recovery certificate (`1f20132`), the bounded multi-host duplicate-effect and
replay certificate (`d45e4dd`), the complete governed worker run-version pin
certificate (`7c1ef74`), the deterministic workflow-control extension
(`9972b3b`), the bounded advanced object-store benchmark wave (`6794b9f`),
the bounded multipart object-store boundary (`a2f35de`), the guarded provider-diverse object-store migration rehearsal
(`ecbef00`) and its boundary-compatibility fix (`d9cc04e`), the bounded
resource profile (`3791b3f`), the corrected live provider-process outage/recovery
wave (`d92b3dc`/`407a4f9`), the live AIAT credentials-manager boundary certificate
(`12ba7c7`/`1808a15`), the fresh configured Compose trace/metrics/worker/runtime/model
read-backs (`cb47e3b`), and the static aggregation at 55/55 checks passing, four pending
evidence items, and a dirty working tree;
the conservative decision remains
**NO-RELEASE**. The `fa42284` implementation certificate and `0e0a76f`
raw-provider composition certificate passed their focused Ruff/tests and live
dual-Postgres evidence; the configured live profile
is retained separately below. The current unconfigured live attempt is retained at
[`provenance/release_ledger_live_current.json`](provenance/release_ledger_live_current.json)
and is not silently substituted for the configured Compose evidence. The latest
configured 77-check aggregation is retained at
[`provenance/release_ledger_live.json`](provenance/release_ledger_live.json) with
70 passes, zero failures, 7 blocked checks, and four pending evidence items;
it remains descriptive and does not change **NO-RELEASE**.

The local durable provider-shaped recovery group (`1679341`, `9c7e76d`) also
passes: the default fixture injects one transient `429`, retries once through
the production adapter/controller, reopens both Postgres stores, passes raw
provider-ingress replay/conflict/tamper handling, and cleans to zero rows. It
does not close external provider outage/restore, callback/delivery, independent
host/process, or sandbox gates.

The local process-isolation group (`cec6558`, corrected by `520c6bf` and
documented in `95b8262`) also passes. Two separate Python child processes on
the same Compose host reconnect to Postgres and settle separate committed
worker-host bindings through the production executor/controller. The retained
evidence verifies distinct process IDs, two `SUCCEEDED` runs, payload-free
usage/artifact/trace projection, durable reopen, and zero remaining fixture
rows. It is a same-host prerequisite only; independent deployed-host loss or
split-brain, external provider/outage, and gVisor/Firecracker evidence remain
open.

The worker-loss recovery soak group (`424805c`) also passes in local Compose
scope. Three separate child processes repeat the production host-loss,
requeue, stale-executor rejection, reassignment, and attempt-two retry path;
each iteration reopens Postgres, retains scalar-only output, and cleans to zero
fixture rows. This is same-host consistency evidence only, not independent
deployed-host, provider-outage, sandbox, deployment-load/chaos, clean-host, or
disaster-recovery evidence.

The encrypted object-store group (`91504dd`, documented in `8ae3125`) also
passes its local provider-neutral certificate. AES-256-GCM encrypts source
bytes before replication; the manifest retains opaque key-ID/nonces/checksums
only, authenticated read-back passes, wrong keys and tampering are rejected,
and a non-empty target is refused before mutation. The follow-on local
clean-process certificate (`b0f27f6`, evidence `59294c0`) persists only
ciphertext and scalar manifest metadata, reopens it in a distinct Python
process with fresh adapters, verifies/replicates it through the production
encrypted-restore helper, and removes all temporary fixtures. Provider-managed
SSE/KMS, external backend/key custody, provider-pair, clean-host/disaster-
recovery, and outage evidence remain open.

The bounded object-store provider-pair group (`351444a`, evidence `f385bd7`,
documented in `3833631`) also passes its live same-host MinIO rehearsal. The
`aiat.object-store-provider-pair.v1` checker dual-writes three checksum-bearing
objects, rejects a simulated primary outage at the AIAT adapter boundary,
recovers from the secondary into a clean recovery target without primary calls,
and removes all reserved objects from both endpoints. This is not provider-
diverse durability, an actual provider process/network outage, provider-managed
SSE/KMS, clean-host restore, or disaster-recovery evidence; those gates remain
open.

The provider-diverse storage follow-up (`f52fde6`, evidence retained in
`object_store_provider_pair_provider_diverse_evidence.json` and
`object_store_provider_benchmark_evidence.json`) also passes its disposable
local boundary. Compose MinIO and SeaweedFS 4.42 each pass three checksum
benchmark cases; the pair checker passes three-object dual-write,
adapter-boundary primary-loss rejection, secondary-only clean restore, and
zero cleanup. This is operator-observed endpoint diversity and comparison
evidence only; provider durability/custody, KMS, actual outage, clean-host,
disaster recovery, and migration cutover remain open.

The advanced benchmark group (`6794b9f`, evidence retained in
`object_store_benchmark_advanced_provider_diverse_evidence.json`, documented in
`d0e4d19`) also passes. Both endpoints complete four concurrent 1 MiB cases
and four concurrent 8 MiB cases with checksum read-back and zero remaining
fixture objects. This closes only the bounded large-object/concurrency wave;
the scalar resource profile is checked separately, while production resource
budgets/portability, provider-managed encryption/KMS, clean-host, disaster
recovery, and provider selection remain open. Provider-process outage/recovery
is certified separately below.

The multipart group (`a2f35de`, evidence retained in
`object_store_multipart_provider_diverse_evidence.json`, documented in
`e804424`) also passes. Both endpoints complete 8 MiB and 16 MiB payloads with
5 MiB parts, checksum read-back, an explicit aborted upload with no object,
and zero remaining fixture objects. The scalar resource profile is checked
separately; production resource budgets/portability, provider-managed
encryption/KMS, clean-host, disaster recovery, and provider selection remain
open. Provider-process outage/recovery is certified separately below.

The bounded resource-profile group (`3791b3f`, evidence refreshed in
`c90fc13`) also passes its fresh corrected two-provider wave. Compose MinIO
and disposable SeaweedFS 4.42 each complete eight 1 MiB/8 MiB checksum
upload/read-back cases at concurrency four with procfs RSS plus wall/CPU
scalars, zero errors, and verified cleanup. In the same fresh reserved
namespace, both providers pass 8 MiB/16 MiB multipart upload/read-back with
5 MiB parts, explicit abort-without-object, and zero cleanup. The earlier
nonexistent-network-alias attempt was a harness/configuration-invalid run and
is excluded from provider evidence and verdicts. These observations do not
assert production resource thresholds or provider selection; KMS, clean-host,
disaster recovery, and portability remain open. Provider-process
outage/recovery is certified separately below.

The corrected provider-process outage/recovery wave (`d92b3dc`, scalar evidence
[`provenance/object_store_provider_outage_live_evidence.json`](provenance/object_store_provider_outage_live_evidence.json),
ledger registration `407a4f9`) passes against fresh disposable MinIO and
SeaweedFS containers. Both providers receive the identical 64 KiB/1 MiB
checksum workload, are stopped through strictly labelled disposable-container
identities on `mas_internal`, become unreachable, restart with anonymous
disposable volumes, read back both objects successfully, and clean to zero
objects. Provider containers, helper containers, and disposable volumes are
removed; the shared canonical network is not removed. Earlier development
attempts are excluded from provider evidence. This closes only the actual
provider-process outage/recovery gate; provider-managed KMS, clean-host/
disaster recovery, production resource thresholds, and independent-host
evidence remain open.

The live AIAT credentials-manager boundary certificate (`12ba7c7`, scalar
evidence [`provenance/credentials_manager_live_evidence.json`](provenance/credentials_manager_live_evidence.json),
registered by `d101901`) passes against the running Compose Postgres boundary.
It verifies ciphertext-at-rest before persistence, metadata-only projection,
policy denial, approved server-side resolution, one-use approval, rate-limit
denial, durable audit rows, and zero fixture residue without retaining key
material, secret values, or payloads. This advances secret-management evidence
for the AIAT-owned boundary only. No provider-managed SSE/KMS endpoint is
configured, so external KMS/key-custody/rotation remains a separate operator
prerequisite and is not inferred from this certificate.

The verified-copy follow-up (`6f5b5b8`, evidence
`object_store_copy_provider_diverse_evidence.json`) also passes. Three
reserved objects are inventoried on Compose MinIO, copied to disposable
SeaweedFS with matching checksums and sizes, preserved until explicit cleanup,
and removed from both prefixes. This is parity evidence only; retention
parity, cutover/rollback, actual outage, clean-host, and disaster-recovery
gates remain open.

The guarded migration rehearsal (`ecbef00`, evidence
`object_store_migration_provider_diverse_evidence.json`) also passes. It uses
an explicitly reserved project across disposable MinIO/SeaweedFS endpoints,
records inventory, checksum/read-back copy, one dual write, human-confirmed
`CUTOVER` → `ROLLED_BACK` history, and removes four fixture objects per side.
Deployment routing and retention authority are unchanged; production cutover,
provider outage, KMS, clean-host, and disaster-recovery evidence remain open.

The security review increment `23e908e` adds a machine-checked register for
the exact OpenCode/Semgrep evidence. It maps all 15 rule groups to exactly 316
findings, records a personal-operator owner and next action, and tracks the 54
engine warnings. The register is a coherence contract only: its static check
passes while `technical_gate_status: blocked` and both worker manifests remain
`findings_review_required`.

The `f999695` worker-plane provider runner remains fail-closed by default and
requires explicit operator opt-in. The retained live certificate
[`provenance/gateway_worker_provider_live.json`](provenance/gateway_worker_provider_live.json)
(`90c3e5d`) records one listed-model check and one successful
`llama-3.3-70b-versatile` completion with `SUCCEEDED` settlement and
binding/reservation release. It retains scalar status/usage metadata only; this
does not add durable worker read-back, independent-host, sandbox, recovery, or
mail-edge callback/bounce evidence to the release decision.

The durable live recovery increment `def4fe9` adds a bounded adapter retry
budget and retains [`provenance/gateway_worker_provider_recovery_live.json`](provenance/gateway_worker_provider_recovery_live.json).
That certificate records one injected transient `429`, one forwarded
`llama-3.3-70b-versatile`/`litellm` completion, `SUCCEEDED` durable settlement,
dual-Postgres reopen, raw-ingress delivered/bounced replay/conflict/tamper
checks, payload-free generated-text redaction, and zero residual fixture rows.
It is retry-boundary evidence only; outage, external callback/delivery,
independent-host, sandbox, and clean-worktree gates remain open.

The Firecracker contract increment `5ed0a0b` adds an AIAT-owned, fail-closed
high-risk launch boundary with immutable kernel/rootfs digests, bounded
resources, read-only rootfs, deny-by-default egress, opaque secret references,
artifact output, and mandatory cleanup. The retained
[`provenance/firecracker_worker_pool_readiness.json`](provenance/firecracker_worker_pool_readiness.json)
passes static validation but is live-blocked because the certified launcher and
Firecracker binary are unavailable; no Docker/runc/gVisor fallback is allowed.
Real microVM smoke/network, provider, recovery, and host certification remain
open.

The continuation groups `8f46ed1`/`4d2ec12` reconcile the protocol artifact and
authority documentation; `0dbfdb7`/`bf65cd3` align docs-index regression
coverage; `dee1a7e`/`dbfda24` enforce and document the metadata-only licence
detail surface; and `2360e07`/`c10ec59` verify and document the host-owned
Compose/systemd service-restart boundary; `8ed53df`/`b8a0c73` certify and
document production gateway-adapter host dispatch; `fa42284` adds the durable
dual-Postgres gateway-worker/mail-edge composition certificate and `67f1599`
extends it through signed delegated identity ingress; `0e0a76f` adds the
raw-provider composition mode with durable provider-message worker/trace
correlation; `1f20132` adds the reserved local Postgres flow-instance recovery
certificate, `2ad5d4c` records its evidence and target-programme updates, and
`d45e4dd`/`84ec4ff` extend and document bounded multi-host duplicate-effect
protection, and `7c1ef74`/`3e81606` extend and document complete local
governed run-version pinning; `1679341`/`9c7e76d` extend and document the
local durable provider-shaped gateway retry certificate; `95b8262` records the
local independent-process worker execution certificate and synchronized
feature/plan/target-programme/roadmap documentation; `91504dd`/`46a8658`
implement and retain the encrypted object-store backup certificate, and
`8ae3125` synchronizes its target/feature/plan/roadmap status; `b0f27f6`,
`59294c0`, and `6cf39b6` add and document the local fresh-process encrypted
restore prerequisite; `424805c` adds and documents the bounded same-host
worker-loss recovery soak; `351444a`/`f385bd7`/`3833631` add, retain, and
document the bounded same-host object-store provider-pair recovery boundary;
`f52fde6` retains and documents the provider-diverse MinIO/SeaweedFS adapter
and benchmark follow-up; `6f5b5b8` retains and documents heterogeneous
verified-copy parity; `ecbef00` implements the guarded live migration
rehearsal, `c086a91` retains its scalar evidence and synchronizes the
target/feature/plan/status/roadmap documentation.
`6794b9f` implements the bounded large-object/concurrency benchmark wave and
`d0e4d19` retains its scalar evidence and synchronizes the target, feature,
plan, status, authority, and roadmap documentation.
`a2f35de` implements the multipart adapter boundary, `d39adce` retains its
scalar MinIO/SeaweedFS evidence, `e804424` synchronizes the target, feature,
plan, P0, authority, and roadmap documentation, and `424d310` adds the
multipart fixture to the release ledger. `a19aff1` refreshes the target, P0,
plan, and authority status references and reruns the aggregate against the
same bounded evidence.
`3791b3f` implements the scalar object-store resource profile, `bb0fa8f`
registers its static/live ledger child, `c90fc13` refreshes the corrected
MinIO/SeaweedFS scalar evidence, and `3f7035a` synchronizes the maintained
feature/plan/authority/target-programme documentation.
`5ac3ea9` refreshes the current native release-host preflight evidence; the
final aggregate below is based on that revision.
The retained release-environment manifest from the latest unconfigured live
run reports revision `5ac3ea99cb71c5e91a02ece637a2e74b6af951f5`, two changed
paths (the two pre-existing protected memory files), fifteen tracked inputs
(including the boundary policy), and digest
`6bbc393ad9224cc8a563e10d14b21dcbf379ba0383b4e9ddc4fe00b74fa2ba37`.

This ledger replaces reliance on the historical July snapshot for the current
implementation pass. It records what was actually run and keeps unavailable
live evidence explicit.

The bounded readiness/declaration groups `5553b19`, `adc7b26`, `d9b1262`,
`569231f`, and `84a1c01` were reviewed after the aggregate snapshot above.
Subsequent focused groups `09bdd19`, `22fc21a`, `234adfb`, `541d6e0`,
`e378f40`, `33e0384`, `52de581`, `f445c17`, `d3482ab`, `5f4b0eb`, and
`823fa6d`, `970f09c`, `85596b0`, `a0faf5b`, `280d363`, `3e8a0ea`, `7541b84`,
and `1761429`, `beabb95`, `46eccee`, `651ad11`, `a260e04`, `2860838`, `380daf5`, and
`fb39128`, `8b7d9f1`, `4d7a495`, `26dc138`, `bc80ad5`, `b5098e7`, `f364763`,
and `cb1c665`, `fcb0f4b`, `7828b48`, `6b0413b`, `140af1c`, `89091c1`, `83e39e6`,
`99a19a2`, `93fdfbc`, `da113af`, `993b1cb`, `d320383`, `826b4c5`,
`8ffb5df`, `f4ae7eb`, `543f392`, `40b87dd`, `32f3a76`, `ed5e551`, `bbd6ba3`, and
`1f947a9`, `c07b4a6`, `d928834`, `50cee61`, `485dfd2`, `24be4ba`, `db898e7`,
`0974434`, `29b700c`, `14968d4`, `888fde3`, `7373360`, `553f196`, `982c9c0`, and
`a3cbd99`, `118ff18`, `156597c`, `b64b15e`, `e6ab3a1`, `b418f8a`, `3108b02`, and
`392d264`, `17d25b0`, `0671eaa`, `00f81b5`, `23e2db9`, `b0ab779`, and
`b07299b`, `038d5f2`, `85369fe`, `f8829d6`, `b3fca97`, `9a80c6c`, and
`1d8aed5`, `10983c8`, `a413997`, `dbf6d10`, `8bb0a91`, `db22e60`,
`500fc57`, `7dfba8c`, `232c0bb`, `d9917f8`, `72e59ec`, `08f1610e`, and
`3116efe`, `9972b3b`, `02c59fe`, `1f20132`, `2ad5d4c`, `d45e4dd`, `84ec4ff`,
`7c1ef74`, and `3e81606` are
also recorded
in the rows below with separate documentation commits. The focused
worker-registry policy group `d8cafbb` is recorded below as well; aggregate
check counts and the
release decision are intentionally not recomputed from the dirty working tree.

## Evidence index

| Boundary | Evidence type | Result | Command or record |
|---|---|---|---|
| Governance denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `888fde3`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4151 npx playwright test e2e/governance-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied combined reads hide Refresh/Retry and all executive action forms while preserving only last-known read context; native/live ACL and WCAG evidence remain open. |
| PM integrations denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `7373360`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4152 npx playwright test e2e/app-operations.spec.ts --workers=1 --grep "PM integrations" --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied reads hide Refresh/Retry and lifecycle-plan generation/review/approval/apply controls while preserving only last-known reconciliation context; native/live ACL and provider evidence remain open. |
| Hiring Board denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `553f196`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4155 npx playwright test e2e/workers-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied worker reads hide Refresh/Retry and registration, evaluation, activation/deactivation, drain, and deletion controls while preserving only last-known worker rows; native/live ACL and worker evidence remain open. |
| Credentials denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `982c9c0`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4156 npx playwright test e2e/credentials-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied reads preserve only previously loaded redacted metadata and hide Refresh/Retry, creation, deletion, placeholder copy, selection, and audit navigation; creation and bulk mutations fail closed on authorization loss; native/live ACL and credential evidence remain open. |
| CEO Live Feed denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `a3cbd99`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4158 npx playwright test e2e/ceo-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied history/SSE/composer responses preserve only previously loaded messages, invalidate in-flight stream callbacks, and hide reconnect/retry, copy/clear/filter, and composer controls; native/live ACL and Redis/router evidence remain open. |
| CEO Command Center chat access-denied recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `038d5f2`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4175 npx playwright test e2e/ceo-chat-states.spec.ts --workers=1 --reporter=line` pass 3/3 for history 403 denial, message-submission 403 denial with retained transcript, and existing stream-failure recovery. Denied history/SSE/message responses expose a named access-status region, preserve loaded transcript context read-only, invalidate the shared stream hook's in-flight callbacks, and hide Clear/retry, quick commands, composer, and confirmation controls; native/live ACL, Redis/router, and full WCAG/native-Linux evidence remain open. |
| Licence metadata evaluator | unit/regression | PASS | Base implementation `cbdcfa6`; certification/rollout and internal-use enforcement hardening `9b84af3`; `uv run --isolated pytest packages/mas-core/tests/test_worker_governance.py packages/mas-core/tests/test_default_shipped_agents.py apps/orchestrator-api/tests/test_workers_test5_lifecycle.py -q` covers metadata-only evaluator verdicts, non-gating steward certification/rollout checks, passed-security activation, and default manifests. `scripts/check_provenance.py --json` validates source/version inventory fields without a licence allowlist or prohibited-component decision |
| Normal adapter availability | static/unit/worker reconciliation | PASS | Default manifests expose TruffleHog, Plane, OpenProject, and Ansible as selectable adapter metadata without `excluded_default_tools`; base-profile choices remain technical packaging defaults and `check_worker_reconciliation.py --json` stays green |
| Bounded security scanner adapters | static/unit/tool-service | PASS | Contract `fc528a8`; implementation hardening `5b830e9`; tools SDK alias/trace group `965ba38`; `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py packages/mas-tools-sdk/tests/test_smoke.py -q`; `uv run --isolated python scripts/check_security_adapters.py --json`; `security.scan` accepts `semgrep`, `skillspector`, and `trufflehog` compatibility aliases, routes each through the configured sandbox adapter with bounded output, and preserves the same audit/grant/rate/approval boundary |
| Security finding review register | static/security contract | PASS (review contract; technical gate blocked) | Commit `23e908e`; `uv run --isolated pytest scripts/tests/test_check_security_scan_review.py -q` and `uv run --isolated python scripts/check_security_scan_review.py --json` validate one exact-source review row, all 15 rule groups, 316 finding-count parity, 54 engine-warning follow-up, owner/next-action fields, and the explicit `licence_metadata_is_gate: false` boundary. The register is not a waiver: `technical_gate_status: blocked` remains until technical dispositions and a passing rerun are recorded. |
| Document ingest fallback | static/unit/tool-service | PASS | `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py -q`; `document.ingest` invokes Docling when installed and otherwise returns usable source text with `available: true`, `configured: true`, `degraded: true`, and `backend: plain_text_fallback`; this does not claim Docling installation or external adapter certification |
| Optional licence metadata stage | unit/regression | PASS | `test_license_metadata_stage_can_be_skipped` proves `SOURCE_REVIEW` can move directly to `SECURITY_REVIEW`; the compatibility `LICENSE_REVIEW` label is optional metadata and cannot block or delay normal internal use |
| Release environment manifest | static/reproducibility | PASS | `uv run --isolated python scripts/check_release_environment.py --json` emits `aiat.release-environment.v1` with the current revision, fifteen release-input hashes covering the network-boundary policy, operator pins, security evidence/review, worker manifests, and the prior inputs, tool identities, environment-presence flags, and a deterministic per-revision digest without values or credentials; current WSL evidence records 2 changed pre-existing memory paths, 5/6 tools available, and 0 configured optional inputs; dirty state remains a release decision reason |
| Native release-host preflight | static host/deployment prerequisite | BLOCKED (current local WSL2 host) | The opt-in `uv run --isolated python scripts/check_release_environment.py --require-native-linux --json` preflight fails closed unless the host is native Linux, Docker/Compose v2 and `runsc` are available, the tree is clean, and all ten deployment image refs are digest-pinned. The current run is retained at [`provenance/native_release_preflight.json`](provenance/native_release_preflight.json): WSL2, no registered `runsc`, dirty tree, and zero configured immutable image refs. This diagnostic does not replace native network, image/SBOM, scan, recovery, or provider evidence; licence metadata remains non-gating |
| Operator runtime/CLI pin contract | static/source | PASS | Checker/test group `dd857ae`; `uv run --isolated python scripts/check_operator_pins.py --json` verifies nine exact production tool/dependency pins and records seven host/optional/deployment capabilities as explicitly unavailable until exact identities are supplied; this technical check is independent of licence metadata |
| Backend and team-runner regression suite | unit/integration | PASS | `uv run --isolated pytest packages/mas-core/tests apps/orchestrator-api/tests apps/tool-service/tests apps/team-runner/tests -q` |
| Bounded metric labels and project-state metric | static/API integration + local live scrape + durable many-project scrape | PASS (static + local live scrape + durable many-project certificate) | `541d6e0` adds a bounded compatibility fallback for resume-time project-state reconciliation when a lightweight storage double lacks the state-only query; `eefe08f` adds per-scrape durable reconciliation and the scoped many-project certificate. `uv run --isolated pytest packages/mas-core/tests/test_metric_series_budget.py apps/orchestrator-api/tests/test_metrics.py scripts/tests/test_check_metric_series_many_projects.py -q`; `check_metric_series_budget.py --json` emits the complete AIAT label inventory, classifies every label as bounded by a protocol/registry/catalogue/bucket contract, rejects `project_id`, passes the synthetic 10,000-project/2,000-total fixture, and the live parser folds Prometheus' synthetic histogram `_created` sample into its declared family; the fresh 2026-08-18 authenticated Compose scrape and durable 10,000-project scrape each pass at 31 series with no `project_id` label and are retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json) and [`provenance/metric_series_many_projects.json`](provenance/metric_series_many_projects.json). Clean native-Linux release-host scale evidence remains open |
| HTTP/message/trace-evidence propagation | static/unit/API/dashboard + local live transport/tool | PASS (committed core; fresh Compose transport/tool read-back; broader sources open) | Core contract `77d5494`; router/agent propagation and cleanup `5bc0aae`; fail-closed live probe group `eac83ae`; tool-service HTTP/usage integration `53d38fc`; API/storage trace writer/read-model wiring `84a1c01`; worker trace/span context persistence `ceb7011`; worker source coverage contract `24c2e35`; worker/mail-edge evidence join `1d8aed5`; bounded incident projection/checker `c357fdf`; operator incident API/dashboard contract and `/logs?trace_id=…` deep link `b4b7cef`; payload-free finding chronology `869202c`; focused API/core trace, SLO, propagation, incident, dashboard build/typecheck, and deep-link tests pass. The fresh 2026-08-18 configured Compose probes pass: `/health` observes one API-request row plus one native transport span, and `time_now` observes one project-usage row plus one native tool span; scalar evidence is retained at [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json) and [`provenance/tool_trace_live.json`](provenance/tool_trace_live.json). Selected live model-backed worker, identity provider mail-edge, production retention authority, and richer live chronology remain separate evidence boundaries |
| API observation ledger | static/unit/API/fixture | PASS (bounded; API route/storage wiring reviewed) | Core contract `9c39919`; integration group `84a1c01`; focused API observation, trace propagation, trace evidence, and SLO/capacity tests pass; `check_api_observability.py --json` remains the payload-free fixture gate. Migrations `0034_api_request_observations`, `0035_trace_correlation_evidence`, and `0036_native_trace_spans` are checked in, and the orchestrator middleware now records normalized observations with trace headers and operator read-back |
| Secret-safe system diagnostics | static/unit/API | PASS | Commit `2860838`; `uv run --isolated pytest apps/orchestrator-api/tests/test_system.py apps/orchestrator-api/tests/test_test10_ops_scripts.py -q` covers database, router, tool-service, optional object-store, degraded aggregation, no-storage 503, and dependency-payload redaction. `GET /system/diagnostics` is read-only, returns bounded status/latency/connection facts or exception type, and never returns credentials, URLs, dependency payloads, or raw error text |
| Operator control CLI | static/unit | PASS | Commits `380daf5`, `f8df50e`, and `2360e07`; `uv run --isolated pytest scripts/tests/test_mas_ctl.py -q` passes six deterministic cases, and the focused operational API suite verifies the executable `scripts/mas-ctl` wrapper plus the host-owned Compose/systemd per-service restart boundary. `bootstrap` requires healthy `/health` plus `ok` diagnostics; HTTP error bodies are suppressed and `resume`/`shutdown` remain explicit authenticated POST commands |
| Communication-policy sender identity | static/unit/mocked router | PASS | Commit `fb39128`; focused policy, message-router, publish-auth, and orchestrator communication-policy tests cover sender role/team coherence, spoofed worker-to-CEO/admin paths, role-specific message types, and HTTP 403 before dedupe/enqueue. Live external-router and dashboard hierarchy evidence remain separate |
| Hierarchy communication-policy overlay | source type/lint/build + focused live E2E | PASS (focused local live image; release image open) | Implementation `8b7d9f1`; evidence-test wording cleanup `3dc61ad`; selector hardening `d5f596e`; fail-closed staged-context handling `45ee42c`; dashboard `npm run typecheck`, focused ESLint, and `npm run build` pass. `npm run test:e2e -- --workers=1 --grep "system visualization exposes hierarchy"` passes 1/1 against a current `mas/dashboard:overlay` image built from a clean explicit context. The `mas.sh` wrapper excludes all disposable `.tmp*` paths and rejects incomplete staging; direct unwrapped WSL Docker-context and native/release-image evidence remain separate, and the API-only hierarchy suites retain two explicit live-evidence skips |
| Native trace-span contract | static/unit/fixture + local live transport/tool/retention | PASS (committed core; local read-back; model/provider open) | Core contract `77d5494`; tool-service native span persistence `53d38fc`; API/storage native-span writers and trace readers `84a1c01`; worker artifact/usage trace context and payload-free storage assertions `ceb7011`; source-category coverage `24c2e35`; bounded incident projection/checker `c357fdf`; operator incident API/dashboard deep link `b4b7cef`; payload-free finding chronology `869202c`; local Postgres retention adapter/certificate `96f5fc0`; focused trace/SLO/incident/retention tests pass and scalar allow-list/redaction remains enforced. Provider mail-edge, production hold/audit/erasure/restore retention, and selected representative model-backed worker coverage remain open |
| Worker trace source coverage | static/unit/fixture + explicit live boundary | PASS (fixture; live dispatch blocked without operator selection) | Commit `24c2e35`; `uv run --isolated pytest packages/mas-core/tests/test_worker_trace_coverage.py -q` and `uv run --isolated python scripts/check_worker_trace_coverage.py --json --require-integration` require model-usage, worker-artifact, native model, native worker, and optional integration source categories. Read-only live mode requires a selected trace; dispatch requires an active model-backed worker/project/profile and `--confirm-dispatch`, emits only counts/status metadata, and does not auto-select or activate a worker. The deterministic worker/mail-edge join and certificate are implemented in `1d8aed5` with explicit scope and no mutation. No live worker-run, mail-edge, retention, sandbox, canary, or rollback evidence is claimed; licence metadata remains informational only |
| Worker/mail-edge evidence join | static/unit/fixture | PASS (deterministic; live sources open) | Commit `1d8aed5`; `uv run --isolated python scripts/check_worker_mail_edge_coverage.py --json --require-integration` passes `aiat.worker-mail-edge-coverage.v1` with worker usage/artifact/model/worker/integration sources, explicit worker/trace correlation, one verified delivery, and one verified bounce. The report is counts-only, payload-free, and non-mutating; external provider delivery, durable worker execution, and live bounce read-back remain open |
| Durable local worker-run evidence | local Compose Postgres + unit/checker | PASS (local durable evidence; live worker/provider open) | Commit `acd3f06`; `check_worker_run_postgres_evidence.py --json` drives the real controller/native adapter through one successful run, persists five transitions, two events, one usage row, one artifact row, and three native spans at migration `0036_native_trace_spans`, closes/reopens Postgres, rebuilds the payload-free trace projection, and removes only the reserved fixture namespace. Evidence is [`provenance/worker_run_postgres_evidence.json`](provenance/worker_run_postgres_evidence.json). Live model/provider, sandbox, canary/rollback, production hold/audit/erasure/restore retention, and outage evidence remain open; licence metadata remains informational only |
| Durable worker-run lease/recovery evidence | local Compose Postgres + unit/checker | PASS (local queue lease boundary; production multi-host open) | Commit `a413997`; `check_worker_lease_recovery_postgres.py --json` drives the existing storage claim, heartbeat, expiry/requeue, reclaim, terminal-transition, and durable read-back methods at migration `0036_native_trace_spans`. The live certificate denies a competing claim and wrong-owner heartbeat, requeues one explicitly expired fixture lease, reclaims it at attempt two, denies terminal re-claim, reopens Postgres with eight transitions and healthy worker metadata, emits no payload, and cleans zero remaining fixture rows. Evidence is [`provenance/worker_lease_recovery_postgres_evidence.json`](provenance/worker_lease_recovery_postgres_evidence.json). Live worker dispatch, real host-loss/split-brain, gVisor/Firecracker, live worker/provider, and outage evidence remain open; licence metadata remains informational only |
| Durable in-flight worker-version pinning | local Compose Postgres + unit/checker | PASS (complete governed pin set; independent host/process open) | Commit `7c1ef74` extending `6a10b0e`; migration `0042_worker_run_host_binding`; `check_worker_version_pinning_postgres.py --json` creates shell-v1/v2, adapter-v1/v2, bundle-v1/v2, worker source/version metadata, one AIAT steward row, and model-profile versions/snapshots. A version-one `RUNNING` run retains its original shell/adapter/bundle/steward/model-resolution snapshot while registry pointers and worker metadata advance; a queued version-two run uses the replacement shell/adapter/bundle/model snapshot. Postgres reopen, payload-free labels/states, and cleanup to zero workers/runs/stewards/model profiles/profile versions/snapshots pass. Independent host/process recovery, live dispatch, provider/sandbox recovery, and outage evidence remain open; licence metadata remains informational only |
| Deterministic worker placement policy | static/unit/fixture | PASS (pure read-only contract; worker-plane isolation and durable multi-host scheduler certified) | Commit `3fb15db` building on `db22e60`; `placement.py` and `check_worker_placement.py` implement and exercise `aiat.worker-placement.v1` over explicit host snapshots. The checker passes eligible worker-host selection, blocked capacity/constraint filtering, duplicate-host fail-closed cases, and control/tool/data rejection via `host_plane_mismatch`; the predicate enforces worker-plane identity, readiness/lease, labels, capabilities, sandbox/isolation, and slot/memory/GPU capacity with deterministic priority/free-capacity ordering, without mutation, network access, provider mutation, or worker dispatch. Evidence is [`provenance/worker_placement_contract.json`](provenance/worker_placement_contract.json). Scheduler selection/fallback is certified by `d9917f8`; host-loss/split-brain, gVisor/Firecracker, and outage evidence remain open; licence metadata remains informational only |
| Durable authenticated worker-host registry | local Compose Postgres + unit/checker | PASS (registration/heartbeat/worker-plane persistence/fencing/read-back) | Commit `3fb15db`; migrations `0037_worker_host_registry`, `0041_worker_host_planes`, and current fencing migration `0039_worker_host_fencing` plus `check_worker_host_registry_postgres.py --json` prove token-digest registration, wrong-token rejection, AIAT-owned generation-bound heartbeat lease renewal, credential-redacted public rows, durable worker-plane persistence/read-back, placement snapshot projection, connection-reopen read-back, expired-lease visibility, and scoped cleanup. Host fencing/recovery is certified separately by `72e59ec`; live worker dispatch, gVisor/Firecracker, live worker/provider, and outage evidence remain open; licence metadata remains informational only |
| Durable worker-host capacity reservations | local Compose Postgres + unit/checker | PASS (generation-aware reservation/settlement) | Commit `232c0bb`; base migration `0038_worker_host_reservations` plus current fencing migration `0039_worker_host_fencing` and `check_worker_host_reservations_postgres.py --json` prove READY/lease enforcement, host-generation retention, row-locked capacity rejection, idempotent reservation-key replay, commit/release/expiry transitions, bounded capacity projection, connection-reopen read-back, and scoped cleanup. Host fencing/recovery is certified by `72e59ec`; live worker/provider and outage evidence remain open; licence metadata remains informational only |
| Durable worker-host multi-host scheduler | local Compose Postgres + unit/checker | PASS (selection/fallback/replay; fencing separate) | Commit `d9917f8`; `host_scheduler.py`, focused scheduler unit/checker tests, Ruff, and `check_worker_host_scheduler_postgres.py --json` prove deterministic preferred-host selection, row-locked fallback after capacity rejection, globally idempotent schedule replay, draining/unleased filtering, blocked full-capacity output, connection-reopen read-back, and scoped cleanup. Host-loss/split-brain fencing/recovery is certified separately by `72e59ec`; live worker dispatch, gVisor/Firecracker, and outage evidence remain open; licence metadata remains informational only |
| Durable worker-run host binding | local Compose Postgres + unit/checker | PASS (assignment authority; live execution open) | Commit `08f1610e`; migration `0042_worker_run_host_binding`; `check_worker_run_host_binding_postgres.py --json` binds two payload-free Worker Runs to worker-plane reservations, proves preferred-host fallback, host lease-generation read-back, assignment-key replay, owner-bound commit/release settlement, connection-reopen read-back, and scoped cleanup. External worker/provider dispatch, sandbox execution, and outage evidence remain open; licence metadata remains informational only. Evidence is [`provenance/worker_run_host_binding_postgres_evidence.json`](provenance/worker_run_host_binding_postgres_evidence.json) |
| Committed worker-plane host execution | local Compose Postgres + unit/checker | PASS (native fixture dispatch; selected model-backed runtime open) | Commit `73c0bda`; `host_executor.py` and the focused checker/unit tests pass Ruff and pytest. The live certificate admits one committed worker-plane binding with matching host generation/current lease, claims the queued run, delegates to the canonical controller/native fixture adapter, reads back `SUCCEEDED`, usage/artifact/native trace evidence after connection reopen, releases binding/reservation, verifies payload-free projection, and cleans to zero fixture rows. Deployed gVisor/Firecracker, provider/remote runtime, selected model-backed worker, multi-host recovery, and licence metadata remain separate/not-gated. Evidence is [`provenance/worker_host_execution_postgres_evidence.json`](provenance/worker_host_execution_postgres_evidence.json) |
| Concurrent multi-host native worker execution and duplicate-effect protection | local Compose Postgres + unit/checker | PASS (two-host native fixture; independent-host/runtime open) | Commits `f9c717b` and `d45e4dd`; `check_worker_multi_host_execution_postgres.py --json`, focused checker tests, and Ruff pass. Two distinct worker-plane host records receive committed reservations and execute two queued runs concurrently through `WorkerHostExecutor`; a raced second claim for one run is rejected with `worker_run_claim_failed`, exactly two adapter dispatches occur, terminal and alternate-run-ID idempotency replays return the canonical `SUCCEEDED` run without redispatch, host lease-generation/current-lease equality is read back, both bindings/reservations release, two usage rows, two artifacts, three native spans per trace, and payload-free coverage survive a Postgres reopen, and scoped cleanup leaves zero fixture rows. Independent deployed hosts, selected model-backed dispatch, gVisor/Firecracker, providers, host-loss/split-brain, and provider recovery remain open; licence metadata is non-gating. Evidence is [`provenance/worker_multi_host_execution_postgres_evidence.json`](provenance/worker_multi_host_execution_postgres_evidence.json) |
| Local independent-process worker execution | local Compose Postgres + unit/checker | PASS (same-host process boundary; independent-host/runtime open) | Commits `cec6558` and `520c6bf`, documented in `95b8262`; `check_worker_independent_process_execution_postgres.py --json` launches two separate Python child processes that reconnect to Postgres and settle separate committed bindings through `WorkerHostExecutor`/`WorkerRunController`. Distinct process IDs, two `SUCCEEDED` runs, two usage rows, two artifacts, three native spans per trace, payload-free projection, durable reopen, and zero remaining fixture rows are retained in [`provenance/worker_independent_process_execution_postgres_evidence.json`](provenance/worker_independent_process_execution_postgres_evidence.json). This is a local prerequisite only; independent deployed hosts, host-loss/split-brain, providers, outages, and gVisor/Firecracker remain open; licence metadata is non-gating |
| Fenced worker-host loss and queued-run recovery | local Compose Postgres + unit/checker | PASS (scoped host-loss/requeue/reassignment plus same-host soak; independent-host/runtime open) | Commits `893293a` and `424805c`; the focused recovery/binding/executor/soak tests and Ruff pass. A host-filtered reconciliation fences one expired worker host and reservation, the canonical Worker Run lease recovery loop requeues the expired claim, stale execution is rejected before dispatch, and `reassign_after_host_loss()` moves the queued binding to host B for a native retry at attempt two; the soak repeats that path in three separate child processes. Every iteration reads back `SUCCEEDED`, released binding/reservation, one usage row, one artifact row, three native spans, payload-free coverage, Postgres reopen, and zero fixture rows. Independent deployed hosts, gVisor/Firecracker, providers, provider outage, deployment load/chaos, and disaster recovery remain open; licence metadata is non-gating. Evidence is [`provenance/worker_host_loss_queue_recovery_postgres_evidence.json`](provenance/worker_host_loss_queue_recovery_postgres_evidence.json) and [`provenance/worker_host_loss_queue_recovery_soak_postgres_evidence.json`](provenance/worker_host_loss_queue_recovery_soak_postgres_evidence.json) |
| Selected model-resolution worker-host execution | local Compose Postgres + unit/checker | PASS (production gateway adapter over bounded local fixture; external provider open) | Commits `6cef1b8`, `9a7db70`, and `8ed53df`; `check_worker_host_model_resolution_postgres.py --json`, focused checker/executor/gateway tests, and targeted Ruff pass. The certificate persists an approved Model Profile/version, resolves it deterministically, stores an immutable snapshot, rejects mismatched snapshot references before claim, registers and dispatches `GatewayWorkerAdapter` once through a bounded local gateway double, carries valid requested/resolved references through a committed worker-host run, and reads back `aiat_gateway` mode, exact provider/model usage, terminal evidence, released binding, payload-free trace coverage, Postgres reopen, and zero fixture rows. Provider/model identifiers are local fixtures; external provider calls, independent hosts, gVisor/Firecracker, and provider-backed recovery remain open; licence metadata is non-gating. Evidence is [`provenance/worker_host_model_resolution_postgres_evidence.json`](provenance/worker_host_model_resolution_postgres_evidence.json) |
| Pre-terminal model usage attribution | local fixture/unit | PASS (immutable snapshot match before terminal persistence; provider identity open) | Commit `199eb5b`; focused controller/host/governance coverage (40 tests), Ruff, and [`test_worker_model_attribution.py`](../packages/mas-core/tests/test_worker_model_attribution.py) prove successful results match the immutable model-resolution snapshot before usage or terminal evidence is persisted. Missing/incomplete snapshots and provider/model mismatches fail closed; legacy/native no-snapshot runs remain compatible. External provider identity and sandbox evidence remain open; licence metadata remains non-gating |
| Governed AIAT model-gateway worker adapter | deterministic fixture/unit + loopback HTTP | PASS (bounded model worker and real client boundary; external provider/sandbox open) | Commits `080ee18`, `f6baebc`, `cec1e4c`, and `cbbfe56`; `check_gateway_worker_adapter.py --json`, `check_gateway_worker_http_fixture.py --json`, focused adapter/checker/manifest/lifecycle tests, gateway-provider tests, and targeted Ruff pass. The real `GatewayWorkerAdapter` and `WorkerRunController` normalize an exact resolved model request and provider/model usage through an in-process gateway double; the new loopback certificate drives the real `LLMGatewayClient` through `/v1/chat/completions`, verifies bearer-secret propagation and bounded payload, retries one fixture `429`, and reads back `SUCCEEDED` terminal evidence. The `aiat_gateway` transport remains declared in the worker manifest contract, builtin runtime catalogue, and static reconciliation checker. The evidence records no external provider/network mutation and no sandbox execution. External provider-backed dispatch, durable host execution, and hardened sandbox certification remain open; licence metadata remains non-gating. Evidence is [`provenance/gateway_worker_adapter_fixture.json`](provenance/gateway_worker_adapter_fixture.json) and [`provenance/gateway_worker_http_fixture.json`](provenance/gateway_worker_http_fixture.json) |
| Gateway worker/mail-edge local composition | deterministic real-controller fixture + durable dual-Postgres certificate + signed/raw provider-facing ingress + local provider-shaped retry + retained live certificate | PASS (local scalar/durable composition, local two-attempt retry, and one configured live worker/provider/mail run; external callback/outage/sandbox open) | Commits `6ebb12c`, `fa42284`, `67f1599`, `0e0a76f`, `17f6547`, `1679341`, and `9c7e76d`; the scalar fixture checker and `check_gateway_worker_mail_edge_postgres.py --json --identity-ingress`, `--provider-ingress`, and local `--provider-recovery --provider-ingress` pass with the real `GatewayWorkerAdapter`/`WorkerRunController`, exact fixture provider/model usage, normalized delivery/verified-webhook/bounce observations, signed and Resend/Svix raw-body replay/conflict/tamper checks, two adapter attempts with one retry, durable provider-message worker/trace correlation, independent worker/identity Postgres reopen, payload-free cross-store correlation, and scoped cleanup. Retained evidence [`provenance/gateway_worker_provider_mail_edge_live.json`](provenance/gateway_worker_provider_mail_edge_live.json) records one configured `llama-3.3-70b-versatile`/`litellm` call, `SUCCEEDED` settlement, dual-Postgres reopen, delivered/bounced raw-ingress statuses `200/200`, replay/conflict/tamper outcomes `200/409/401`, payload-free coverage, generated-text redaction, and zero residual rows. Local recovery evidence is [`provenance/gateway_worker_mail_edge_provider_recovery_postgres_evidence.json`](provenance/gateway_worker_mail_edge_provider_recovery_postgres_evidence.json). External-provider callback/delivery/outage recovery, independent host/process, sandbox, and host-runtime evidence remain open; licence metadata remains non-gating |
| Gateway worker failure classification | unit/fixture + durable local retry | PASS (bounded retryable/permanent/input error semantics and local one-retry path; external provider recovery open) | Commits `b2ae516`, `1679341`, and `9c7e76d`; focused adapter/checker tests and Ruff prove pre-dispatch input errors are terminal validation failures, known transient gateway statuses are retryable provider failures, permanent gateway statuses are terminal provider rejections, and one injected local `429` retries once through durable controller settlement. Reports retain only status/cause type metadata and do not expose provider response text or credentials; licence metadata remains non-gating |
| Durable live provider transient recovery | local Compose dual-Postgres + explicit live provider retry | PASS (one injected transient 429, one real provider completion; outage/host recovery open) | Commit `def4fe9`; `GatewayWorkerAdapter` now supports a bounded provider retry budget and retains only scalar `provider_attempts`/`provider_retry_count` metadata. `check_gateway_worker_mail_edge_postgres.py --live-provider --provider-recovery --provider-ingress --allow-external-provider` passed against the configured `llama-3.3-70b-versatile`/`litellm` route: one injected transient failure, one forwarded provider completion, `SUCCEEDED` durable settlement, dual-Postgres reopen, delivered/bounced raw-ingress `200/200`, replay/conflict/tamper `200/409/401`, payload-free generated-text redaction, and zero residual fixture rows. Evidence is [`provenance/gateway_worker_provider_recovery_live.json`](provenance/gateway_worker_provider_recovery_live.json). This is bounded retry evidence, not independent-host loss, provider outage, external callback/delivery confirmation, or sandbox evidence; licence metadata remains non-gating |
| Gateway provider fallback/cooldown recovery | static/unit/fixture | PASS (local transport-outage fallback and cooldown reset; durable external outage open) | Commits `00a468d` and `48b32ef`; `LLMGatewayClient.chat_completion_with_fallback` now catches every `httpx.TransportError`, and `check_gateway_provider_recovery.py --json` proves primary outage → secondary fallback → primary recovery with model/provider cooldown clearance across three bounded attempts. The fixture uses no external network, worker dispatch, durable write, or licence gate; durable external-provider outage recovery remains open |
| Gateway worker host-executor composition | deterministic real host/controller/adapter fixture | PASS (bounded in-memory worker-plane composition; durable host/provider/sandbox open) | Commit `38c99f4`; `check_gateway_worker_host_fixture.py --json`, its focused test, host-executor/controller/adapter tests, exact evidence comparison, and targeted Ruff pass. The real `WorkerHostExecutor`, `WorkerRunController`, and `GatewayWorkerAdapter` admit a committed binding, claim and settle one queued run, attribute exact fixture model/usage, release the binding, and expose native worker/model spans plus usage and a synthetic report pointer through payload-free coverage. No generated payload, durable host, external provider, independent host, sandbox, or live recovery claim is made; licence metadata remains non-gating. Evidence is [`provenance/gateway_worker_host_fixture.json`](provenance/gateway_worker_host_fixture.json) |
| Gateway worker host failure settlement | deterministic real host/controller/adapter failure fixture | PASS (bounded transient/permanent settlement; live provider recovery open) | Commit `2abc02a`; `check_gateway_worker_host_failure_fixture.py --json`, its focused test, host-executor/controller/adapter tests, exact evidence comparison, and targeted Ruff pass. The real host boundary settles retryable `429` and permanent `401` gateway cases as `FAILED`, releases each committed binding/reservation, retains only status/cause metadata, and excludes injected provider detail from evidence. No automatic live retry, external provider recovery, durable host, or sandbox claim is made; licence metadata remains non-gating. Evidence is [`provenance/gateway_worker_host_failure_fixture.json`](provenance/gateway_worker_host_failure_fixture.json) |
| Explicit live worker-plane provider runner | bounded live provider dispatch; durable/mail/sandbox/recovery open | Commits `f999695` and `90c3e5d` | `check_gateway_worker_provider_live.py` and its focused tests pass targeted Ruff/pytest. With explicit operator opt-in, the runner reads `/v1/models`, selects `llama-3.3-70b-versatile`, and drives one bounded completion through the real host executor/controller/adapter chain. The retained certificate records one successful provider call, `SUCCEEDED` settlement, binding/reservation release, scalar status/usage metadata only, and no generated text or credentials. The default invocation remains `blocked`; durable Postgres, independent hosts, gVisor/Firecracker, provider recovery, and mail-edge callback/bounce evidence remain open; licence metadata remains non-gating. Evidence is [`provenance/gateway_worker_provider_live.json`](provenance/gateway_worker_provider_live.json) |
| Durable worker-host fencing and expired-host recovery | local Compose Postgres + unit/checker | PASS (split-brain and host-loss boundary) | Commit `72e59ec`; migration `0039_worker_host_fencing`, focused recovery unit/checker tests, Ruff, and `check_worker_host_recovery_postgres.py --json` prove re-registration generation fencing, stale-heartbeat rejection, atomic expiry of old-generation reservations, expired READY lease transition to `OFFLINE`, placement exclusion, connection-reopen read-back, and scoped cleanup. Evidence is [`provenance/worker_host_recovery_postgres_evidence.json`](provenance/worker_host_recovery_postgres_evidence.json). Live worker dispatch, gVisor/Firecracker, provider-backed recovery, and outage evidence remain open; licence metadata remains informational only |
| Mail-edge/provider observation contract | static/unit/fixture + explicit live boundary | PASS (static/persistence/projection; local ingress, raw-provider composition, and Postgres durability certified; external boundary open) | Commits `85369fe`, `cfafe38`, `2d21a2f`, `29d4da5`, `074ef8a`, `aab6285`, `2d04b30`, `1d8aed5`, and `0e0a76f`; the core fixture/checker plus focused identity/provider/orchestrator/script suites pass the payload-free `aiat.mail-edge-observation.v1` normalizer, verified-webhook/bounce coverage evaluator, duplicate/conflicting event-ID handling, signed delegated identity persistence, exact raw-body Resend/Svix verification, provider-facing ingress, durable provider-message worker/trace correlation, projected provider-span classification, optional signed identity dashboard read-back, dashboard projection, redaction boundary, the real local ASGI ingress certificate, the rebuilt Compose/Postgres certificate with migration `0003_mail_edge_observations`, independent reopen/read-back, scoped cleanup, and the deterministic worker/mail-edge evidence join. Evidence is [`provenance/mail_edge_ingress_certification.json`](provenance/mail_edge_ingress_certification.json), [`provenance/mail_edge_postgres_ingress_certification.json`](provenance/mail_edge_postgres_ingress_certification.json), [`provenance/worker_mail_edge_coverage_fixture.json`](provenance/worker_mail_edge_coverage_fixture.json), and [`provenance/gateway_worker_mail_edge_provider_postgres_evidence.json`](provenance/gateway_worker_mail_edge_provider_postgres_evidence.json). `--live` reads only a selected trace and returns `blocked` without explicit worker/trace/configuration. Configured external provider callback/delivery, selected worker/bounce read-back, outage/restore, and complete mail-span evidence remain open. Licence metadata remains informational only |
| Trace retention planner | static/unit/fixture | PASS (non-mutating; live open) | `uv run --isolated pytest packages/mas-core/tests/test_trace_retention.py -q`; `uv run --isolated python scripts/check_trace_retention.py --json` passes deterministic `aiat.trace-retention-plan.v1` retain/archive/delete/invalid classification plus explicit-boolean legal-hold protection; held rows remain retained and are excluded from deletion IDs, while ambiguous strings do not activate a hold (`9a80c6c`). No database mutation, live deletion/archive, authoritative hold registry, project narrowing, or restore parity is claimed |
| Trace retention plan API/checker | static/unit/API/contract | PASS (typed read-only; production enforcement open) | Commits `f8829d6`, `b3fca97`, and `9a80c6c`; focused API/core/script tests and `check_api_contract.py --json` pass. Operator-only `GET /observability/retention/plan` reads bounded native-span metadata and returns `mode: read-only-plan` plus `mutation_performed: false`; the typed Pydantic/OpenAPI response rejects extra fields and true mutation claims, exposes bounded legal-hold count/candidate fields, and `scripts/check_trace_retention.py --live` emits only counts/policy scalars. Production authoritative holds, erasure, project narrowing, durable audit, archive, provider recovery, and restore parity remain open |
| Trace retention execution guard | static/unit/fixture + local Postgres | PASS (deterministic rehearsal + reserved-fixture local certificate) | Commits `01996c9`, `57e13cb`, `15054ba`, `5d71309`, `67f5eae`, and `96f5fc0`; `test_postgres_retention.py`, `test_retention_execution.py`, `test_check_trace_retention_execution.py`, and focused Ruff pass. Configured `check_trace_retention_execution.py --live --json` reaches migration `0036_native_trace_spans`, keeps preview non-mutating, verifies database-local backup/read-back parity, applies one trace-scoped delete after human confirmation, preserves two held rows, and cleans the reserved namespace to zero; evidence is [`provenance/trace_retention_execution_live.json`](provenance/trace_retention_execution_live.json). Production hold authority, durable audit, erasure, archive, provider recovery, and restore rollback remain open; missing DSN still exits blocked |
| Signed identity mail SLO/trace projection | static/unit/client/API projection | PASS (bounded optional source) | Commits `f577675` and `cfafe38`; identity migrations `0002_mail_trace_correlation` and `0003_mail_edge_observations` persist safe delivery-attempt trace/span IDs plus normalized provider event rows, while signed `/v1/dashboard/mail-relay` rows are reduced to outcome/time/trace/event metadata only and filtered to the requested window; absent identity configuration remains explicit `mail_delivery: no_data` |
| SLO/capacity operational read models | static/unit/API/fixture + local transport evidence | PASS (bounded; API/storage projections integrated; live report attention) | Core contract `14b4e4b`; API/storage integration `84a1c01`; focused SLO/capacity, trace, and API contract checks pass; the fresh 2026-08-18 Compose read-back observes five services and 46 events with `capacity_status: clear`, while `slo_status: attention` and `SLO_SOURCES_NOT_OBSERVED` remain explicit. The scalar report is [`provenance/slo_capacity_live.json`](provenance/slo_capacity_live.json). `aiat.slo-policy.v1`, `aiat.slo-report.v1`, and `aiat.capacity-forecast.v1` remain descriptive; native model/tool/mail-edge/full-span sources, load/soak/chaos, and disaster-recovery evidence remain open |
| LiteLLM/OmniRoute monitoring adapter | unit/fixture | PASS (non-networking) | Commit `525a94b`; `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py -k monitoring_adapter -q`; the configured adapter emits `aiat.monitoring-analytics-plan.v1` endpoint/synthetic-check metadata, rejects credentials embedded in analytics URLs, and never probes or mutates services; Prometheus-compatible output remains optional |
| Third-party inventory policy | static validation | PASS | Implementation `cbdcfa6`; `uv run --isolated python scripts/check_provenance.py` validates 21 components, including the operator-supplied SkillSpector metadata record, while licence/restriction values remain catalogue notices only |
| Documentation authority/index | static validation | PASS | `uv run --isolated python scripts/check_docs_index.py --json`; the canonical target, thirteen current feature documents, three ordered plans, maintained links, roadmap references, and metadata-only policy markers reconcile |
| Changed Python paths | static validation | PASS | isolated `compileall` for worker registry, orchestrator API, dashboard policy, image-contract, and provenance paths |
| CEO/service/dashboard ACL | API security | PASS | Commits `d405ccb` and `e9b4da4`; `uv run --isolated pytest apps/orchestrator-api/tests/test_auth_boundary.py packages/mas-core/tests/test_dashboard_access.py -q` covers section authorization, operator-only persistence, deny-by-default principal/section combinations, and recovery invariants |
| Team-runner storage/network boundary | static + API/client contract + local live matrix | PASS (policy-backed static/API + refreshed WSL2 live) / BLOCKED (native release host) | Boundary base `43bee16`; typed storage-client and fail-closed identity hardening `22fc21a`; policy contract `96fb71f` at [`provenance/network_boundary_policy.yaml`](provenance/network_boundary_policy.yaml); the secret-safe WSL2 matrix was refreshed 2026-08-18 and passes all 11 runners with gateway allow-list, protected-service/egress denial, storage-health read-back, no forbidden runner environment names, and no Docker sockets; focused boundary tests also reject runner host ports and a public `workers` network; `uv run --isolated python scripts/check_network_boundary.py --json` and `--live --json` consume the same policy rows; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json) |
| Object-store contract fixture/runner | static/unit + local deployed live run | PASS (fixture + refreshed reconciled local MinIO live; aggregate private-network child pass; external open) | IAM reconciliation helper `5558f3c`; the secret-safe local MinIO evidence was refreshed 2026-08-11 in `22c736d`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_conformance.py packages/mas-core/tests/test_phase7_storage.py -q`; `bash infra/compose/scripts/check-minio-conformance.sh` and the deterministic fixture both pass 8/8 scoped cases after the credential-safe persisted IAM reconciliation; `check_object_store_conformance.py --live --json --compose-local` now invokes the same probe inside the orchestrator container, so the release child no longer times out on the private `minio:9000` alias; secret-safe evidence is retained at [`provenance/object_store_live_conformance.json`](provenance/object_store_live_conformance.json), with the repair helper at [`infra/compose/scripts/reconcile-minio-agent-user.sh`](../infra/compose/scripts/reconcile-minio-agent-user.sh); provider-pair/SeaweedFS, encrypted backup, retention, benchmark, and multi-host evidence remain open |
| Object-store benchmark contract | static/unit + explicit two-provider live boundary | PASS (fixture + bounded live large-object/concurrency wave; broader reliability open) | `aiat.object-store-benchmark.v1` implementation and CLI are covered by `uv run --isolated pytest packages/mas-core/tests/test_object_store_benchmarks.py -q` (5 passed); the 2026-08-18 live run against Compose MinIO plus disposable SeaweedFS 4.42 passes four concurrent 1 MiB cases and four concurrent 8 MiB cases per provider, with checksum read-back and zero remaining fixture objects. Scalar evidence is [`provenance/object_store_benchmark_advanced_provider_diverse_evidence.json`](provenance/object_store_benchmark_advanced_provider_diverse_evidence.json); each run emits no credentials and records `operator_review_required` rather than selecting a provider. The separate scalar resource profile now covers process RSS/wall/CPU; production budgets, outage, recovery, provider-managed encryption/KMS, and clean-host/disaster-recovery comparison remain open |
| Object-store multipart contract | static/unit + explicit two-provider live boundary | PASS (bounded multipart live wave; broader reliability open) | Commit `a2f35de`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_multipart.py -q` plus `uv run --isolated python scripts/check_object_store_multipart.py --json` pass the fixture contract. The fresh 2026-08-18 corrected wave against Compose MinIO plus disposable SeaweedFS 4.42 passes 8 MiB and 16 MiB payloads with 5 MiB parts, checksum read-back, explicit abort-without-object, and zero remaining fixture objects in the same reserved project used for the resource profile. Scalar verification is retained with [`provenance/object_store_resource_profile_provider_diverse_evidence.json`](provenance/object_store_resource_profile_provider_diverse_evidence.json); resource budgets, provider outage, provider-managed encryption/KMS, and clean-host/disaster-recovery comparison remain open |
| Object-store resource profile | static/unit + explicit two-provider live boundary | PASS (fresh corrected scalar wave; retained live evidence consumed by aggregate) | Commit `3791b3f` plus ledger registrations `bb0fa8f`/`0d73fbe`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_resource_profile.py -q` and `uv run --isolated python scripts/check_object_store_resource_profile.py --json` pass the bounded fixture. The fresh corrected live wave runs eight 1 MiB/8 MiB checksum cases at concurrency four per provider with identical procfs sampling, zero errors, and cleanup; MinIO and SeaweedFS both pass. Multipart upload/read-back, 5 MiB-part abort behavior, and zero cleanup also pass in the same reserved namespace. The release aggregate now validates the retained scalar certificate without host-side provider credentials. Evidence is [`provenance/object_store_resource_profile_provider_diverse_evidence.json`](provenance/object_store_resource_profile_provider_diverse_evidence.json); the prior nonexistent-alias attempt is explicitly excluded as harness/configuration invalid. No production resource threshold or provider-selection decision is made |
| Object-store copy/parity fixture | static/unit | PASS (fixture only) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_migration.py -q` and `uv run --isolated python scripts/check_object_store_copy.py --json`; `aiat.object-store-copy.v1` copies explicit project-scoped refs, verifies source/target checksum and size after read-back, preserves source data, and cleans failed target copies; provider-pair migration, cutover, backup, restore, and benchmark evidence remain open |
| Object-store live copy/parity runner | live-runner readiness | PASS (live blocked) | `uv run --isolated python scripts/check_object_store_copy.py --live --json` returns exit 2 with `status: blocked` when source/target configuration is absent; the runner inventories source bytes into checksum-bearing refs, refuses empty inventory, and performs non-destructive target read-back parity when configured; provider-pair evidence, cutover, backup, restore, and benchmark remain open |
| Object-store migration workflow | static/unit/fixture + guarded live rehearsal | PASS (fixture + disposable MinIO/SeaweedFS rehearsal; production gates open) | Commits `ecbef00` and `c086a91`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_rollout.py packages/mas-core/tests/test_object_store_migration_runner.py scripts/tests/test_check_object_store_migration.py -q` and focused Ruff pass. The fixture and guarded `--live` checker prove `aiat.object-store-migration.v1` inventory → verified copy → one dual-write → human-confirmed cutover → human-confirmed rollback with reserved-prefix cleanup to zero; evidence is [`provenance/object_store_migration_provider_diverse_evidence.json`](provenance/object_store_migration_provider_diverse_evidence.json). The rehearsal does not change deployment routing or retention authority; production cutover, provider outage, KMS, clean-host, and disaster-recovery evidence remain open |
| Dashboard ACL policy | unit/contract | PASS | Core policy/test group `d405ccb`; enforcement/proxy group `e9b4da4`; `uv run --isolated pytest packages/mas-core/tests/test_dashboard_access.py apps/orchestrator-api/tests/test_auth_boundary.py -q` covers finite sections, deny-by-default unknowns, deterministic persistence, operator recovery invariants, and authenticated principal separation |
| Dashboard operator proxy/type contract | static/typecheck | PASS | Commits `e9b4da4` and `fc4f0fa`; credential and identity proxies use the strict operator key plus bounded section context, and the project-evidence error-state repair restores the workspace `npm run typecheck` contract |
| Projects list denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `17d25b0`; `npm run lint` (two unrelated hook warnings), `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4166 npx playwright test e2e/projects-states.spec.ts --workers=1 --reporter=line` pass 4/4 for stale recovery, first-load 403 denial, retained-read 401 denial, and deletion 403 denial. Denied paired reads and mutations expose a named access-status region, preserve only previously loaded project/flow definitions as read-only text, clear selection, and hide Refresh/Retry, New Project, filters, sorting, archive, and delete controls; native/live ACL and project evidence remain open |
| Dashboard local Compose E2E matrix | authenticated local UI | PASS WITH EXPLICIT SKIP (58/59) | `e378f40` hardens the credential and tools operation selectors against accessible-name/strict-mode drift; `d5f596e` uses the overlay accessible name and explicit trace-control IDs; `514aeeb` keeps hiring evaluation details expanded after evaluation; `npm run test:e2e -- --workers=1` in `apps/mas-dashboard` passes 58/59. The WSL2 Compose run covers operational UI, hierarchy communication-policy/path tracing, CEO chat/hiring, project workspace, schema-driven flow editing, all eight branching/recovery scenarios, hiring-board evaluation details, identity stale-record/retry state, PM integration conflict/stale retry, project-detail stale/retry state, runtime-status panels, shell skip-link/mobile focus recovery, evidence-detail scalar redaction, theme preference, and all stale/recovery read surfaces. Secret-safe evidence is [`provenance/dashboard_e2e_live.json`](provenance/dashboard_e2e_live.json); the live DLQ replay case is intentionally skipped pending an operator-owned safe fixture, and native-Linux/mobile/WCAG evidence remains open |
| Identity-resource stale/retry and table accessibility | static/build/source-built Playwright (preparatory P1) | PASS | Commits `46eccee`, `651ad11`, `a260e04`; `npm run typecheck`, focused ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4142 npx playwright test identity-states.spec.ts --workers=1 --reporter=line` pass (focused browser coverage 1/1). The shared `IdentityResourcePage` loader aborts obsolete requests, ignores late responses by generation, retains rows and the warning while retrying, and clears stale state after recovered data arrives; the table exposes a caption, scoped column headers, explicit row-action names, and 44px action targets without rendering sensitive fields, while the page exposes named main/status/metadata/table regions, explicit busy state, and decorative-icon suppression. Provider/live identity evidence remains open. |
| External-account action policy | static/API/unit | PASS | Commit `f577675`; `PYTHONPATH=apps/identity-service uv run --isolated pytest apps/identity-service/tests/test_identity_service.py -q`; `aiat.external-account-action-policy.v1` exposes category-sensitive signup, rotation/closure human gates, immediate suspension, and governed browser-session rules |
| Production image contract | static + fail-closed live identity | PASS (static + SBOM schema; live blocked) | Contracts `7d69fbd`, `1d373ee`, `42b03a3`, and `2804a9f`; `uv run --isolated pytest packages/mas-core/tests/test_image_provenance_runner.py -q` (8 passed) and `uv run --isolated python scripts/check_image_provenance.py --json` pass the immutable Compose/Dockerfile contract, unique inventory/ref identity, checked-in build-recipe, bounded digest/lock, and repository-contained artifact-path checks, plus the checked-in `production-image-lock.example.env` coverage for every application image input. `--require-sbom` validates CycloneDX format/version, metadata component, named components, and unique `bom-ref` values without inspecting licence metadata; `--live --require-sbom --json` returns exit 2 because deployment-supplied immutable `*_IMAGE_REF` values and release artifacts are absent. Development-only wrapper defaults are isolated in `b9a77e9` |
| Local runtime identity/release wrapper | static Compose/shell/Docker validation | PASS | Wrapper hardening `fd41874`; runner prompt timezone implementation `c955ac8`; `mas/infra/compose/mas.sh validate`, `check_image_provenance.py --json`, and `check_operator_pins.py --json` pass. The wrapper supplies only development defaults, requires distinct CEO/worker keys, propagates the company IANA timezone into runner prompts, pins its `uv` image bootstrap, and documents identity migration head `0002_mail_trace_correlation`; direct production Compose still requires populated immutable image refs |
| Worker manifest/runtime/provenance reconciliation | static + read-only authenticated local live binding + scan-evidence linkage | PASS (39/39 persisted defaults; technical findings remain open) | Runtime catalogue/checker/test group `80e0ca3`; the secret-safe Compose live evidence was refreshed 2026-08-18 in `cb47e3b`; `uv run --isolated python scripts/check_worker_reconciliation.py --json` plus authenticated `--live --json` matches all 39 checked-in defaults to the live `/capabilities/workers` rows with zero missing rows or binding mismatches. Secret-safe evidence is [`provenance/worker_reconciliation_live.json`](provenance/worker_reconciliation_live.json). Coding/tester rows link to `security_scan_evidence.yaml`, which records 316 Semgrep findings and 54 engine warnings, so both remain non-activatable; host package availability is advisory and Compose runtime imports are recorded separately |
| Worker registry grant/update policy | static/API fixture | PASS (66 focused cases; adjacent suites pass) | Commit `d8cafbb`; `uv run --isolated pytest apps/orchestrator-api/tests/test_workers_test4_config.py -q` covers the four accepted update-policy values, invalid-policy `422` responses, direct/persisted forbidden-tool denial, and no-write-on-denial behavior. Capability/team authority changes revalidate persisted `required_tools` against the canonical tool policy before storage mutation; licence/restriction metadata remains informational only |
| Default worker implementation bindings | static contract + explicit live boundary | PASS (static; live blocked by operator environment) | `uv run --isolated pytest packages/mas-core/tests/test_default_worker_bindings.py -q`; `uv run --isolated python scripts/check_default_worker_bindings.py --json` reconciles all 15 documented default worker slots across department, runtime, transport, isolation, runtime-catalogue support, runtime/integration adapter entrypoints, capability, adapter configuration, and required tools. `--live --json` is fail-closed and returns exit 2 until an operator selects a live environment; the report never treats licence metadata as a gate |
| Worker-run lifecycle contract | deterministic controller fixture + explicit live boundary | PASS (fixture; live blocked without operator-selected run) | Commit `fe6fb8d`; `uv run --isolated pytest packages/mas-core/tests/test_worker_run_lifecycle.py -q`; `uv run --isolated python scripts/check_worker_run_lifecycle.py --json` drives the real `WorkerRunController`/`NativeWorkerAdapter` through checkpoint persistence, pause/resume, cold cancellation, cold-crash failure normalization, lease-expiry requeue, and artifact/usage-before-terminal ordering; `--live --json` returns exit 2 without mutating a live run, while database/sandbox/canary/rollback evidence remains open |
| Selected worker-run readiness preflight | fixture + authenticated read-only live diagnostic | PASS (fixture); BLOCKED (current selected live state) | Commit `5553b19`; `uv run --isolated pytest packages/mas-core/tests/test_worker_run_readiness.py -q` and `uv run --isolated python scripts/check_worker_run_readiness.py --json` pass the complete snapshot fixture. The live checker requires explicit worker/project UUIDs, reads only bounded worker/project/company/assignment/budget/profile/health records, and never auto-selects, activates, provisions identity, reserves budget, dispatches, or returns payloads. The current local coding-worker/terminal-project selection exits 2 with inactive worker, missing immutable pointers, terminal project, and missing assignment; identity, sandbox runtime, canary, live-run, rollback, and licence metadata remain separate/not-gated |
| Selected steward certification readiness preflight | fixture + authenticated read-only live diagnostic | PASS (fixture); BLOCKED (current selected live state) | Commit `adc7b26`; `uv run --isolated pytest packages/mas-core/tests/test_worker_steward_readiness.py -q` and `uv run --isolated python scripts/check_worker_steward_readiness.py --json` pass the complete candidate fixture. The live checker requires explicit worker/candidate UUIDs, reads only bounded worker/steward/candidate records, and never generates or certifies a candidate, approves, activates, rolls out, dispatches, or returns payloads. The current local coding-worker selection exits 2 with `steward_not_ready`, `security_scan_not_passed`, and `candidate_not_found`; licence metadata remains separate/not-gated |
| Selected worker-run readiness health guard | fixture + mocked read-only live diagnostic | PASS | Commits `2eea80a` and `dac268c`; `test_check_worker_run_readiness.py` proves that both a 503/unavailable and a 200 response with no usable `health_status` become the stable `read_worker_health_unavailable` blocker. The preflight remains read-only, does not activate/dispatch/reserve state, and does not use licence metadata as a gate |
| Team-runner manifest identity bindings | static/unit | PASS | Commit `d9b1262`; `uv run --isolated pytest packages/mas-core/tests/test_team_worker_manifest_refs.py apps/team-runner/tests/test_team_config.py -q` and `uv run --isolated python scripts/check_team_worker_manifest_refs.py --json` reconcile 11 team files and 39 exact agent→manifest IDs; no registration/activation mutation and licence metadata remains informational |
| Team-runner startup manifest enforcement | unit/startup contract | PASS | Commit `569231f`; `uv run --isolated pytest apps/team-runner/tests/test_team_config.py apps/team-runner/tests/test_shutdown.py -q` verifies mounted-manifest reconciliation, fail-closed missing references, exact `AgentConfig` propagation, and health metadata; startup remains read-only and does not register or activate workers |
| Exact OpenCode security scan | external source + Semgrep evidence | FINDINGS_REVIEW_REQUIRED | Exact commit `10c894bdeef3618f5666fb506ef7f9491bb964d8` (`v1.17.13`) scanned with Semgrep `1.168.0`; 5,062 files, 316 findings, 19 `ERROR`, 54 engine warnings; evidence digest `e960a57d7b397d8af9271025b2025b0f46567967b4c684eaab8652a75d47d3b5`; the companion [`provenance/security_scan_review.yaml`](provenance/security_scan_review.yaml) keeps one operator-owned open review with all findings mapped to explicit next actions and does not waive the gate |
| Worker certification matrix | deterministic static/unit declaration | PASS | Commit `a62ddb7` (generator group `4c5fd68`); `uv run --isolated pytest packages/mas-core/tests/test_worker_certification_matrix.py -q` and `uv run --isolated python scripts/generate_worker_certification_matrix.py --check`; 39 rows separate declared runtime/adapter inputs from pending security, canary, and live evidence; the regression checks generated-artifact parity, exact manifest coverage, and metadata-only licence handling |
| Microsoft Agent Framework adapter boundary | unit/fixture + isolated real-package probe | PASS (isolated profile; default activation open) | Commit `b937a89`; focused MAF adapter/compatibility/certification tests pass; the adapter accepts an explicit client boundary, normalizes real `AgentResponse.text`, and fails closed without a client for the installed MAF shape; provider and live activation remain separate |
| Microsoft Agent Framework/MCP compatibility lock | static contract + preflight | PASS (isolated profile; default activation blocked) | Commit `b937a89`; `uv run --isolated python scripts/check_runtime_compatibility.py --json` preserves the production MCP `1.23.3` lock and reports activation blockers, while the optional profile locks MAF `1.13.0`/MCP `1.29.0` and retains [`provenance/maf_runtime_certification.json`](provenance/maf_runtime_certification.json); no default-image installation or network mutation occurs |
| Microsoft Agent Framework isolated profile certification | isolated real-package deterministic fixture | PASS (fake client only) | Commit `b937a89`; `uv run --isolated python scripts/check_maf_runtime.py --python <isolated-maf-venv>/bin/python --json` imports MAF `1.13.0`/MCP `1.29.0`, runs `MicrosoftAgentFrameworkAdapter` to `bounded-fixture-result`, records one fake-client call, and verifies health/shutdown; provider/MCP/project/tool calls, security, sandbox, canary, live worker-run, and rollback remain not checked |
| Code-review adapter catalogue/default | static/unit/fixture | PASS (external candidates blocked) | Catalogue `fc528a8`; implementation `5b830e9`; `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py apps/tool-service/tests/test_default_shipped_tool_catalog.py apps/tool-service/tests/test_review_tool_contract.py -q`; `uv run --isolated python scripts/check_code_review_adapters.py --json` proves the local deterministic diff reviewer is the default and generic external candidates cannot activate without exact source/revision/version evidence. Review submissions preserve structured findings and publish canonical `REVIEW_RESPONSE` envelopes; the bind-mounted project Git initializer uses inline identity flags and avoids config-lock chmod assumptions |
| Worker runtime readiness | static + live import probe | PASS (local Compose image imports; certification remains open) | Commit `4c5fd68`; `uv run --isolated pytest packages/mas-core/tests/test_worker_runtime_readiness.py -q`; `uv run --isolated python scripts/check_worker_runtime_readiness.py --json` reports 39 manifests without certifying them; the aggregate `--live --json --compose-local` probe imports required LangGraph/CrewAI packages inside `mas-orchestrator-api-1` and passes with no required runtime blockers; secret-safe evidence is retained at [`provenance/worker_runtime_readiness_live.json`](provenance/worker_runtime_readiness_live.json), while optional MAF/AutoGen/Letta imports and security, sandbox, canary, live-run, and rollback checks remain separate |
| Default runtime install profile | static source/lock/Dockerfile contract | PASS | Commits `9a10a4b` and tracked workspace lock `2b13d89`; `uv lock --check` and `uv run --isolated python scripts/check_runtime_install_profile.py --json` reconcile the `runtime-default` LangGraph/CrewAI extra, locked versions (`langgraph` 0.6.11 and `crewai` 1.6.1), runtime-catalogue imports, and production Dockerfile install command; package imports and worker certification remain separate |
| Workspace lock reproducibility | clean-archive/static packaging | PASS | Tracked lock commit `2b13d89`; `uv sync --locked --dev --dry-run` resolves 351 packages and would install the declared workspace without lock drift, while a clean Git archive contains `mas/uv.lock` and passes `check_docs_index.py` |
| Default worker steward lifecycle | deterministic domain fixture + restart-safe persistence path | PASS (domain evidence; live open) | Existing steward contract `c80e339`, fixture coverage test `fe6fb8d`, durable compatibility-matrix writer/reader and bounded evidence test `ceb7011`; `uv run --isolated python scripts/check_worker_steward_contract.py --json` plus `uv run --isolated pytest packages/mas-core/tests/test_worker_steward_contract.py apps/orchestrator-api/tests/test_steward_rehydration.py packages/mas-core/tests/test_compatibility_matrix_persistence.py -q` exercises dedicated steward, immutable candidate, compatibility matrix, certification/approval, shadow → read-only canary → promotion, regression blocking, and pre-activation rollback for both externally sourced default workers; certification records the matrix in the same-process steward cache and durable store, while API rehydration restores durable active bundle/adapter IDs plus persisted compatibility-matrix rows, normalizing profile/capability JSON and failing closed on malformed evidence. Evidence is [`provenance/worker_steward_contract.json`](provenance/worker_steward_contract.json). Database reconciliation, security, sandbox, live canary, and worker-run evidence remain open |
| Sandbox runtime readiness | static + live Docker runtime probe | PASS (static Compose/worker contract; live gVisor blocked) | Commits `a24c554` and `2c098f5`; `uv run --isolated pytest packages/mas-core/tests/test_sandbox_runtime_readiness.py -q`; `uv run --isolated python scripts/check_sandbox_runtime_readiness.py --json` reconciles 39 manifests, 10 hardened external workers, and the AIAT-owned `opencode-runtime` Compose boundary (internal-only network, no host ports, non-root/read-only, cap-drop ALL, no-new-privileges, bounded CPU/memory/PIDs, noexec/nosuid tmpfs); the Docker CLI is reachable but `--live --json` returns exit 2 because the required gVisor `runsc` runtime is not registered and no `runc` fallback is permitted, while digest-pinned smoke/network, canary, and Firecracker checks remain separate |
| Firecracker high-risk worker launch contract | static + read-only host readiness | PASS (contract); BLOCKED (host) | Commit `5ed0a0b`; focused contract/checker tests and Ruff pass. `check_firecracker_worker_pool.py --live --json` validates the immutable kernel/rootfs, bounded resource, read-only, deny-by-default egress, opaque-secret, artifact, and cleanup contract without launching a VM; the current host lacks both `aiat-firecracker-launcher` and `firecracker`, so microVM smoke/network/provider/recovery are not checked and no weaker fallback is allowed. Evidence is [`provenance/firecracker_worker_pool_readiness.json`](provenance/firecracker_worker_pool_readiness.json) |
| Runtime benchmark readiness | read-only live API probe | PASS (fresh Compose dependency dry-run; certification boundary remains open) | Base contract `ad31793`; bounded endpoint/test hardening `4d61279`; the secret-safe authenticated Compose run was refreshed 2026-08-18 in `cb47e3b`; `uv run --isolated pytest packages/mas-core/tests/test_runtime_benchmarks.py apps/orchestrator-api/tests/test_epsilon_runtimes.py -q` passes. Third-party imports execute off the API event loop with a capped timeout and explicit `benchmark_timeout`/`benchmark_error` responses; the live probe passes one LangGraph and one CrewAI dependency dry-run and is retained at [`runtime_benchmarks_live.json`](provenance/runtime_benchmarks_live.json). Missing API/package/validation evidence remains exit 2 when encountered; package benchmarks never certify a worker canary/live run/rollback |
| Default runtime adapter conformance | unit/fixture + Compose package/lifecycle and lock-parity probe | PASS (adapter and exact-lock evidence; worker certification open) | Commit `9a10a4b`; `uv run --isolated pytest packages/mas-core/tests/test_runtime_adapter_conformance.py -q`; `docker exec mas-orchestrator-api-1 python /app/scripts/check_runtime_adapter_conformance.py --live --json` passes actual `LangGraphAdapter` and `CrewAIAdapter` classes with locked LangGraph `0.6.11` and CrewAI `1.6.1`, verifying manifest/message translation, bounded completion, health, and shutdown without model/tool/provider/project calls; secret-safe evidence is [`provenance/runtime_adapter_conformance_live.json`](provenance/runtime_adapter_conformance_live.json). Sandbox, canary, live worker-run, and rollback evidence remain open |
| Model override expiry and budget settlement replay | focused unit/storage | PASS (preparatory P1) | Commit `63b2db5`; `PYTHONPATH=mas/packages/mas-api-sdk ./.venv/bin/pytest -q apps/orchestrator-api/tests/test_worker_dispatch_governance.py packages/mas-core/tests/test_phase7_storage.py -k 'model_override_expiry or settle_budget_reservation'`; serialized expiry fails closed and terminal settlement retries are no-ops; provider-specific live recovery and settlement chaos evidence remain open |
| LLM gateway transient failure vocabulary | static/unit (preparatory P1) | PASS | Commit `288996e`; `uv run --isolated pytest packages/mas-core/tests/test_gateway_providers.py -q`; normal/streaming/fallback gateway paths share explicit 408/409/412/425/429/5xx retry classification while permanent client 4xx responses remain non-retryable |
| Model/provider cooldown failover | static/unit/persistence (preparatory P1) | PASS | Commit `288996e`; `uv run --isolated pytest packages/mas-core/tests/test_observability.py packages/mas-core/tests/test_persistence.py packages/mas-core/tests/test_gateway_providers.py -q`; bounded model cooldowns, two-failure provider cooldowns, persisted state, SmartRouter exclusion, and automatic fallback filtering pass; provider-specific live outage/recovery and chaos evidence remain open |
| Model profile/runtime catalogue | static/unit/API/dashboard + local live read-only reconciliation (preparatory P1) | PASS WITH FINDINGS (approved coverage present; one operator-owned stale row) | Commits `288996e`, `09bdd19`, `ab0a0fe`, `381dbcc`, evidence refresh `db51dac`, and route checker `f6ed16f`; the 2026-08-18 secret-safe read-only run observes 93 registry models, 94 persisted profile versions, 93 approved covered entries, and no pending registered model. The explicit `aiat/omniroute-coding` identity alias reconciles to canonical `litellm/omniroute-coding`; one stale `local/test-model-1` row remains an operator-visible finding. The local `/v1/models` route exposes nine entries and all five AIAT aliases; no completion/provider call or mutation was performed. Commands remain `uv run --isolated python scripts/check_model_profile_catalogue.py --live --json --require-approved` and `uv run --isolated python scripts/check_model_gateway_readiness.py --live --json`; evidence is retained at [`provenance/model_profile_catalogue_live.json`](provenance/model_profile_catalogue_live.json) and [`provenance/model_gateway_readiness_live.json`](provenance/model_gateway_readiness_live.json). The API/dashboard export and operator proxy remain deterministic; provider recovery remains open |
| Default model-profile bootstrap | static/unit/API seed path + live persisted evidence | PASS WITH FINDINGS | Commits `09bdd19` and `381dbcc`; `uv run --isolated pytest packages/mas-core/tests/test_model_profile_seeder.py -q`; startup and `/system/seed-default-company` invoke the idempotent `aiat.model-profile-bootstrap.v1` declaration for all 93 registry models plus `opencode-phase0b-coding`/`omniroute-coding`, preserve operator rows, and block conflicts; the live catalogue confirms 94 persisted versions and 93 approved registry bindings, with the OpenCode/LiteLLM identity alias reconciled and one operator-owned stale row retained as an explicit finding |
| Model profile live verifier | fail-closed runner | PASS (local live; one finding retained) | Commits `381dbcc` and `db51dac`; `uv run --isolated python scripts/check_model_profile_catalogue.py --live --json --require-approved` passes the approval gate against the local API and reports `pass_with_profile_findings`; missing API/auth still exits 2, while the retained report records 93 approved covered entries, no pending model, and one stale operator-owned row without exposing profile payloads |
| Executive reconciliation and role views | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | Core model/checker `be030ac`; API/dashboard integration `d1b8839`; `uv run --isolated pytest packages/mas-core/tests/test_executive_reconciliation.py packages/mas-core/tests/test_executive_reconciliation_runner.py apps/orchestrator-api/tests/test_model_profile_catalogue.py -q`; deterministic `aiat.executive-reconciliation.v1` aggregates durable project/usage/worker-run/budget/model evidence, `aiat.executive-views.v1` projects bounded CFO/CTO/CEO summaries, and `scripts/check_executive_reconciliation.py --live --json` provides a secret-safe bounded coverage/finding verifier with optional `--require-clean`, returning exit 2 when live API/DB evidence is unavailable. API/dashboard routes, reservation/settlement wiring, and live DB population/provider recovery remain separate integrations |
| Executive write action envelope | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | API/dashboard integration `d1b8839`; `uv run --isolated pytest apps/orchestrator-api/tests/test_executive_actions.py apps/orchestrator-api/tests/test_api_contract.py -q`; `aiat.executive-action.v1` routes CFO model overrides through the durable request owner, CTO worker runs through canonical governed dispatch, and CEO privileged actions through the audited gate; worker output/payload secrets are excluded from responses; dashboard proxies and the typed confirmation panel cover all three routes; local profile evidence is 93/94 approved covered with one operator-owned stale finding, while provider/live recovery, broader governance forms, and chaos evidence remain open |
| Guarded self-improvement lifecycle | static/unit/storage/API fixture + local Compose Postgres certificate; explicit worker/provider boundary | PASS (fixture/API/local durable lifecycle; live worker/provider blocked) | Core contract/fixture commit `4d8dddf`; API/storage integration `64218ab`; `uv run --isolated pytest packages/mas-core/tests/test_self_improvement.py -q` and `uv run --isolated pytest mas/scripts/tests/test_check_self_improvement_postgres_evidence.py -q`; `uv run --isolated python scripts/check_self_improvement_lifecycle.py --json` proves the typed project request, independent gates, human-only promotion, shadow/canary, exact rollback, bounded terminal outcome/cost/incident/rollback/KPI learning, and frozen five-kind artifact manifest/read-back parity. `10983c8` adds the reserved local Compose Postgres certificate: migration `0036_native_trace_spans`, six technical gates, stale CAS rejection, human approval, five durable checksum/size read-backs, exact prior-version rollback, terminal outcome/history read-back, and scoped cleanup; evidence is [`provenance/self_improvement_postgres_evidence.json`](provenance/self_improvement_postgres_evidence.json). `--live --json` remains blocked until worker/control-plane integration is configured; external certified-worker/provider, issue/worker-run/branch/artifact/budget/deployment evidence remain open; licence metadata is informational only |
| Self-improvement candidate detection | static/unit/fixture; live signal boundary | PASS (fixture; live blocked) | Core contract/fixture commit `4d8dddf`; `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` proves `aiat.self-improvement-candidate-detection.v1` normalization for defect/metric/upstream-update/cost/operator-goal signals, exact duplicate collapse, conflicting-ID rejection, deterministic risk/budget mapping, secret-safe metadata, and zero project/budget/credential/deployment side effects; `--live` remains blocked until operator-selected signal sources exist |
| CEO evidence envelope and citation route | static/unit/API/dashboard (preparatory P1) | PASS (bounded deterministic + fallback citation surface) | Commit `f1801bb`; `2ca5f3d` adds operator-authenticated artifact/usage detail authorities and `6c52552` retains the last safe projection through refresh failure; `uv run --isolated pytest apps/orchestrator-api/tests/test_ceo_chat.py apps/orchestrator-api/tests/test_evidence_detail_routes.py -q` and `check_api_contract.py --json` pass. API-owned CEO actions/read responses publish and return `aiat.ceo-evidence.v1` with bounded scalar references and traces, never arbitrary payloads or secret values; the legacy fallback accepts only explicit stripped `AIAT_EVIDENCE: kind=id` markers and labels them `unverified`; dashboard renders owning-section links for project/flow/governance/worker/credential/integration/tool/project-evidence/log surfaces plus dedicated `/evidence/{kind}/{id}` records. The bounded `aiat.evidence-detail.v1` projection now covers fourteen scalar citation kinds through `8fefc8b`/`c8505eb`/`dc50719`/`5357166`/`ec8cf67`/`2ca5f3d`; workspace typecheck/build/lint pass, focused source-built coverage is 9/9, stale-refresh recovery retains safe detail and the canonical link, while broader stale/offline recovery and full golden paths remain open |
| Compose network denial/allow matrix | live integration | PASS (policy-backed refreshed local WSL2 matrix); BLOCKED (native release host) | The `43bee16` Compose boundary plus `aiat.network-boundary-policy.v1` (`96fb71f`) were exercised without removing volumes. `check_network_boundary.py --live --json` passes all 11 runners with named gateway access, protected-service and unapproved-egress denial, control-plane storage health, no forbidden runner environment names, and no Docker sockets; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json). Native-Linux recreation, provisioned-key dashboard ACL coverage, and post-fix closure of `DEF-2026-07-14-036` remain open. |
| CEO service identity and section ACL | static + API security | PASS (static/API + authenticated local matrix); native UI/release matrix open | Commits `d405ccb` and `e9b4da4`; distinct CEO/worker principals, persisted `system_config` ACL, operator-only update, fail-closed dashboard section middleware, strict operator-key proxies, and human/CEO/service/worker matrix pass. The refreshed local endpoint matrix is retained at [`provenance/dashboard_acl_live.json`](provenance/dashboard_acl_live.json); native-Linux deployment/UI denial evidence remains open |
| Production image digest inventory | supply-chain/release | PASS (static contract + SBOM schema); live blocked/open | `1d373ee` adds the non-secret `infra/compose/production-image-lock.example.env` template and coverage regression; `42b03a3` makes `check_image_provenance.py --require-sbom` reject malformed CycloneDX artifacts; `2804a9f` adds unique image/ref identity, checked-in build-recipe, bounded digest/lock, and repository-contained artifact-path validation; `docs/provenance/production_images.yaml` matches Compose image variables; fixed infrastructure refs and Dockerfile bases carry digests; application/gateway refs require deployment-supplied immutable `*_IMAGE_REF` values; `check_image_provenance.py --live --require-sbom --json` remains fail-closed without deployment refs/artifacts; development `mas.sh` fallbacks are isolated to the dev overlay; clean pull/source/lock/scan reconciliation remains open |
| Object-store backup/restore manifest | static/unit fixture + local same-provider live rehearsal | PASS (fixture + refreshed local MinIO rehearsal; provider-diverse open) | Restore safety group `93bf755`; the secret-safe local rehearsal was refreshed 2026-08-11 in `22c736d`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_backup.py -q` includes a non-empty-target regression; `uv run --isolated python scripts/check_object_store_backup_restore.py --json` proves `aiat.object-store-backup.v1` source → backup → empty-target preflight → clean-target restore with exact key/checksum parity and `clean_target_verified: true`; `bash infra/compose/scripts/check-minio-backup-restore.sh` passes two disposable objects with manifest/read-back parity and scoped cleanup, retained at [`provenance/object_store_backup_restore_live.json`](provenance/object_store_backup_restore_live.json); the explicit three-provider `--live --json` runner still blocks without separate providers, encryption, retention, provider durability, and clean-environment disaster-recovery evidence |
| Object-store provider-pair recovery | static/unit + live same-host MinIO pair | PASS (bounded adapter-boundary recovery; provider-diverse/open outage gates) | Commits `351444a`, `f385bd7`, and `3833631`; `uv run --isolated ruff check scripts/check_object_store_provider_pair.py scripts/tests/test_check_object_store_provider_pair.py`, `PYTHONPATH=scripts uv run --isolated pytest -q scripts/tests/test_check_object_store_provider_pair.py`, and the fixture runner pass `aiat.object-store-provider-pair.v1`. The live rehearsal dual-writes three checksum-bearing objects, rejects a simulated primary loss at the AIAT adapter boundary, restores from the secondary into a clean target with zero primary calls, and cleans all three reserved prefixes to zero; scalar-only evidence is [`provenance/object_store_provider_pair_evidence.json`](provenance/object_store_provider_pair_evidence.json). It uses two local MinIO endpoints and does not claim provider-diverse durability, a provider process/network outage, provider-managed SSE/KMS, clean-host restore, or disaster recovery; licence metadata remains non-gating |
| Object-store provider-diverse pair and benchmark | live disposable MinIO + SeaweedFS adapter boundary | PASS (endpoint-diversity/comparison observation; durability and migration open) | Evidence/docs group `f52fde6` retains [`provenance/object_store_provider_pair_provider_diverse_evidence.json`](provenance/object_store_provider_pair_provider_diverse_evidence.json) and [`provenance/object_store_provider_benchmark_evidence.json`](provenance/object_store_provider_benchmark_evidence.json). Compose MinIO and disposable SeaweedFS 4.42 each pass three checksum cases at sizes 1/32/4096 bytes; the pair checker passes three-object dual-write, adapter-boundary primary-loss rejection, secondary-only clean restore, and zero cleanup. No provider process is stopped; provider durability/custody, KMS, clean-host/disaster recovery, and migration cutover remain open; actual process outage/recovery is certified separately; licence metadata remains non-gating |
| Object-store provider-process outage/recovery | live disposable MinIO + SeaweedFS process stop/restart | PASS (both providers; retained live evidence consumed by aggregate) | Implementation `d92b3dc`; scalar evidence [`provenance/object_store_provider_outage_live_evidence.json`](provenance/object_store_provider_outage_live_evidence.json); release child `object_store_provider_outage` registered in `407a4f9` and retained-live validation in `0d73fbe`/`14ae490`. The identical 64 KiB/1 MiB workload passes checksum/read-back before and after a real controlled process stop, restart, and zero-residue cleanup for both providers. Container identity, canonical `mas_internal` network, anonymous disposable-volume removal, failure-classification fields, and zero post-run residue are retained; no payloads, credentials, logs, or provider-selection decision are retained. Provider-managed KMS, clean-host/disaster recovery, production resource thresholds, and independent-host evidence remain open; licence metadata remains non-gating |
| AIAT credentials-manager secret boundary | private Compose Postgres + production `CredentialsManager` | PASS (AIAT-owned secret management; provider KMS open) | Implementations `12ba7c7`/`a946368`; scalar evidence [`provenance/credentials_manager_live_evidence.json`](provenance/credentials_manager_live_evidence.json); Compose-local release child registration `f9527b3`; the private probe passes all 12 checks and leaves zero fixture rows. Key material, values, payloads, and credentials are excluded. Provider-managed SSE/KMS, external custody/rotation, clean-host bootstrap, and disaster recovery are not configured or claimed; licence metadata remains non-gating |
| Object-store verified copy/parity | live disposable MinIO → SeaweedFS adapter boundary | PASS (three-object checksum/size parity; migration approval open) | Evidence/docs group `6f5b5b8` retains [`provenance/object_store_copy_provider_diverse_evidence.json`](provenance/object_store_copy_provider_diverse_evidence.json). The live source inventory copies three reserved objects from Compose MinIO to disposable SeaweedFS, reads back matching checksums/sizes, preserves source until explicit cleanup, and leaves zero source/target objects. Retention parity, cutover/rollback, actual outage, clean-host/disaster recovery, and licence metadata remain non-gating/open as applicable |
| Object-store migration rehearsal evidence | live disposable MinIO → SeaweedFS workflow boundary | PASS (reserved-prefix rehearsal; production cutover open) | Evidence/docs group `c086a91` retains [`provenance/object_store_migration_provider_diverse_evidence.json`](provenance/object_store_migration_provider_diverse_evidence.json). The checker requires explicit endpoints, a reserved project, fixture seeding, and separate human confirmations; it passes three-object inventory, checksum/read-back copy, one dual write, `CUTOVER` → `ROLLED_BACK` history, payload-free reporting, and four-object-per-side cleanup to zero. Deployment routing and retention authority are unchanged; production cutover, outage, KMS, clean-host/disaster recovery, and licence metadata remain non-gating/open as applicable |
| Object-store encrypted backup and fresh-process restore | static/unit fixture | PASS (AIAT-owned envelope plus local fresh-process prerequisite; provider-managed/external open) | Commits `91504dd` and `b0f27f6`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_encryption.py -q`, `uv run --isolated python scripts/check_object_store_encryption.py --json`, and `uv run --isolated python scripts/check_object_store_clean_environment_restore.py --json` pass AES-256-GCM encryption before adapter write, opaque key-ID-only manifest projection, ciphertext checksum/read-back, wrong-key/tamper rejection, clean-target refusal before mutation, distinct-process bundle restore through fresh adapters, payload-free evidence, and scoped cleanup. Evidence is [`provenance/object_store_encryption_evidence.json`](provenance/object_store_encryption_evidence.json) and [`provenance/object_store_clean_environment_restore_evidence.json`](provenance/object_store_clean_environment_restore_evidence.json); provider-managed SSE/KMS, external Garage/R2/B2, key custody/rotation, provider-pair, clean-host/disaster-recovery, and outage evidence remain open; licence metadata is non-gating |
| Metric-series budget | static + local live scrape + durable many-project scrape | PASS (static + refreshed local scrape + durable certificate) | `541d6e0` preserves bounded resume-time reconciliation for legacy storage doubles; `eefe08f` adds the per-scrape durable reconciliation hook and scoped many-project checker; `uv run --isolated python scripts/check_metric_series_budget.py --json` exercises 10,000 synthetic projects with 2,000-total/32-project-state budgets and no `project_id` label, while `docker exec mas-orchestrator-api-1 python /tmp/check_metric_series_many_projects.py --json` certifies 10,000 durable local Postgres rows, 31 bounded series, all 18 state values, no `project_id` label, scoped cleanup, and baseline restoration; retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json) and [`provenance/metric_series_many_projects.json`](provenance/metric_series_many_projects.json). Clean native-Linux release-host scale evidence remains open |
| Tool-service image budget and dependency profile | static contract + local build/resource probe | PASS (static + local Linux probe; native release open) | Contract `b24ca0c`; dependency/profile boundary `e6ee8b8`; `apps/tool-service/pyproject.toml` keeps Playwright in the opt-in `browser` extra while the core gateway remains browser-free, and `Dockerfile.tool-service` keeps browser/Docling/Semgrep in the separately budgeted `extensions` profile with exact uv `0.4.30`; `uv run --isolated pytest packages/mas-core/tests/test_image_budgets.py apps/tool-service/tests/test_default_shipped_tool_catalog.py -q` and `uv run --isolated python scripts/check_image_budgets.py --json` pass the profile ceilings. Retained local measurements are core `267957904` bytes and extensions `4155668123` bytes, with bounded loopback `/health` probes at 26,836 ms/112.3 MiB and 29,913 ms/137.7 MiB respectively. Evidence is [`provenance/image_budgets_live.json`](provenance/image_budgets_live.json); compressed archive size, clean native-Linux build/pull, and vulnerability/SBOM evidence remain open |
| API/protocol contract export | static contract (preparatory P1) | PASS | Commits `2860838`, `f8829d6`, `b3fca97`, `9a80c6c`, and `8f46ed1` regenerate/reconcile the checked-in artifacts; `uv run --isolated python scripts/check_api_contract.py --json`; 238 OpenAPI paths/135 schemas, `aiat.v1`, and generated dashboard/SDK types match runtime/provenance hashes, including the read-only system diagnostics route, native trace-evidence, SLO/capacity, evidence-package, self-improvement outcome-action, artifact-bundle/read-back, bounded artifact/usage evidence reads, and typed read-only retention-plan response. `8f46ed1` reconciles the checked-in `WorkerManifest.transport` enum with runtime `aiat_gateway` and records protocol SHA-256 `03412f2347bb5396f260d9d75fa4d494d228af7703ccd42e7bd473ccaf8c4263` (`OpenAPI 9e57c46151096908ae14e0f522f833ea87fe55e852dbf1c6c39d386277cbcae1`, TypeScript `a10893c6f253ffb760623cb34482c3caa86de1402d664a456b450857a9fd8e94`, Python `dcc6c3f2dbc9a1b382192e7b111b76c48b8a66d46868f4f6f98f903141411c0e`) |
| Current API/protocol contract export | static contract (committed follow-up) | PASS | Commits `f8829d6`, `b3fca97`, `9a80c6c`, and `8f46ed1` add/reconcile the retention-plan operation, typed response, legal-hold fields, and `aiat_gateway` transport enum, then regenerate or verify the checked-in OpenAPI/TypeScript/Python/protocol artifacts; `uv run --isolated python scripts/check_api_contract.py --json` passes with 238 OpenAPI paths, 135 schemas, 271 operations, and matching provenance hashes (`OpenAPI 9e57c46151096908ae14e0f522f833ea87fe55e852dbf1c6c39d386277cbcae1`, protocol `03412f2347bb5396f260d9d75fa4d494d228af7703ccd42e7bd473ccaf8c4263`, TypeScript `a10893c6f253ffb760623cb34482c3caa86de1402d664a456b450857a9fd8e94`, Python `dcc6c3f2dbc9a1b382192e7b111b76c48b8a66d46868f4f6f98f903141411c0e`). This follows the static ledger snapshot and does not change the NO-RELEASE decision |
| Built-in PM/SCM adapter declarations | static/unit/fixture; provider live boundary | PASS | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_declarations.py -q`; `uv run --isolated python scripts/check_provider_adapter_declarations.py --json` reconciles the real YouTrack adapter plus GitHub `pm`/`delivery`/`checks` profiles and bounded path guards with zero provider HTTP calls or mutations; provider-specific mock/live/outage/restore evidence remains open |
| Built-in PM/SCM mocked HTTP conformance | static/unit/fixture; provider live boundary | PASS | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_http_conformance.py -q`; `uv run --isolated python scripts/check_provider_adapter_http_conformance.py --json` drives eight real-adapter YouTrack/GitHub cases through local mocked responses for health/configuration, projection/read-back, cursors, deactivation, comments/links, GitHub source-control paths, webhook handling, and retryable/permanent failures; no external HTTP or provider mutation occurs |
| PM inbound canary replay and ACTIVE activation scope | static/unit/API/storage (live provider boundary) | PASS (bounded governance; live open) | Commit `72e1aef`; `uv run --isolated pytest apps/orchestrator-api/tests/test_pm_control_plane.py packages/mas-core/tests/test_pm_integrations.py packages/mas-core/tests/test_phase7_storage.py -q` plus `check_api_contract.py --json` pass. ACTIVE inbound mutations persist sanitized actor/command/projection evidence atomically; verified terminal-conflict replay is digest/expiry/arming/connection/one-command gated; ACTIVE lifecycle plans snapshot trusted actor scope/blast radius/default-deny policy and transactional approval evidence; evidence-only reconciliation observations remain non-blocking while actionable conflicts stay blocking. Operator-owned verified replay fixtures and provider/native live canary, rollback, and restore evidence remain open; licence metadata is informational only |
| External-account lifecycle fixture | static/unit/fixture; provider live boundary | PASS | `uv run --isolated pytest packages/mas-core/tests/test_external_account_lifecycle.py -q`; `uv run --isolated python scripts/check_external_account_lifecycle.py --json` drives the actual `IdentityService` through eight in-memory cases for category approval/idempotency, one-use browser leases, credential rotation/session revocation, closure approval, immediate suspension, fail-closed unknown categories, and secret-safe output without external account/provider calls |
| Machine-readable release ledger | static aggregation; live boundary | PASS (55/55 static + configured live evidence); BLOCKED/NO-RELEASE (live profiles) | `4d7a495` adds the native release-host preflight, `5ed0a0b` adds the Firecracker readiness child, `48b32ef` adds the bounded gateway-provider-recovery child, `23e908e` adds the security-finding-review child, `bb0fa8f` registers the object-store resource-profile child, `407a4f9` registers the guarded object-store provider-outage child, `d101901` plus `f9527b3` register the AIAT credentials-manager child and its private Compose execution boundary, and `8dbf508`/`0d73fbe` add fail-closed retained-live evidence validation for reviewed provider certificates; release-ledger regression coverage pins the children. The unconfigured local 77-check `--live --json` run (2026-08-18T15:53:05.092363Z, revision `417b667`) records 58 passes, zero failures, 19 externally blocked probes, and four pending evidence items; the safe summary is retained at [`provenance/release_ledger_live_current.json`](provenance/release_ledger_live_current.json). The refreshed configured Compose 77-check aggregation (2026-08-18T17:24:47Z) records 70 passes, zero failures, 7 externally blocked probes, and four pending evidence items; its scalar summary and trace/metrics/worker/runtime/model/credentials/provider-diverse evidence are retained at [`provenance/release_ledger_live.json`](provenance/release_ledger_live.json). Native release-host, operator-selected worker certification, immutable image identity, gVisor, Firecracker host certification, provider/mail, unresolved security findings, external KMS/key custody, clean-host/disaster recovery, and self-improvement source remain separate open gates; licence metadata remains non-gating. |
| Project evidence package | static/unit/API/dashboard (preparatory P1) | PASS (committed core/API/dashboard surfaces; project-page/live preparatory) | Commits `a44a1aa`, `d0472af`, `cbf00d9`, `33e0384`, `82bbaeb`, `1112d5e`, and dashboard error-state repair `fc4f0fa`; the deterministic `EvidencePackage` builder, scope-policy resolver, isolated package/policy API router boundary, operator-only idempotent snapshot upsert, project workspace coverage, and API/UI evidence-package categories pass focused tests. `aiat.project-evidence-package.v1` groups repository, documents, tests, security, deployment, cost, approvals, flow, worker, artifact, and audit sources, while licence/restriction values remain metadata-only notices; live durable snapshot/provider/worker generation and native recovery remain open |
| Project evidence package stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `bc80ad5`; `npm run typecheck`, targeted ESLint, full dashboard `npm run lint` (two unrelated hook warnings), `npm run build`, and the source-built `project-evidence-states.spec.ts` pass 1/1. The page uses `cache: "no-store"`, retains the last successful package when a refresh fails, labels it as last known, and exposes a keyboard-visible Retry action that clears after successful recovery; initial failures remain explicit. Full project-page composition and live provider/worker evidence remain open |
| Project evidence package accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `89091c1`; targeted ESLint, `npm run typecheck`, `npm run build`, and the source-built `project-evidence-states.spec.ts` pass 1/1. The page exposes named main/package sections, labeled 44px back/refresh actions, and a captioned evidence table with scoped column headers; the Impeccable detector returned no warnings for the changed page/test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live provider/worker evidence remain open |
| Dashboard contract generation | static/typecheck (preparatory P1) | PASS | Commits `2860838`, `f8829d6`, `b3fca97`, and `9a80c6c`; `uv run --isolated python scripts/generate_typescript_api.py --check`; `npm run typecheck` in `apps/mas-dashboard` passes after the project-evidence `ErrorBanner` prop repair `fc4f0fa`; 135 models and 271 operations generated deterministically, including the typed read-only retention-plan response and legal-hold fields |
| Dashboard theme preference foundation | static/typecheck/lint/build/source-built Playwright (preparatory P1) | PASS (foundation; full page parity/native visual open) | Commit `5e3cc13`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4101 npm run test:e2e -- dashboard-theme.spec.ts` pass 2/2 for persisted explicit light/dark selection and system media switching. `ThemeProvider` applies a no-flash `data-theme` bootstrap, the sidebar/mobile controls expose `system`/`light`/`dark`, the legacy slate palette has a bounded light migration layer, and reduced-motion preferences disable authored animation/transition duration; complete page-by-page contrast/visual/mobile parity remains open. |
| Evidence detail read model | static/build/source-built Playwright (preparatory P1) | PASS (fourteen canonical kinds; evidence-detail stale refresh covered) | Commits `8fefc8b`, `c8505eb`, `dc50719`, `5357166`, `ec8cf67`, `2ca5f3d`, and `6c52552`; `npm run typecheck`, `npm run lint`, `npm run build`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4112 npx playwright test dashboard-evidence-detail.spec.ts --reporter=line`, and the focused API evidence suite pass; dashboard source-built coverage is 9/9. `aiat.evidence-detail.v1` reads project, flow, flow-instance, worker, worker-run, credential, dead-letter, runtime, trace, model, integration, tool, artifact, and usage records through allow-listed backend paths and scalar fields only; model catalogue/integration list/tool manifest selection is bounded by the citation ID, artifact/usage reads are operator-authenticated, strings/numbers are bounded, nested payloads/secrets/metadata/pricing/resource/configuration/profile bindings/tool schemas/credential requirements/trace items are omitted, temporary detail failure preserves citation identity, the last successful scalar projection remains visible through a failed refresh with an explicit Refresh control, obsolete requests are aborted on navigation, and unsupported kinds remain identity-only. Broader stale/offline recovery remains open. |
| Evidence detail accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `32f3a76`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4136 npx playwright test dashboard-evidence-detail.spec.ts --workers=1 --reporter=line` pass 9/9. The page exposes a named page/canonical-citation region, semantic bounded-detail region with an `aria-busy` refresh state, decorative-icon suppression, and 44px CEO-chat/canonical-link/Refresh targets; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and broader evidence-detail recovery remain open. |
| System visualisation accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `ed5e551`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4138 npx playwright test app-operations.spec.ts --workers=1 --grep "system visualization exposes hierarchy, permissions, orchestration, and path tracing" --reporter=line` pass 1/1. The page exposes named loading/error/ready landmarks, horizontal visualization tabs with semantic tab/tabpanel links, and 44px breadcrumb, refresh, Mermaid-copy, path-trace, graph/detail, policy, retry, and back-link targets; deterministic fixtures cover the four system-visualisation API reads, and contrast classes identified by the Impeccable detector were corrected before commit. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live control-plane evidence remain open. |
| System visualisation denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `db898e7`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4147 npx playwright test e2e/app-operations.spec.ts --workers=1 --grep "system visualization" --reporter=line` pass 4/4 for healthy, partial, offline, and 403 hierarchy states. A denied hierarchy read renders a named access-status region, operator guidance, and a 44px dashboard return link without Retry; partial notices identify each failed source. Full WCAG/native-Linux visual certification and live ACL evidence remain open. |
| Shared identity-resource denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `0974434`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4148 npx playwright test e2e/identity-states.spec.ts --workers=1 --reporter=line` pass 2/2 for stale/retry preservation and a 403 first-load denial. The shared page removes Refresh/Retry while denied, exposes a named access-status region and 44px dashboard return link, and preserves already loaded metadata-only rows if authorization is lost after a successful read; provider/live identity certification remains open. |
| Shared ErrorBanner decorative-icon semantics | static/build/source-built Playwright (preparatory P1) | PASS | Commit `29b700c`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4149 npx playwright test e2e/app-operations.spec.ts --workers=1 --grep "system visualization" --reporter=line` pass 4/4, including the partial-state assertion that the shared warning icon has `aria-hidden="true"`. Full WCAG/native-Linux visual certification remains open. |
| PM integrations accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `bbd6ba3`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4139 npx playwright test app-operations.spec.ts --workers=1 --grep "PM integrations preserve conflicts and expose a stale refresh retry" --reporter=line` pass 1/1. The page exposes a named busy main landmark, explicit summary/connections/reconciliation/lifecycle regions, labeled lifecycle inputs, and 44px refresh/retry/generation/approval/apply controls; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and provider-owned evidence remain open. |
| System Overview accessibility baseline | static/build/source-built Playwright with local fixtures (preparatory P1) | PASS | Commit `c07b4a6`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4140 E2E_ORCHESTRATOR_URL=http://127.0.0.1:9999 npx playwright test first-run-states.spec.ts --workers=1 --reporter=line` plus the same test at port `4141` against a `not_seeded` fixture pass 1/1 each. The home page exposes a named main and hero/status surface, explicit health/overview-metrics/first-run/company-project-state/Quick Links regions, decorative-icon suppression, and 44px graph/Quick Links/seed controls; the Impeccable detector returned no warnings. The direct live backend was unavailable during this run, so full control-plane, WCAG, native-Linux, and visual certification remain open. |
| System Overview source-status recovery | static/build/source-built Playwright with deterministic fixtures (preparatory P1) | PASS | Commits `50cee61`, `24be4ba`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4144 EXPECTED_OVERVIEW_STATE=offline npx playwright test system-overview-recovery.spec.ts --workers=1 --reporter=line` plus the same test with `EXPECTED_OVERVIEW_STATE=partial` pass 1/1 each. The page classifies seven independent control-plane/metrics reads as healthy, partial, or offline, names failed sources, avoids inferring unavailable values, exposes a 44px GET retry, and hides decorative EmptyState icons from assistive technology; full retained stale history, WCAG, native-Linux, and live control-plane evidence remain open. |
| System Overview access-denied recovery | static/build/source-built Playwright with local 403 fixture (preparatory P1) | PASS | Commit `b0ab779`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4173 EXPECTED_OVERVIEW_STATE=denied npx playwright test e2e/system-overview-recovery.spec.ts --workers=1 --reporter=line` pass 1/1 against an isolated 403 orchestrator/Prometheus fixture. Denied sources expose a named access-status region, retain available overview values as read-only context, and hide retry plus first-run seed actions; native/live ACL, retained live history, and full WCAG/native-Linux evidence remain open. |
| Governance read-surface stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `52de581`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4113 npx playwright test governance-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The combined model-profile, runtime catalogue, WorkerRun, and external-steward reads use `cache: "no-store"`; if any source fails after a successful load, the page retains the last known data, labels it stale, exposes header Refresh and banner Retry controls, and clears the warning after a successful retry. Provider/live governance evidence remains open. |
| Governance accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f4ae7eb`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4133 npx playwright test governance-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named main/read-surface structure, explicit executive/model-profile/WorkerRun/steward/catalogue regions, a captioned/scoped WorkerRun table, accessible catalogue status, and 44px refresh, retry, executive-form, and confirmation controls; the Impeccable detector returned no warnings for the changed page, action panel, or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live governance evidence remain open. |
| System Control status stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f445c17`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4114 npx playwright test system-status-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The canonical `/api/system/status` read uses `cache: "no-store"`; after a successful load, a failed refresh retains the runtime status, labels it as the last known system status, and exposes header Refresh plus banner Retry controls. The implementation keeps the initial loading state explicit and does not claim native/live runtime evidence. |
| System Control accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `543f392`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4134 npx playwright test system-status-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named main/loading state, explicit runtime-status/schedule/control/dialog regions, scheduled-event semantics, and 44px refresh, retry, shutdown/resume, schedule-input/save, and confirmation controls; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live System Control evidence remain open. |
| System Control denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `14968d4`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4150 npx playwright test e2e/system-status-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied status reads hide Refresh/Retry and shutdown/resume/schedule mutations while preserving only last-known read context; native/live ACL and WCAG evidence remain open. |
| Projects list stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `d3482ab`, `7828b48`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test projects-states.spec.ts --workers=1 --reporter=line` pass 1/1. The paired `/api/projects` and `/api/flows?is_active=true` reads use `cache: "no-store"`; either source failing after a successful load retains the last known project/flow data, labels the list stale, separates load errors from create/action errors, and exposes header Refresh plus banner Retry controls. The table exposes a caption, scoped headers, explicit description disclosure, responsive overflow, and 44px selection/filter/sort/link/action targets. Native/live project evidence remains open. |
| Tools catalogue stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `5f4b0eb`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4116 npx playwright test tools-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/tools` catalogue read uses `cache: "no-store"`; after a successful load, a failed refresh retains tool definitions and circuit-breaker summaries, keeps them visible, labels the catalogue stale, and exposes header Refresh plus banner Retry controls. Native/live tool evidence remains open. |
| Tools catalogue accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `83e39e6`; targeted ESLint, `npm run typecheck`, `npm run build`, and the source-built `tools-states.spec.ts` pass 1/1. The page exposes named main/search/group regions, captioned/scoped group tables, keyboard-visible tool expansion, and 44px refresh/group/search/copy/retry/empty-state targets; the Impeccable detector returned no warnings for the changed page/test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live tool evidence remain open |
| Tools catalogue denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `b418f8a`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4163 npx playwright test e2e/tools-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied responses expose a named access-status region, preserve only previously loaded tool metadata, hide refresh/retry, search, grouping, expansion, and copy controls, and retain read-only tables/details; native/live ACL and tool-service evidence remain open. |
| Flows list denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `3108b02`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4164 npx playwright test e2e/flows-states.spec.ts --workers=1 --reporter=line` pass 4/4 for stale retention, first-load 403 denial, retained-read 401 denial, and deletion 403 denial. Denied reads/deletes preserve only previously loaded definitions as read-only text and hide Refresh/Retry, New Flow, search/status filters, selection, editing, and deletion controls; native/live ACL and flow evidence remain open. |
| Dead-letter queue stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `823fa6d`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4117 npx playwright test dlq-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/dlq` read uses `cache: "no-store"`; after a successful load, a failed refresh retains the message set and replay context, keeps cards visible, labels the queue stale, and exposes header Refresh plus banner Retry controls. Native/live DLQ evidence remains open. |
| Dead-letter queue accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `99a19a2`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test dlq-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/summary/filter/list/disclosure regions, `aria-pressed` severity filters, keyboard-visible envelope inspection, and 44px refresh/retry/selection/replay/filter/inspection targets; the Impeccable detector returned no warnings for the changed page and filter component. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live DLQ evidence remain open. |
| Dead-letter queue denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `e6ab3a1`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4162 npx playwright test e2e/dlq-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied read/replay responses expose a named access-status region, preserve only previously loaded messages, clear selection/replay state, hide refresh/retry, filters, selection, and replay controls, and retain read-only envelope inspection; native/live ACL and DLQ evidence remain open. |
| Credentials metadata stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `970f09c`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4118 npx playwright test credentials-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/credentials` read uses `cache: "no-store"`; after a successful load, a failed refresh retains only redacted credential metadata (names, placeholders, policy, usage), keeps rows visible, labels the list stale, and exposes header Refresh plus banner Retry controls. The fixture contains no secret value; native/live expiry, revocation, and provider evidence remain open. |
| Credentials accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `93fdfbc`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test credentials-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/security/data regions, a captioned/scoped credentials table, a labeled creation dialog with explicit field associations, and 44px refresh/audit/selection/copy/delete/dialog targets; the Impeccable detector returned no warnings for the changed page and bulk-action component. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live credential evidence remain open. |
| Metrics partial/stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `85596b0`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4119 npx playwright test metrics-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The six `/api/metrics` Prometheus query families use `cache: "no-store"`; partial query failures retain successful series, label the surface stale, preserve the last-successful refresh timestamp, and expose header Refresh plus banner Retry controls. Native/live telemetry and Prometheus evidence remain open. |
| Metrics denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `b64b15e`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4161 npx playwright test e2e/metrics-states.spec.ts --workers=1 --reporter=line` pass 3/3 for partial recovery, first-load 403 denial, and authorization loss after a successful read. Denied responses from any query family expose a named access-status region, preserve only previously loaded series, and hide refresh/retry, time-range, and reconnect controls; native/live ACL and Prometheus evidence remain open. |
| Metrics accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `da113af`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4127 npx playwright test metrics-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/summary/chart regions, a semantic time-range control, and 44px range/refresh/retry/empty-state targets; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live metrics evidence remain open. |
| Container Logs accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `993b1cb`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4128 npx playwright test logs-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/filter/legend/output/status regions, 44px stream/filter/recovery targets, and an `aria-busy` log output; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live container-log evidence remain open. |
| Flows list stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `a0faf5b`; `npm run typecheck` and `npm run build` pass, the focused Flows file/test lint passes, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4120 npx playwright test flows-states.spec.ts --reporter=line` passes 1/1. The `/api/flows?limit=1000` (and status-filtered) read uses `cache: "no-store"`; after a successful load, a failed refresh retains flow definitions, keeps rows visible, labels the view as showing last-known flows, and exposes header Refresh plus banner Retry controls. Native/live flow evidence remains open. |
| Flows list stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `a0faf5b`, `6b0413b`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test flows-states.spec.ts --workers=1 --reporter=line` pass 1/1. The `/api/flows?limit=1000` (and status-filtered) read retains last-known definitions through refresh failure and exposes stale/retry recovery; the table now has an accessible name/caption, scoped headers, responsive overflow, and 44px refresh/create/search/filter/selection/link/delete targets. Native/live flow evidence remains open. |
| Flow editor load/stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `b5098e7`; `npm run typecheck`, full `npm run lint` (two unrelated hook warnings), `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4111 npx playwright test flow-editor-states.spec.ts --workers=1 --reporter=line` pass 1/1. Existing-flow first load exposes an explicit unavailable state with Retry; refresh failures retain the last-known flow/canvas, show stale labeling, preserve backend error detail, and recover through Retry. New-flow creation remains available without the refresh control. Native/live flow/runtime recovery remains open. |
| Flow editor load/stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `b5098e7`, `140af1c`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test flow-editor-states.spec.ts --workers=1 --reporter=line` pass 1/1. Existing-flow first load remains explicit and retryable; refresh failures retain the last-known canvas, and the editor exposes semantic header/main/palette/canvas/config landmarks plus 44px toolbar/palette/config/generated-form targets. Native/live flow/runtime recovery remains open. |
| Flow editor denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `392d264`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4165 npx playwright test e2e/flow-editor-states.spec.ts --workers=1 --reporter=line` pass 4/4 for stale recovery, first-load 403 denial, retained-read 401 denial, and save 403 denial. Denied reads/saves preserve only the last successfully loaded canvas as read-only and hide Refresh/Retry, palette, editing, undo/redo, and save controls; native/live ACL and flow/runtime evidence remain open. |
| New-flow builder denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `b07299b`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4174 npx playwright test e2e/flow-builder-states.spec.ts --workers=1 --reporter=line` pass 3/3 for initial catalogue denial, create denial, and readiness-validation denial. Denied reads/mutations expose a named access-status region, retain the current draft canvas as read-only context, and hide template, palette, node-configuration, validation, activation, and creation controls; native/live ACL and full WCAG/native-Linux evidence remain open. |
| Project detail first-load/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f364763`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4112 npx playwright test project-detail-states.spec.ts --workers=1 --reporter=line` pass 1/1. A failed first project read now shows Project unavailable with backend error detail and keyboard-visible Retry; a successful retry renders the project workspace. Existing-project stale/retry recovery remains covered by the workspace path; full project-page composition and live provider/worker generation remain open. |
| Project detail accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `40b87dd`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4135 npx playwright test project-detail-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named page/loading state, explicit project status, 44px refresh/retry/back and primary project-view tab targets, semantic project/workspace tab-panel relationships, and an `aria-busy` workspace panel; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full project composition, WCAG/native-Linux visual certification, and live provider/worker evidence remain open. |
| Project detail denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `0671eaa`; `npm run lint` (two unrelated existing hook warnings), `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4167 npx playwright test e2e/project-detail-states.spec.ts --workers=1 --reporter=line` pass 4/4 for transient recovery, initial 403 denial, retained-header 401 denial, and workflow-mutation 403 denial. Denied canonical reads/mutations expose a named access-status region, retain only the last-known project header as read-only context, clear pending interaction state, and hide refresh/retry, tabs, panels, and workspace/workflow/flow/context/evidence/repository/approval/transition controls; native/live ACL and project evidence remain open. |
| Project evidence package denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `00f81b5`; `npm run typecheck`, full `npm run lint`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4169 npx playwright test e2e/project-evidence-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, initial 403 denial, and retained-package 401 denial. Denied package reads expose a named access-status region, retain a previously loaded package as read-only context, hide Refresh/Retry and package controls, and preserve safe Back to project navigation; native/live ACL and project evidence remain open. |
| Evidence detail denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `23e2db9`; `npm run typecheck`, `npm run lint`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4171 npx playwright test e2e/dashboard-evidence-detail.spec.ts --workers=1 --reporter=line` pass 11/11 for scalar redaction, stale recovery, initial 403 denial, and retained-read 401 denial. Denied scalar reads expose a named access-status region, retain any last-known projection as read-only, hide Refresh, and preserve citation identity plus safe CEO/owning-section navigation; native/live ACL and broader evidence certification remain open. |
| Project workspace stale/retry recovery and nested tab semantics | static/build/source-built Playwright (preparatory P1) | PASS | Commits `cb1c665`, `fcb0f4b`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4114 npx playwright test project-workspace-states.spec.ts --workers=1 --reporter=line` pass 1/1. A failed workspace refresh retains activity/resources/cost data, preserves the last repository snapshot across partial failure, labels the surface as showing last known workspace data, and recovers through Retry. Activity/Resources/Cost tabs expose semantic tab/tabpanel relationships, roving `tabIndex`, Arrow/Home/End navigation, and 44px targets. First-load handling is explicit; live workspace/provider/worker generation remains open. |
| Agent Streams reconnect/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `3e8a0ea`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4123 npx playwright test streams-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The stream page reads bounded history with `cache: "no-store"`, guards obsolete reconnect generations, retains history/messages across failed SSE or history refresh, labels last-known data, and exposes Reconnect plus Retry. Native/live Redis/router stream evidence remains open. |
| Agent Streams accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `d320383`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4129 npx playwright test streams-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/filter/feed/status regions, a captioned message table, keyboard-accessible expandable rows, 44px stream/filter/action targets, and an `aria-busy` feed state; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live stream evidence remain open. |
| Agent Streams denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `118ff18`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4159 npx playwright test e2e/streams-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss via SSE after a successful read. Denied history/SSE responses expose a named access-status region, invalidate in-flight stream callbacks, preserve only previously loaded messages, and hide reconnect/retry, filter, pause, clear, and copy controls; native/live ACL and Redis/router evidence remain open. |
| Hiring Board stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `7541b84`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4124 npx playwright test workers-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The worker catalogue retains its last successful rows after a failed refresh, labels the view as showing last-known workers, keeps rows visible, and exposes Retry; first-load failures show an unavailable state. Native/live worker certification evidence remains open. |
| Hiring Board accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `826b4c5`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4131 npx playwright test workers-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/policy/summary/filter/table regions, integration/runtime status landmarks, a captioned/scoped worker table, keyboard-expandable rows, associated registration-dialog fields, and 44px refresh/register/filter/selection/row-action/dialog targets; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live worker evidence remain open. |
| CEO Live Feed reconnect/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `1761429`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4125 npx playwright test ceo-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The CEO feed retains bounded history/messages across failed reconnect or history refresh, guards obsolete generations, labels last-known data, and exposes Reconnect plus Retry without changing the governed composer. Native/live Redis/router CEO evidence remains open. |
| CEO Live Feed accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `1f947a9`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4130 npx playwright test ceo-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/composer/summary/filter/feed/status regions, 44px stream/composer/filter/recovery targets, a busy feed state, and keyboard-expandable messages; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live CEO feed evidence remain open. |
| CEO Command Center chat stream/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `beabb95`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4126 npx playwright test ceo-chat-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The chat retains bounded history through live-stream failure, guards obsolete history/live callbacks, keeps the transcript labelled last known, exposes a keyboard-visible Retry action, and keeps history and stream failures independent so a late history response cannot hide a stream outage. Native/live Redis/router CEO evidence remains open. |
| CEO Command Center chat accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `8ffb5df`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4132 npx playwright test ceo-chat-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/workspace/transcript/composer regions, a live transcript log with busy state, 44px navigation/composer/quick-command/recovery targets, explicit chat guidance regions, and a mobile-safe accessible activity link. The Impeccable detector was run once before the final class corrections; its three gray-on-color findings were corrected in the implementation, and no detector rerun was used. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live CEO chat evidence remain open |
| Credentials refresh render-state repair | static/lint/build/source-built Playwright (preparatory P1) | PASS | Commit `e6e6980`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4121 npx playwright test credentials-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The stale credentials table uses state, rather than reading a ref during render, to distinguish initial loading from retained metadata; full dashboard lint now reports zero errors and two unrelated hook warnings. |
| Container Logs stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `280d363`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4122 npx playwright test logs-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The SSE route retains the last log buffer after an error payload, labels it as last known, exposes Retry, and replaces the retained buffer on the first successful event after retry; native/live container log evidence remains open. |
| Container Logs denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `156597c`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4160 npx playwright test e2e/logs-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied SSE responses expose a named access-status region, invalidate obsolete stream generations, preserve only previously loaded lines, and hide load/retry, filter, clear, copy, and download controls; native/live ACL and container-log evidence remain open. |
| Python SDK contract generation | static/unit (preparatory P1) | PASS | Commits `2860838`, `f8829d6`, `b3fca97`, and `9a80c6c`; `uv run --isolated python scripts/generate_python_api.py --check`; `uv run --isolated pytest packages/mas-api-sdk/tests -q`; 135 models and 271 operations match the OpenAPI artifact, including the typed read-only retention-plan response and legal-hold fields |
| Company manifest policy fields | static/contract (preparatory P1) | PASS | Typed manifest group `e0f0aee`; `uv run --isolated pytest packages/mas-core/tests/test_company_manifest.py -q`; optional timezone, retention (including trace days/sample rate), privacy, evidence, model, and deployment blocks validate, and legacy manifests remain compatible |
| Company timezone propagation | static/unit/tool-service/dashboard (preparatory P1) | PASS (tool/dashboard; Compose follow-up) | Commits `8bcff1a` and `ee1361f`; `uv run --isolated pytest apps/tool-service/tests/test_time.py apps/orchestrator-api/tests/test_system.py -k 'time or schedule' -q` plus `npm run typecheck` in `apps/mas-dashboard` prove valid IANA-zone reads, manifest-derived scheduler defaults, and invalid-zone UTC fallback while durable records remain UTC. Compose/native display evidence remains separate |
| Prompt/tool and review contract reconciliation | static/unit (preparatory P1) | PASS | Contract `20f0499`; implementation `5b830e9`; `uv run --isolated python scripts/check_prompt_tool_reconciliation.py --json`; all 11 shipped prompts resolve to 114 concrete, policy-allowed tools; review submissions use canonical `REVIEW_RESPONSE` envelopes with signed caller context and CEO privileged requests use `/ceo/privileged-action` |
| PM/SCM provider conformance fixture | static/unit/CLI (preparatory P1) | PASS (fixture; live blocked) | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_pm_integrations.py packages/mas-core/tests/test_provider_conformance_runner.py -q`; `uv run --isolated python scripts/check_provider_conformance.py --json` passes 10 fake-provider cases; `--live --json` returns exit 2 until provider-specific sandbox/mock HTTP/outage/restore evidence is supplied |
| Flow node-schema contract | static/unit (preparatory P1) | PASS | `uv run --isolated python scripts/generate_flow_node_schemas.py --check`; 9 node types at v1.0 match backend validation, JSON artifact, `/flows/node-schemas`, and generated dashboard metadata |
| Dashboard node-schema editor | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; both flow editors render the generated contract plus editable typed form, governed worker/profile selectors, CSV/JSON fields, deprecated `team_id`/`action` fields only in collapsed compatibility controls, and the dashboard store/proxy exposes explicit saved-definition migration |
| Evidence-policy contract | static/unit (preparatory P1) | PASS | Evidence tests cover required artifact kinds; `/evidence-policies` publishes built-ins and policy dry-run validation includes worker-run/repository resources |
| Evidence-policy selection | static/API/unit (preparatory P1) | PASS | Company defaults persist through the active manifest; `PUT /projects/{project_id}/evidence-policy` persists project defaults and milestone overrides; `resolve_evidence_policy_selection` and `check_evidence_policy_resolution.py --json` cover project-milestone → project → flow → company-milestone → company → manual precedence without using licence metadata as a gate; live transition proof remains open |
| Evidence-policy scope resolution fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | `uv run --isolated pytest packages/mas-core/tests/test_evidence_policy_resolution.py -q`; `uv run --isolated python scripts/check_evidence_policy_resolution.py --json` passes seven deterministic precedence/fallback cases and reports `licence_metadata_is_gate: false`; `--live` is explicitly blocked without an authenticated API scenario |
| External-account action policy fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | Commit `f577675`; `uv run --isolated pytest packages/mas-core/tests/test_external_account_action_policy.py -q`; `uv run --isolated python scripts/check_external_account_action_policy.py --json` reconciles all five action rules, four category dispositions, and fail-closed unknown action/category behavior without mutating identity/provider state; `--live` is blocked until a provider-specific sandbox/outage scenario exists |
| Outbound-mail lifecycle fixture | static/unit/fixture (relay live open) | PASS | Commit `f577675`; `uv run --isolated pytest packages/mas-core/tests/test_outbound_mail_lifecycle.py -q`; `uv run --isolated python scripts/check_outbound_mail_lifecycle.py --json` drives the actual `IdentityService` through approval pause, request/submission idempotency, definitive provider-failure retry, ambiguous-outage reconciliation hold, and secret-safe output without external relay calls |
| Self-improvement candidate detection | static/unit/fixture (live signal sources open) | PASS | Commit `4d8dddf`; `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` reconciles defect, metric, upstream-update, cost, and operator-goal signals with deterministic deduplication/risk/budget mapping, conflicting-ID rejection, secret-safe metadata, and zero project/budget/credential/deployment side effects |
| Self-improvement lifecycle persistence | static/unit/storage/API + local Compose Postgres (live worker/provider boundary) | PASS (local durable certificate; live boundary open) | Commits `64218ab` and `10983c8`; `PYTHONPATH=packages/mas-api-sdk uv run --isolated pytest apps/orchestrator-api/tests/test_self_improvement_api.py packages/mas-core/tests/test_phase7_storage.py -k self_improvement -q` plus the checker test pass. The local checker drives the canonical project writer and revisioned CAS lifecycle through six technical gates, human approval, five artifact read-backs, exact rollback, terminal outcome/history persistence, connection-backed read-back, stale-write rejection, and scoped cleanup; evidence is [`provenance/self_improvement_postgres_evidence.json`](provenance/self_improvement_postgres_evidence.json). Live certified-worker, provider, deployment, and control-plane reconciliation remain open; licence metadata is informational only |
| Reusable flow templates | static/unit/API (preparatory P1) | PASS | Commit `234adfb`; six canonical templates validate through `validate_flow`; `/flow-templates` and `/flows/from-template` are covered by `test_flow_templates.py` and `test_flows.py`, with import paths using the package-level workflow exports |
| Dashboard canonical template consumption | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; new-flow starter cards fetch `/api/flow-templates`, preserve template configs/evidence metadata, remap branch references, and retain a blank-canvas fallback |
| Flow definition lifecycle | static/API (preparatory P1) | PASS | Commit `234adfb`; new and updated definitions persist the explicit node-schema version, and export/hash, deterministic diff, import, publish, deprecate, compatible migration, and explicitly mapped active-node graph-rewrite endpoints are covered by `apps/orchestrator-api/tests/test_flows.py`; live recovery remains open |
| Flow-instance recovery boundary | static + guarded live API probe | PASS (blocked without instance/API) | `uv run --isolated pytest packages/mas-core/tests/test_flow_instance_recovery.py -q`; `scripts/check_flow_instance_recovery.py` is read-only by default, requires `--confirm` for state-changing actions, validates post-action state/execution history, and returns exit 2 when URL/instance/live evidence is absent; full worker/project/UI recovery remains open |
| Flow control-topology contract | static/unit/fixture (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py -q`; `uv run --isolated python scripts/check_flow_topology.py --json` reconciles parallel branch declarations, join fan-in, and switch case targets with persisted edges; no worker dispatch/storage mutation occurs and live fan-out/recovery remains open |
| Flow execution semantics contract | static/unit/fixture (preparatory P1) | PASS (deterministic traversal; live open) | `uv run --isolated pytest packages/mas-core/tests/test_flow_execution_semantics.py -q`; `uv run --isolated python scripts/check_flow_execution_semantics.py --json` drives the real traversal engine through fan-out, one-branch join waiting, single join scheduling, selected switch routing, and unknown-case blocking; duplicate join scheduling and completed-join reactivation are prevented, while live fan-out/join/watchdog/recovery evidence remains open |
| Durable flow-instance recovery evidence | local Compose Postgres + unit/checker | PASS (local durable recovery; native/live open) | Commit `1f20132`; the reserved `check_flow_instance_recovery_postgres.py` reaches migration `0042_worker_run_host_binding`, preserves superseded retry history, preserves context across a flow switch while resetting execution authority, retries a cancelled instance, reopens Postgres, and removes three projects/two flows/three instances with zero remaining rows. Evidence is [`provenance/flow_instance_recovery_postgres_evidence.json`](provenance/flow_instance_recovery_postgres_evidence.json); native watchdog, worker/provider canary, durable audit authority, UI, and cold-crash recovery remain open |
| Governed asynchronous flow-task binding | static/unit/fixture (preparatory P1) | PASS (binding contract; live open) | Commit `234adfb`; `uv run --isolated pytest packages/mas-core/tests/test_flow_worker_binding.py packages/mas-core/tests/test_flow_retry_persistence.py apps/orchestrator-api/tests/test_flows.py -q`; `uv run --isolated python scripts/check_flow_worker_binding.py --json` proves queued/claimed/running Worker Runs keep their task active, terminal states settle, parallel bindings remain copy-on-write, safe retry re-enters governed dispatch, and unknown states fail closed; both API retry paths preserve prior executions as `SUPERSEDED`, and the API now fails closed when evidence-preserving retry storage is absent; live worker canary/recovery remains open |
| Watchdog and safe-retry recovery semantics | static/unit/fixture (preparatory P1) | PASS (controller contract; live open) | Commit `9972b3b`; `uv run --isolated pytest packages/mas-core/tests/test_workflow_watchdog_recovery.py -q`; `uv run --isolated python scripts/check_workflow_watchdog_recovery.py --json` proves boot grace, downtime-aware timeout, watchdog failure transition, recorded-safe-state retry, terminal-state exclusion, explicit human cancellation, review-circuit escalation, node-timeout failure, and invalid-transition rejection without storage/worker mutation; native watchdog, storage-backed transition history, and cold-recovery remain open |
| Flow compatibility-alias audit | static/API/unit (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_dry_run.py -q`; dry-run returns deterministic `compatibility_aliases` findings and concrete worker-binding recommendations without mutating saved definitions |
| Saved-definition worker migration | static/API/unit (preparatory P1) | PASS | Commit `54ad710`; `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_legacy_migration.py apps/orchestrator-api/tests/test_flow_dry_run.py -k 'legacy or dry_run' -q`; `POST /flows/{flow_id}/migrate-legacy-tasks` previews missing/unknown bindings and creates an immutable worker-bound version with `aiat.flow-legacy-task-migration.v1` metadata, with the dashboard proxy/store preserving the operator boundary; live worker canary/recovery remains open |
| Sprint retrospective/profile lineage | focused API/unit (preparatory P1) | PASS | Commit `1112d5e`; `uv run --isolated pytest apps/orchestrator-api/tests/test_test7_operator_ui.py -k 'completed_issue_updates_agent_profile_once or completed_sprint_persists_retrospective_lineage' -q`; terminal issue completion observes the assigned profile exactly once and persists a `sprint_retrospective` KPI snapshot whose raw data names source/completed issue IDs and assigned-agent lineage; live transition/recovery remains open |

| Operator sign-in accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `d928834`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4143 npx playwright test login-accessibility.spec.ts --workers=1 --reporter=line` pass 1/1. The unauthenticated route exposes a named main/operator-sign-in structure, explicit busy/status announcements, labeled credential fields, password-visibility state, and 44px password/sign-in targets; the Impeccable detector returned no warnings for the changed page/test. Full WCAG/native-Linux visual certification remains open |
| Shared identity-resource route matrix | source-built Playwright (preparatory P1) | PASS | Commit `485dfd2`; `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4145 npx playwright test identity-resource-matrix.spec.ts --workers=1 --reporter=line` passes 9/9 with safe metadata-only fixtures across identities, approvals, audit, sessions, external accounts, domains, relay, mailboxes, and outbound mail; provider/live identity certification remains open |
## Licence metadata assertion

The evaluator and steward tests deliberately include missing, restricted, and
unclassified licence metadata. Those values are retained in the report and
provenance record as notices, while technical source/version, security,
sandbox, compatibility, adapter, model, budget, and human gates remain
independent blockers.
The historical `LICENSE_REVIEW` label is also non-blocking; the steward test
suite rejects a direct `LICENSE_REVIEW` → `BLOCKED` transition.

## Carry-forward blockers

- `DEF-2026-07-14-036`: the refreshed local WSL2 negative network matrix passes and is retained at `provenance/network_boundary_live.json`; a post-fix native-Linux negative test is still required for closure.
- The team-runner storage boundary is statically/API-tested; native DNS/TCP/HTTP
  denial and positive control-plane storage evidence are still required to
  close the historical network defect.
- Coding/tester security evidence has been executed but is not clean: the
  exact-source Semgrep report has 316 findings and 54 engine warnings. Both
  manifests remain `findings_review_required` and cannot be activated solely
  because OpenCode interface evidence exists. The machine-checked review
  register at `provenance/security_scan_review.yaml` assigns the complete rule
  inventory to the personal operator; it records follow-up work, not a waiver.
- Production images and dependency/runtime records still need deployment-supplied
  immutable digest, source, lock, generated SBOM, and scan reconciliation. The
  static checker now also rejects duplicate inventory identities, missing local
  build recipes, malformed non-pending digest/lock metadata, and artifact paths
  outside the repository (`2804a9f`), while `42b03a3` rejects malformed
  CycloneDX artifacts when required. The clean native-Linux artifact set is not
  present in this workspace.
- CEO-vs-human dashboard/API section separation is covered by static/API tests;
  native deployment/network evidence remains open.
- Application image digests, SBOMs, vulnerability scans, and core/extension
  resource measurements remain open: Docker is reachable, but deployment image
  references, native build/scan inputs, and clean measurement evidence are not
  available in this working environment.
- The object-store conformance runner has a retained local deployed MinIO
  result (8/8 scoped cases) and a secret-safe IAM reconciliation helper. The
  R6 exit gate still requires provider-pair/SeaweedFS comparison, encrypted
  backup, clean-host restore, benchmark, and multi-host storage evidence; the
  bounded empty-target restore preflight is covered by `93bf755`.
- The refreshed local orchestrator and rebuilt tool service are running the
  current source with database migration head `0036_native_trace_spans`. The
  bounded live trace probe observes one API-request row and one native
  `/health` transport span; the separate pure `time_now` probe observes one
  project-usage row and one native `tool_service` span. Retained evidence is
  [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json)
  plus [`provenance/tool_trace_live.json`](provenance/tool_trace_live.json).
  The host-side tool probe now resolves the Compose-only `tool-service:8002`
  alias to loopback only for a local orchestrator; remote configured URLs are
  left unchanged, and both live trace children pass in the corrected aggregate.
  Commit `24c2e35` adds explicit native model/worker source-category coverage
  and a fail-closed selected-worker checker; the fixture passes, but no live
  dispatch is claimed. Representative model-backed worker, identity provider
  mail-edge/bounce, production retention authority/audit/erasure/restore, and native multi-service evidence remain
  open.

The next ledger revision must be generated from a clean frozen commit and an
environment manifest, then include live, recovery, security, and resource
evidence rather than inheriting these static results.
