# AIAT Current Release Ledger

**Run date:** 2026-08-11  
**Base revision:** `c205617c2b7d48113c76b1cdda25d0e31a867d84` (aggregate static run revision)
**Working-tree state:** dirty; this ledger is a P0 progress ledger, not a production release certificate  
**Decision:** **NO-RELEASE / P0 INCOMPLETE**

The latest static aggregation at this revision (2026-08-11T03:47:19Z) is
48/48 checks passing, with two pending technical evidence items and a dirty
working tree; the conservative decision remains **NO-RELEASE**. The configured
live profile is retained separately below and is not silently refreshed by a
static-only run.

This ledger replaces reliance on the historical July snapshot for the current
implementation pass. It records what was actually run and keeps unavailable
live evidence explicit.

## Evidence index

| Boundary | Evidence type | Result | Command or record |
|---|---|---|---|
| Licence metadata evaluator | unit/regression | PASS | `uv run --isolated pytest packages/mas-core/tests/test_worker_governance.py packages/mas-core/tests/test_default_shipped_agents.py apps/orchestrator-api/tests/test_workers_test5_lifecycle.py -q` |
| Normal adapter availability | static/unit/worker reconciliation | PASS | Default manifests expose TruffleHog, Plane, OpenProject, and Ansible as selectable adapter metadata without `excluded_default_tools`; base-profile choices remain technical packaging defaults and `check_worker_reconciliation.py --json` stays green |
| Bounded security scanner adapters | static/unit/tool-service | PASS | `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py -q`; `uv run --isolated python scripts/check_security_adapters.py --json`; `security.scan` accepts `semgrep`, `skillspector`, and `trufflehog` compatibility aliases, routes each through the configured sandbox adapter with bounded output, and preserves the same audit/grant/rate/approval boundary |
| Document ingest fallback | static/unit/tool-service | PASS | `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py -q`; `document.ingest` invokes Docling when installed and otherwise returns usable source text with `available: true`, `configured: true`, `degraded: true`, and `backend: plain_text_fallback`; this does not claim Docling installation or external adapter certification |
| Optional licence metadata stage | unit/regression | PASS | `test_license_metadata_stage_can_be_skipped` proves `SOURCE_REVIEW` can move directly to `SECURITY_REVIEW`; the compatibility `LICENSE_REVIEW` label is optional metadata and cannot block or delay normal internal use |
| Release environment manifest | static/reproducibility | PASS | `uv run --isolated python scripts/check_release_environment.py --json` emits `aiat.release-environment.v1` with the current revision, thirteen release-input hashes covering operator pins, security evidence, worker manifests, and the prior inputs, tool identities, environment-presence flags, and deterministic digest `865fab3f44a0ab8a846483f71cbb0c42f119ea7f3e2655e108b2af7900c11756` without values or credentials; current WSL evidence records 362 changed paths, 5/6 tools available, and 0 configured optional inputs; dirty state remains a release decision reason |
| Operator runtime/CLI pin contract | static/source | PASS | `uv run --isolated python scripts/check_operator_pins.py --json` verifies nine exact production tool/dependency pins and records seven host/optional/deployment capabilities as explicitly unavailable until exact identities are supplied; this technical check is independent of licence metadata |
| Backend and team-runner regression suite | unit/integration | PASS | `uv run --isolated pytest packages/mas-core/tests apps/orchestrator-api/tests apps/tool-service/tests apps/team-runner/tests -q` |
| Bounded metric labels and project-state metric | static/API integration + local live scrape | PASS (static + local scrape; many-project open) | `uv run --isolated pytest packages/mas-core/tests/test_metric_series_budget.py apps/orchestrator-api/tests/test_metrics.py -q`; `check_metric_series_budget.py --json` emits the complete AIAT label inventory, classifies every label as bounded by a protocol/registry/catalogue/bucket contract, rejects `project_id`, passes the synthetic 10,000-project/2,000-total fixture, and the live parser folds Prometheus' synthetic histogram `_created` sample into its declared family; the refreshed local scrape passes at 31 series with no `project_id` label and is retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json), while native many-project scrape remains open |
| HTTP/message/trace-evidence propagation | static/unit/API + local live transport/tool | PASS (committed core; local read-back; broader sources open) | Core contract commit `77d5494`; `uv run --isolated pytest packages/mas-core/tests/test_tracing.py packages/mas-core/tests/test_trace_evidence.py packages/mas-core/tests/test_native_trace_spans.py -q`; `uv run --isolated python scripts/check_trace_evidence.py --json` and `scripts/check_native_trace_spans.py --json` pass. API/storage writer wiring, local live transport/tool read-back, model-backed worker, identity provider mail-edge, and live retention remain separate evidence boundaries |
| API observation ledger | static/unit/API/fixture | PASS (bounded) | `uv run --isolated pytest packages/mas-core/tests/test_api_observations.py apps/orchestrator-api/tests/test_trace_propagation.py -q`; `uv run --isolated python scripts/check_api_observability.py --json` passes normalized-route, status/duration-bound, trace-safe, payload-free fixture; migrations `0034_api_request_observations`, `0035_trace_correlation_evidence`, and `0036_native_trace_spans` plus storage read/write methods are checked in |
| Native trace-span contract | static/unit/fixture + local live transport/tool | PASS (committed core; local read-back; model/provider open) | Core contract commit `77d5494`; `uv run --isolated pytest packages/mas-core/tests/test_native_trace_spans.py packages/mas-core/tests/test_trace_evidence.py -q`; `uv run --isolated python scripts/check_native_trace_spans.py --json` passes scalar allow-list/redaction. API/storage writer wiring, local live transport/tool read-back, provider mail-edge/live-retention, and representative model-backed worker coverage remain open |
| Trace retention planner | static/unit/fixture | PASS (non-mutating; live open) | `uv run --isolated pytest packages/mas-core/tests/test_trace_retention.py -q`; `uv run --isolated python scripts/check_trace_retention.py --json` passes deterministic `aiat.trace-retention-plan.v1` retain/archive/delete/invalid classification; no database mutation, live deletion/archive, project narrowing, or restore parity is claimed |
| Signed identity mail SLO/trace projection | static/unit/client/API projection | PASS (bounded optional source) | `PYTHONPATH=apps/identity-service uv run --isolated pytest apps/identity-service/tests/test_identity_service.py -q` and `uv run --isolated pytest apps/orchestrator-api/tests/test_identity_reconciliation.py apps/orchestrator-api/tests/test_slo_capacity.py -q`; identity migration `0002_mail_trace_correlation` persists safe delivery-attempt trace/span IDs, while signed `/v1/dashboard/mail-relay` rows are reduced to outcome/time/trace metadata only and filtered to the requested window; absent identity configuration remains explicit `mail_delivery: no_data` |
| SLO/capacity operational read models | static/unit/API/fixture + local transport evidence | PASS (bounded; local transport observed; native model/tool/mail evidence open) | `uv run --isolated pytest packages/mas-core/tests/test_slo.py apps/orchestrator-api/tests/test_slo_capacity.py -q`; `uv run --isolated python scripts/check_slo_capacity.py --json` passes its deterministic fixture and `--live --json` remains the bounded report probe; the local trace transport evidence is retained at [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json). `aiat.slo-policy.v1`, `aiat.slo-report.v1`, and `aiat.capacity-forecast.v1` cover descriptive API/queue/worker/tool/model/PM/SCM/mail/recovery targets, durable usage cost/token forecasts, confidence, and budget headroom; durable API request, optional signed identity mail-attempt, PM/SCM delivery, and worker-recovery rows are projected when present, while absent native model/tool/mail-edge/full-span sources remain `no_data`/`insufficient_data` and do not block execution |
| LiteLLM/OmniRoute monitoring adapter | unit/fixture | PASS (non-networking) | `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py -k monitoring_adapter -q`; the configured adapter emits `aiat.monitoring-analytics-plan.v1` endpoint/synthetic-check metadata without probing or mutating services; Prometheus-compatible output remains optional |
| Third-party inventory policy | static validation | PASS | `uv run --isolated python scripts/check_provenance.py` (`21 components`, including the operator-supplied SkillSpector metadata record) |
| Documentation authority/index | static validation | PASS | `uv run --isolated python scripts/check_docs_index.py --json`; the canonical target, eleven current feature documents, three ordered plans, maintained links, roadmap references, and metadata-only policy markers reconcile |
| Changed Python paths | static validation | PASS | isolated `compileall` for worker registry, orchestrator API, dashboard policy, image-contract, and provenance paths |
| CEO/service/dashboard ACL | API security | PASS | `uv run --isolated pytest apps/orchestrator-api/tests/test_auth_boundary.py -q` |
| Team-runner storage/network boundary | static + API/client contract + local live matrix | PASS (static/API + refreshed WSL2 live) / BLOCKED (native release host) | Commit `43bee16`; `uv run --isolated pytest apps/orchestrator-api/tests/test_team_runner_storage_boundary.py apps/team-runner/tests/test_storage_client.py packages/mas-core/tests/test_network_boundary.py -q`; `uv run --isolated python scripts/check_network_boundary.py --json` and `--live --json` pass all 11 current WSL2 runners with gateway allow-list, protected-service/egress denial, storage-health read-back, no forbidden runner environment names, and no Docker sockets; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json) |
| Object-store contract fixture/runner | static/unit + local deployed live run | PASS (fixture + reconciled local MinIO live; aggregate private-network child pass; external open) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_conformance.py packages/mas-core/tests/test_phase7_storage.py -q`; `bash infra/compose/scripts/check-minio-conformance.sh` and the deterministic fixture both pass 8/8 scoped cases after the credential-safe persisted IAM reconciliation; `check_object_store_conformance.py --live --json --compose-local` now invokes the same probe inside the orchestrator container, so the release child no longer times out on the private `minio:9000` alias; secret-safe evidence is retained at [`provenance/object_store_live_conformance.json`](provenance/object_store_live_conformance.json), with the repair helper at [`infra/compose/scripts/reconcile-minio-agent-user.sh`](../infra/compose/scripts/reconcile-minio-agent-user.sh); provider-pair/SeaweedFS, encrypted backup, retention, benchmark, and multi-host evidence remain open |
| Object-store copy/parity fixture | static/unit | PASS (fixture only) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_migration.py -q` and `uv run --isolated python scripts/check_object_store_copy.py --json`; `aiat.object-store-copy.v1` copies explicit project-scoped refs, verifies source/target checksum and size after read-back, preserves source data, and cleans failed target copies; provider-pair migration, cutover, backup, restore, and benchmark evidence remain open |
| Object-store live copy/parity runner | live-runner readiness | PASS (live blocked) | `uv run --isolated python scripts/check_object_store_copy.py --live --json` returns exit 2 with `status: blocked` when source/target configuration is absent; the runner inventories source bytes into checksum-bearing refs, refuses empty inventory, and performs non-destructive target read-back parity when configured; provider-pair evidence, cutover, backup, restore, and benchmark remain open |
| Object-store migration workflow | static/unit/fixture; guarded live boundary | PASS (fixture; live blocked) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_rollout.py packages/mas-core/tests/test_object_store_migration_runner.py -q`; `uv run --isolated python scripts/check_object_store_migration.py --json` proves `aiat.object-store-migration.v1` inventory → verified copy → optional dual-write → human-confirmed cutover → human-confirmed rollback, while `--live --json` returns exit 2 without provider-specific routing/retention/rollback evidence |
| Dashboard ACL policy | unit/contract | PASS | `uv run --isolated pytest packages/mas-core/tests/test_dashboard_access.py -q` |
| Dashboard operator proxy/type contract | static/typecheck | PASS | `npm run typecheck` in `apps/mas-dashboard`; credential and identity proxies use the operator key plus bounded section context |
| Dashboard local Compose E2E matrix | authenticated local UI | PASS WITH EXPLICIT SKIP (34/35) | `npm run test:e2e -- --workers=1` in `apps/mas-dashboard`; focused shell and identity regressions also pass (`e2e/dashboard-shell-accessibility.spec.ts` 2/2, `e2e/identity-states.spec.ts` 1/1), the targeted system/PM resilience filter passes 4/4, and the one-test flow-builder golden path passes after the project flow catalogue was made `cache: "no-store"`. The WSL2 Compose run covers operational UI, CEO chat/hiring, project workspace, schema-driven flow editing, all eight branching/recovery scenarios, hiring board, identity stale-record/retry state, PM integration conflict/stale retry, project-detail stale/retry state, runtime-status panels, shell skip-link/mobile focus recovery, and system-visualization partial/offline retry states. Secret-safe evidence is [`provenance/dashboard_e2e_live.json`](provenance/dashboard_e2e_live.json); the live DLQ replay case is intentionally skipped pending an operator-owned safe fixture, and native-Linux/mobile/WCAG evidence remains open |
| External-account action policy | static/API/unit | PASS | `PYTHONPATH=apps/identity-service uv run --isolated pytest apps/identity-service/tests/test_identity_service.py -q`; `aiat.external-account-action-policy.v1` exposes category-sensitive signup, rotation/closure human gates, immediate suspension, and governed browser-session rules |
| Production image contract | static + fail-closed live identity | PASS (static; live blocked) | Contract `7d69fbd`; `uv run --isolated pytest packages/mas-core/tests/test_image_provenance_runner.py -q` and `uv run --isolated python scripts/check_image_provenance.py --json` pass the immutable Compose/Dockerfile contract; the current Docker CLI is reachable, but `--live --json` returns exit 2 because deployment-supplied immutable `*_IMAGE_REF` values are absent; live scope is local `RepoDigests` identity only and does not claim SBOM/scan/build evidence. Development-only wrapper defaults are isolated in `b9a77e9` |
| Worker manifest/runtime/provenance reconciliation | static + read-only authenticated local live binding + scan-evidence linkage | PASS (39/39 persisted defaults; technical findings remain open) | `uv run --isolated python scripts/check_worker_reconciliation.py --json` plus authenticated local `--live --json`; all 39 checked-in defaults match persisted `/capabilities/workers` rows with zero missing rows or binding mismatches. Secret-safe evidence is [`provenance/worker_reconciliation_live.json`](provenance/worker_reconciliation_live.json). Coding/tester rows link to `security_scan_evidence.yaml`, which records 316 Semgrep findings and 54 engine warnings, so both remain non-activatable; host package availability is advisory and Compose runtime imports are recorded separately |
| Default worker implementation bindings | static contract + explicit live boundary | PASS (static; live blocked by operator environment) | `uv run --isolated pytest packages/mas-core/tests/test_default_worker_bindings.py -q`; `uv run --isolated python scripts/check_default_worker_bindings.py --json` reconciles all 15 documented default worker slots across department, runtime, transport, isolation, runtime-catalogue support, runtime/integration adapter entrypoints, capability, adapter configuration, and required tools. `--live --json` is fail-closed and returns exit 2 until an operator selects a live environment; the report never treats licence metadata as a gate |
| Worker-run lifecycle contract | deterministic controller fixture + explicit live boundary | PASS (fixture; live blocked without operator-selected run) | `uv run --isolated pytest packages/mas-core/tests/test_worker_run_lifecycle.py -q`; `uv run --isolated python scripts/check_worker_run_lifecycle.py --json` drives the real `WorkerRunController`/`NativeWorkerAdapter` through checkpoint persistence, pause/resume, cold cancellation, cold-crash failure normalization, lease-expiry requeue, and artifact/usage-before-terminal ordering; `--live --json` returns exit 2 without mutating a live run, while database/sandbox/canary/rollback evidence remains open |
| Exact OpenCode security scan | external source + Semgrep evidence | FINDINGS_REVIEW_REQUIRED | Exact commit `10c894bdeef3618f5666fb506ef7f9491bb964d8` (`v1.17.13`) scanned with Semgrep `1.168.0`; 5,062 files, 316 findings, 19 `ERROR`, 54 engine warnings; evidence digest `e960a57d7b397d8af9271025b2025b0f46567967b4c684eaab8652a75d47d3b5` |
| Worker certification matrix | deterministic static declaration | PASS | `uv run --isolated python scripts/generate_worker_certification_matrix.py --check`; 39 rows separate declared runtime/adapter inputs from pending security, canary, and live evidence; `test_worker_governance.py` exercises LangGraph/CrewAI bridge conformance |
| Microsoft Agent Framework adapter boundary | unit/fixture | PASS (compatibility boundary; activation open) | `uv run --isolated pytest packages/mas-core/tests/test_microsoft_agent_framework_adapter.py -q`; fake-module coverage proves bounded Agent/ChatAgent construction, async run/invoke translation, shutdown, and missing package/instructions fail-closed paths; locked package/MCP compatibility and activation are tracked separately |
| Microsoft Agent Framework/MCP compatibility lock | static contract + preflight | PASS (activation blocked) | `uv run --isolated pytest packages/mas-core/tests/test_microsoft_agent_framework_compatibility.py packages/mas-core/tests/test_microsoft_agent_framework_adapter.py -q`; `uv run --isolated python scripts/check_runtime_compatibility.py --json` records `agent-framework==1.13.0`, MCP `>=1.27,<2`, current workspace MCP `1.23.3`, and missing optional MAF as explicit blockers; no installation or network mutation occurs |
| Code-review adapter catalogue/default | static/unit/fixture | PASS (external candidates blocked) | `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py apps/tool-service/tests/test_default_shipped_tool_catalog.py -q`; `uv run --isolated python scripts/check_code_review_adapters.py --json` proves the local deterministic diff reviewer is the default and generic external candidates cannot activate without exact source/revision/version evidence |
| Worker runtime readiness | static + live import probe | PASS (local Compose image imports; certification remains open) | `uv run --isolated pytest packages/mas-core/tests/test_worker_runtime_readiness.py -q`; `uv run --isolated python scripts/check_worker_runtime_readiness.py --json` reports 39 manifests without certifying them; the aggregate `--live --json --compose-local` probe imports required LangGraph/CrewAI packages inside `mas-orchestrator-api-1` and passes with no required runtime blockers; secret-safe evidence is retained at [`provenance/worker_runtime_readiness_live.json`](provenance/worker_runtime_readiness_live.json), while optional MAF/AutoGen/Letta imports and security, sandbox, canary, live-run, and rollback checks remain separate |
| Default runtime install profile | static source/lock/Dockerfile contract | PASS | `uv run --isolated python scripts/check_runtime_install_profile.py --json` reconciles the `runtime-default` LangGraph/CrewAI extra, locked versions (`langgraph` 0.6.11 and `crewai` 1.6.1), runtime-catalogue imports, and production Dockerfile install command; package imports and worker certification remain separate |
| Default worker steward lifecycle | deterministic domain fixture + restart-safe persistence path | PASS (domain evidence; live open) | `uv run --isolated python scripts/check_worker_steward_contract.py --json` plus `uv run --isolated pytest apps/orchestrator-api/tests/test_steward_rehydration.py packages/mas-core/tests/test_compatibility_matrix_persistence.py -q` exercises dedicated steward, immutable candidate, compatibility matrix, certification/approval, shadow → read-only canary → promotion, regression blocking, and pre-activation rollback for both externally sourced default workers; certification records the matrix in the same-process steward cache and durable store, while API rehydration restores durable active bundle/adapter IDs plus persisted compatibility-matrix rows, normalizing profile/capability JSON and failing closed on malformed evidence. Evidence is [`provenance/worker_steward_contract.json`](provenance/worker_steward_contract.json). Database reconciliation, security, sandbox, live canary, and worker-run evidence remain open |
| Sandbox runtime readiness | static + live Docker runtime probe | PASS (live blocked) | `uv run --isolated pytest packages/mas-core/tests/test_sandbox_runtime_readiness.py -q`; `uv run --isolated python scripts/check_sandbox_runtime_readiness.py --json` reconciles 39 manifests and 10 hardened external workers; the Docker CLI is reachable but `--live --json` returns exit 2 because the required gVisor `runsc` runtime is not registered, while digest-pinned smoke/network, canary, and Firecracker checks remain separate |
| Runtime benchmark readiness | read-only live API probe | PASS (authenticated local live evidence) | `uv run --isolated pytest packages/mas-core/tests/test_runtime_benchmarks.py -q`; authenticated `uv run --isolated python scripts/check_runtime_benchmarks.py --live --url http://localhost:8000 --json` passed deterministic LangGraph/CrewAI dependency dry-runs on 2026-08-11; [`runtime_benchmarks_live.json`](provenance/runtime_benchmarks_live.json) retains bounded results. Missing API/package/validation evidence remains exit 2; package benchmarks never certify a worker canary/live run/rollback |
| Default runtime adapter conformance | unit/fixture + Compose package/lifecycle and lock-parity probe | PASS (adapter and exact-lock evidence; worker certification open) | `uv run --isolated pytest packages/mas-core/tests/test_runtime_adapter_conformance.py -q`; `docker exec mas-orchestrator-api-1 python /app/scripts/check_runtime_adapter_conformance.py --live --json` passes actual `LangGraphAdapter` and `CrewAIAdapter` classes with locked LangGraph `0.6.11` and CrewAI `1.6.1`, verifying manifest/message translation, bounded completion, health, and shutdown without model/tool/provider/project calls; secret-safe evidence is [`provenance/runtime_adapter_conformance_live.json`](provenance/runtime_adapter_conformance_live.json). Sandbox, canary, live worker-run, and rollback evidence remain open |
| Model override expiry and budget settlement replay | focused unit/storage | PASS (preparatory P1) | `uv run --isolated pytest apps/orchestrator-api/tests/test_worker_dispatch_governance.py -k 'model_override_expiry or successful_dispatch or failed_dispatch or setup_failures' -q` and `uv run --isolated pytest packages/mas-core/tests/test_phase7_storage.py -k 'settle_budget_reservation' -q`; serialized expiry fails closed and terminal settlement retries are no-ops; provider-specific live recovery and settlement chaos evidence remain open |
| LLM gateway transient failure vocabulary | static/unit (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_gateway_providers.py packages/mas-core/tests/test_phase4_5.py -q`; normal/streaming/fallback gateway paths share explicit 408/409/412/425/429/5xx retry classification while permanent client 4xx responses remain non-retryable |
| Model/provider cooldown failover | static/unit/persistence (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_observability.py packages/mas-core/tests/test_persistence.py packages/mas-core/tests/test_gateway_providers.py -q`; bounded model cooldowns, two-failure provider cooldowns, persisted state, SmartRouter exclusion, and automatic fallback filtering pass; provider-specific live outage/recovery and chaos evidence remain open |
| Model profile/runtime catalogue | static/unit/API/dashboard + local live read-only reconciliation (preparatory P1) | PASS WITH FINDINGS (approved coverage present; reconciliation pending) | `uv run --isolated pytest packages/mas-core/tests/test_model_profile_catalogue_runner.py packages/mas-core/tests/test_model_profile_seeder.py -q`; `uv run --isolated python scripts/check_model_profile_catalogue.py --json`; `--live --json --require-approved` observes 93 registry models, 94 persisted profile versions, 92 approved covered entries, one pending registry model, and two non-registered profile findings; secret-safe evidence is retained at [`provenance/model_profile_catalogue_live.json`](provenance/model_profile_catalogue_live.json). The API/dashboard export remains deterministic; stale/extra profile reconciliation and provider recovery remain open |
| Default model-profile bootstrap | static/unit/API seed path + live persisted evidence | PASS WITH FINDINGS | `uv run --isolated pytest packages/mas-core/tests/test_model_profile_seeder.py apps/orchestrator-api/tests/test_system.py -q`; startup and `/system/seed-default-company` invoke the idempotent `aiat.model-profile-bootstrap.v1` declaration for all 93 registry models plus `opencode-phase0b-coding`/`omniroute-coding`, preserve operator rows, and block conflicts; the live catalogue now confirms 94 persisted versions and 92 approved registry bindings, while one pending model and two non-registered rows remain explicit findings |
| Model profile live verifier | fail-closed runner | PASS (local live; findings retained) | `uv run --isolated python scripts/check_model_profile_catalogue.py --live --json --require-approved` now passes the approval gate against the local API and reports `pass_with_profile_findings`; missing API/auth still exits 2, while the retained report records 92 approved covered entries, one pending model, and two non-registered findings without exposing profile payloads |
| Executive reconciliation and role views | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | `uv run --isolated pytest packages/mas-core/tests/test_executive_reconciliation.py apps/orchestrator-api/tests/test_model_profile_catalogue.py -q`; deterministic `aiat.executive-reconciliation.v1` aggregates durable project/usage/worker-run/budget/model evidence, `aiat.executive-views.v1` projects bounded CFO/CTO/CEO summaries, `/executive/views/{role}` exposes one projection over the same report, reservation/settlement invariants are checked, and `/executive/reconciliation` plus System Overview expose the read-only surface; `scripts/check_executive_reconciliation.py --live --json` now provides a secret-safe bounded coverage/finding verifier with optional `--require-clean`, returning exit 2 when live API/DB evidence is unavailable; role-scoped write routes are recorded separately below, while live DB population/provider recovery remains open |
| Executive write action envelope | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | `uv run --isolated pytest apps/orchestrator-api/tests/test_executive_actions.py apps/orchestrator-api/tests/test_api_contract.py -q`; `aiat.executive-action.v1` routes CFO model overrides through the durable request owner, CTO worker runs through canonical governed dispatch, and CEO privileged actions through the audited gate; worker output/payload secrets are excluded from responses; dashboard proxies and the typed confirmation panel cover all three routes; local profile evidence is partial (92/94 approved covered with one pending and two non-registered findings), while provider/live recovery, broader governance forms, and chaos evidence remain open |
| Guarded self-improvement lifecycle | static/unit/storage/API fixture; explicit live boundary | PASS (fixture/storage/API; live blocked without worker/control-plane integration) | `uv run --isolated pytest packages/mas-core/tests/test_self_improvement.py packages/mas-core/tests/test_flow_templates.py packages/mas-core/tests/test_phase7_storage.py apps/orchestrator-api/tests/test_self_improvement_api.py -q`; `uv run --isolated python scripts/check_self_improvement_lifecycle.py --json` proves `aiat.self-improvement.v1` owner/risk/budget/evidence project request, independent technical gates, human-only promotion, shadow/canary, exact rollback, revisioned link snapshots, bounded terminal outcome/cost/incident/rollback/KPI-learning persistence with idempotent IDs, and frozen five-kind `aiat.self-improvement-artifacts.v1` manifest generated from normalized worker records with canonical artifact-row IDs plus SHA-256/size read-back parity for all five artifacts; authenticated project creation plus lifecycle GET/reference/action endpoints use the canonical project config/history writer; `--live --json` returns exit 2 until live worker/control-plane integration is configured; external certified-worker/provider and live issue/worker-run/branch/artifact/budget/deployment evidence remain open |
| Self-improvement candidate detection | static/unit/fixture; live signal boundary | PASS (fixture; live blocked) | `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` proves `aiat.self-improvement-candidate-detection.v1` normalization for defect/metric/upstream-update/cost/operator-goal signals, exact duplicate collapse, conflicting-ID rejection, deterministic risk/budget mapping, secret-safe metadata, and zero project/budget/credential/deployment side effects; `--live --json` remains blocked until operator-selected signal sources exist |
| CEO evidence envelope and citation route | static/unit/API/dashboard (preparatory P1) | PASS (bounded deterministic + fallback citation surface) | `uv run --isolated pytest apps/orchestrator-api/tests/test_ceo_chat.py -q` plus dashboard `npm run typecheck`; API-owned CEO actions/read responses publish and return `aiat.ceo-evidence.v1` with bounded scalar references and traces, never arbitrary payloads or secret values; the legacy fallback accepts only explicit stripped `AIAT_EVIDENCE: kind=id` markers and labels them `unverified`; dashboard renders owning-section links for project/flow/governance/worker/credential/integration/tool/project-evidence/log surfaces plus dedicated `/evidence/{kind}/{id}` records. Resource-specific detail loading and full golden paths remain open |
| Compose network denial/allow matrix | live integration | PASS (refreshed local WSL2 matrix); BLOCKED (native release host) | The `43bee16` Compose boundary was recreated without removing volumes. `check_network_boundary.py --live --json` passes all 11 runners with named gateway access, protected-service and unapproved-egress denial, control-plane storage health, no forbidden runner environment names, and no Docker sockets; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json). Native-Linux recreation, provisioned-key dashboard ACL coverage, and post-fix closure of `DEF-2026-07-14-036` remain open. |
| CEO service identity and section ACL | static + API security | PASS (static/API + authenticated local matrix); native UI/release matrix open | Distinct CEO/worker principals, persisted `system_config` ACL, operator-only update, dashboard section context, and human/CEO/service/worker matrix pass. The refreshed local endpoint matrix is retained at [`provenance/dashboard_acl_live.json`](provenance/dashboard_acl_live.json); native-Linux deployment/UI denial evidence remains open |
| Production image digest inventory | supply-chain/release | PASS (static contract); live blocked/open | `docs/provenance/production_images.yaml` matches Compose image variables; fixed infrastructure refs and Dockerfile bases carry digests; application/gateway refs require deployment-supplied immutable `*_IMAGE_REF` values; `check_image_provenance.py --live --json` is fail-closed and records only local `RepoDigests` identity; development `mas.sh` fallbacks are isolated to the dev overlay; SBOM/clean pull/scan reconciliation not run |
| Object-store backup/restore manifest | static/unit fixture + local same-provider live rehearsal | PASS (fixture + local MinIO rehearsal; provider-diverse open) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_backup.py -q`; `uv run --isolated python scripts/check_object_store_backup_restore.py --json` proves `aiat.object-store-backup.v1` source → backup → clean-target restore with exact key/checksum parity; `bash infra/compose/scripts/check-minio-backup-restore.sh` passes two disposable objects with manifest/read-back parity and scoped cleanup, retained at [`provenance/object_store_backup_restore_live.json`](provenance/object_store_backup_restore_live.json); the explicit three-provider `--live --json` runner still blocks without separate providers, encryption, retention, provider durability, and clean-environment disaster-recovery evidence |
| Metric-series budget | static + local live scrape | PASS (static + local scrape; native many-project open) | `uv run --isolated python scripts/check_metric_series_budget.py --json` exercises 10,000 synthetic projects with 2,000-total/32-project-state budgets and no `project_id` label; `--live --json` passes the refreshed local orchestrator scrape at 31 bounded series after normalizing the histogram `_created` sample, retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json); native many-project scrape evidence is still open |
| Tool-service image budget | static contract + local build/resource probe | PASS (static + local Linux probe; native release open) | Contract `b24ca0c`; `uv run --isolated pytest packages/mas-core/tests/test_image_budgets.py -q` and `uv run --isolated python scripts/check_image_budgets.py --json` pass the profile ceilings; retained local measurements are core `267957904` bytes and extensions `4155668123` bytes, with bounded loopback `/health` probes at 26,836 ms/112.3 MiB and 29,913 ms/137.7 MiB respectively. Evidence is [`provenance/image_budgets_live.json`](provenance/image_budgets_live.json); compressed archive size, clean native-Linux build/pull, and vulnerability/SBOM evidence remain open |
| API/protocol contract export | static contract (preparatory P1) | PASS | `uv run --isolated python scripts/check_api_contract.py --json`; 233 OpenAPI paths, `aiat.v1`, and generated dashboard/SDK types match runtime/provenance hashes, including native trace-evidence, SLO/capacity, evidence-package, self-improvement outcome-action, artifact-bundle/read-back fields (`OpenAPI 73465edfc52963e02068b276dfd0896ec7d7e3629db11b04431828d321488927`, `protocol 1c69427b868da88b3889a2c8b6701a99de0c01001b054121203bdd433b29a8f7`, TypeScript `b34f67a8b86574ea8ee6bfbdc4526e279d4bb504ce3b2eff8a38cac28ad15b39`, Python `c2f5a67edb99ae411bd82f9245875036349c66fefff12c1422d70a9a6cadd972`) |
| Built-in PM/SCM adapter declarations | static/unit/fixture; provider live boundary | PASS | `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_declarations.py -q`; `uv run --isolated python scripts/check_provider_adapter_declarations.py --json` reconciles the real YouTrack adapter plus GitHub `pm`/`delivery`/`checks` profiles and bounded path guards with zero provider HTTP calls or mutations; provider-specific mock/live/outage/restore evidence remains open |
| Built-in PM/SCM mocked HTTP conformance | static/unit/fixture; provider live boundary | PASS | `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_http_conformance.py -q`; `uv run --isolated python scripts/check_provider_adapter_http_conformance.py --json` drives eight real-adapter YouTrack/GitHub cases through local mocked responses for health/configuration, projection/read-back, cursors, deactivation, comments/links, GitHub source-control paths, webhook handling, and retryable/permanent failures; no external HTTP or provider mutation occurs |
| External-account lifecycle fixture | static/unit/fixture; provider live boundary | PASS | `uv run --isolated pytest packages/mas-core/tests/test_external_account_lifecycle.py -q`; `uv run --isolated python scripts/check_external_account_lifecycle.py --json` drives the actual `IdentityService` through eight in-memory cases for category approval/idempotency, one-use browser leases, credential rotation/session revocation, closure approval, immediate suspension, fail-closed unknown categories, and secret-safe output without external account/provider calls |
| Machine-readable release ledger | static aggregation; live boundary | PASS (static + refreshed local boundary); BLOCKED/NO-RELEASE (aggregate live profile) | `uv run --isolated python scripts/check_release_ledger.py --json` aggregates 48 static checks with no failures. The current configured 64-check `--live --json` profile records 55 passes, zero failures, and nine externally blocked probes; the authenticated worker-reconciliation child passes 39/39 persisted defaults with zero missing rows or binding mismatches, while trace/tool endpoint configuration, default-worker certification, image identity, gVisor, outbound relay, and self-improvement source remain blocked. Each child checker is bounded by a 60-second timeout and the summary is retained at [`provenance/release_ledger_live.json`](provenance/release_ledger_live.json). The standalone local boundary matrix is green. Clean-worktree and the remaining native/provider evidence remain separate gates. |
| Project evidence package | static/unit/API/dashboard (preparatory P1) | PASS (committed core/API/dashboard surfaces; project-page/live preparatory) | `a44a1aa` commits the deterministic `EvidencePackage` builder, scope-policy resolver, focused tests, and both fail-closed fixture checkers; `d0472af` isolates package-level workflow exports; `cbf00d9` adds the isolated package/policy API router, operator-only snapshot route, company/project policy writes, and clean-checkout route tests; `82bbaeb` adds the project evidence page, canonical evidence deep-link page, and API proxies with clean-checkout `npm run typecheck`; `aiat.project-evidence-package.v1` groups repository, documents, tests, security, deployment, cost, approvals, flow, worker, artifact, and audit sources, while licence/restriction values remain metadata-only notices; project-page composition, live durable snapshot/provider/worker generation, and native recovery remain open |
| Dashboard contract generation | static/typecheck (preparatory P1) | PASS | `uv run --isolated python scripts/generate_typescript_api.py --check`; `npm run typecheck` in `apps/mas-dashboard`; 129 models and 266 operations generated deterministically |
| Python SDK contract generation | static/unit (preparatory P1) | PASS | `uv run --isolated python scripts/generate_python_api.py --check`; `uv run --isolated pytest packages/mas-api-sdk/tests -q`; 129 models and 266 operations match the OpenAPI artifact |
| Company manifest policy fields | static/contract (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_company_manifest.py -q`; typed optional timezone, retention (including trace days/sample rate), privacy, evidence, model, and deployment blocks validate, and legacy manifests remain compatible |
| Company timezone propagation | static/unit/typecheck (preparatory P1) | PASS | Runner prompt/time-tool/scheduler tests and dashboard typecheck pass; Compose, `.env.example`, and development wrapper consume `AIAT_COMPANY_TIMEZONE`; persistence remains UTC |
| Prompt/tool and review contract reconciliation | static/unit (preparatory P1) | PASS | `uv run --isolated python scripts/check_prompt_tool_reconciliation.py --json`; all 11 shipped prompts resolve to 114 concrete, policy-allowed tools; review submissions use canonical `REVIEW_RESPONSE` envelopes and CEO privileged requests use `/ceo/privileged-action` |
| PM/SCM provider conformance fixture | static/unit/CLI (preparatory P1) | PASS (fixture; live blocked) | `uv run --isolated pytest packages/mas-core/tests/test_pm_integrations.py packages/mas-core/tests/test_provider_conformance_runner.py -q`; `uv run --isolated python scripts/check_provider_conformance.py --json` passes 10 fake-provider cases; `--live --json` returns exit 2 until provider-specific sandbox/mock HTTP/outage/restore evidence is supplied |
| Flow node-schema contract | static/unit (preparatory P1) | PASS | `uv run --isolated python scripts/generate_flow_node_schemas.py --check`; 9 node types at v1.0 match backend validation, JSON artifact, `/flows/node-schemas`, and generated dashboard metadata |
| Dashboard node-schema editor | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; both flow editors render the generated contract plus editable typed form, governed worker/profile selectors, CSV/JSON fields, deprecated `team_id`/`action` fields only in collapsed compatibility controls, and the dashboard store/proxy exposes explicit saved-definition migration |
| Evidence-policy contract | static/unit (preparatory P1) | PASS | Evidence tests cover required artifact kinds; `/evidence-policies` publishes built-ins and policy dry-run validation includes worker-run/repository resources |
| Evidence-policy selection | static/API/unit (preparatory P1) | PASS | Company defaults persist through the active manifest; `PUT /projects/{project_id}/evidence-policy` persists project defaults and milestone overrides; `resolve_evidence_policy_selection` and `check_evidence_policy_resolution.py --json` cover project-milestone → project → flow → company-milestone → company → manual precedence without using licence metadata as a gate; live transition proof remains open |
| Evidence-policy scope resolution fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | `uv run --isolated pytest packages/mas-core/tests/test_evidence_policy_resolution.py -q`; `uv run --isolated python scripts/check_evidence_policy_resolution.py --json` passes seven deterministic precedence/fallback cases and reports `licence_metadata_is_gate: false`; `--live` is explicitly blocked without an authenticated API scenario |
| External-account action policy fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | `uv run --isolated pytest packages/mas-core/tests/test_external_account_action_policy.py -q`; `uv run --isolated python scripts/check_external_account_action_policy.py --json` reconciles all five action rules, four category dispositions, and fail-closed unknown action/category behavior without mutating identity/provider state; `--live` is blocked until a provider-specific sandbox/outage scenario exists |
| Outbound-mail lifecycle fixture | static/unit/fixture (relay live open) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_outbound_mail_lifecycle.py -q`; `uv run --isolated python scripts/check_outbound_mail_lifecycle.py --json` drives the actual `IdentityService` through approval pause, request/submission idempotency, definitive provider-failure retry, ambiguous-outage reconciliation hold, and secret-safe output without external relay calls |
| Self-improvement candidate detection | static/unit/fixture (live signal sources open) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` reconciles defect, metric, upstream-update, cost, and operator-goal signals with deterministic deduplication/risk/budget mapping, conflicting-ID rejection, secret-safe metadata, and zero project/budget/credential/deployment side effects |
| Reusable flow templates | static/unit/API (preparatory P1) | PASS | Six canonical templates validate through `validate_flow`; `/flow-templates` and `/flows/from-template` are covered by `test_flow_templates.py` and `test_flows.py` |
| Dashboard canonical template consumption | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; new-flow starter cards fetch `/api/flow-templates`, preserve template configs/evidence metadata, remap branch references, and retain a blank-canvas fallback |
| Flow definition lifecycle | static/API (preparatory P1) | PASS | Flow export/hash, deterministic diff, import, publish, deprecate, compatible migration, and explicitly mapped active-node graph-rewrite endpoints are covered by `apps/orchestrator-api/tests/test_flows.py`; live recovery remains open |
| Flow-instance recovery boundary | static + guarded live API probe | PASS (blocked without instance/API) | `uv run --isolated pytest packages/mas-core/tests/test_flow_instance_recovery.py -q`; `scripts/check_flow_instance_recovery.py` is read-only by default, requires `--confirm` for state-changing actions, validates post-action state/execution history, and returns exit 2 when URL/instance/live evidence is absent; full worker/project/UI recovery remains open |
| Flow control-topology contract | static/unit/fixture (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py -q`; `uv run --isolated python scripts/check_flow_topology.py --json` reconciles parallel branch declarations, join fan-in, and switch case targets with persisted edges; no worker dispatch/storage mutation occurs and live fan-out/recovery remains open |
| Flow execution semantics contract | static/unit/fixture (preparatory P1) | PASS (deterministic traversal; live open) | `uv run --isolated pytest packages/mas-core/tests/test_flow_execution_semantics.py -q`; `uv run --isolated python scripts/check_flow_execution_semantics.py --json` drives the real traversal engine through fan-out, one-branch join waiting, single join scheduling, selected switch routing, and unknown-case blocking; duplicate join scheduling and completed-join reactivation are prevented, while live fan-out/join/watchdog/recovery evidence remains open |
| Governed asynchronous flow-task binding | static/unit/fixture (preparatory P1) | PASS (binding contract; live open) | `uv run --isolated pytest packages/mas-core/tests/test_flow_worker_binding.py packages/mas-core/tests/test_flow_retry_persistence.py apps/orchestrator-api/tests/test_flows.py -q`; `uv run --isolated python scripts/check_flow_worker_binding.py --json` proves queued/claimed/running Worker Runs keep their task active, terminal states settle, parallel bindings remain copy-on-write, safe retry re-enters governed dispatch, and unknown states fail closed; both API retry paths preserve prior executions as `SUPERSEDED`, including the no-safe-node fallback; live worker canary/recovery remains open |
| Watchdog and safe-retry recovery semantics | static/unit/fixture (preparatory P1) | PASS (controller contract; live open) | `uv run --isolated pytest packages/mas-core/tests/test_workflow_watchdog_recovery.py -q`; `uv run --isolated python scripts/check_workflow_watchdog_recovery.py --json` proves boot grace, downtime-aware timeout, watchdog failure transition, recorded-safe-state retry, and terminal-state exclusion without storage/worker mutation; native watchdog/cold-recovery remains open |
| Flow compatibility-alias audit | static/API/unit (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_dry_run.py -q`; dry-run returns deterministic `compatibility_aliases` findings and concrete worker-binding recommendations without mutating saved definitions |
| Saved-definition worker migration | static/API/unit (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_legacy_migration.py -q`; `POST /flows/{flow_id}/migrate-legacy-tasks` previews missing/unknown bindings and creates an immutable worker-bound version with `aiat.flow-legacy-task-migration.v1` metadata; live worker canary/recovery remains open |
| Sprint retrospective/profile lineage | focused API/unit (preparatory P1) | PASS | `timeout 45s uv run --isolated pytest apps/orchestrator-api/tests/test_test7_operator_ui.py -k 'completed_issue_updates_agent_profile_once or completed_sprint_persists_retrospective_lineage' -vv`; terminal issue completion persists a `sprint_retrospective` KPI snapshot whose raw data names source/completed issue IDs and assigned-agent lineage; live transition/recovery remains open |

## Licence metadata assertion

The evaluator and steward tests deliberately include missing, restricted, and
unclassified licence metadata. Those values are retained in the report and
provenance record as notices, while technical source/version, security,
sandbox, compatibility, adapter, model, budget, and human gates remain
independent blockers.
The historical `LICENSE_REVIEW` label is also non-blocking; the steward test
suite rejects a direct `LICENSE_REVIEW` → `BLOCKED` transition.

## Carry-forward blockers

- `DEF-2026-07-14-036`: the refreshed local WSL2 negative network matrix passes and is retained at `provenance/network_boundary_live.json`; a post-fix native-Linux negative test is still required for closure.
- The team-runner storage boundary is statically/API-tested; native DNS/TCP/HTTP
  denial and positive control-plane storage evidence are still required to
  close the historical network defect.
- Coding/tester security evidence has been executed but is not clean: the
  exact-source Semgrep report has 316 findings and 54 engine warnings. Both
  manifests remain `findings_review_required` and cannot be activated solely
  because OpenCode interface evidence exists.
- Production images and dependency/runtime records still need immutable digest,
  source, lock, SBOM, and scan reconciliation.
- CEO-vs-human dashboard/API section separation is covered by static/API tests;
  native deployment/network evidence remains open.
- Application image digests, SBOMs, vulnerability scans, and core/extension
  resource measurements remain open: Docker is reachable, but deployment image
  references, native build/scan inputs, and clean measurement evidence are not
  available in this working environment.
- The object-store conformance runner has a retained local deployed MinIO
  result (8/8 scoped cases) and a secret-safe IAM reconciliation helper. The
  R6 exit gate still requires provider-pair/SeaweedFS comparison, encrypted
  backup and clean restore, benchmark, and multi-host storage evidence.
- The refreshed local orchestrator and rebuilt tool service are running the
  current source with database migration head `0036_native_trace_spans`. The
  bounded live trace probe observes one API-request row and one native
  `/health` transport span; the separate pure `time_now` probe observes one
  project-usage row and one native `tool_service` span. Retained evidence is
  [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json)
  plus [`provenance/tool_trace_live.json`](provenance/tool_trace_live.json).
  The host-side tool probe now resolves the Compose-only `tool-service:8002`
  alias to loopback only for a local orchestrator; remote configured URLs are
  left unchanged, and both live trace children pass in the corrected aggregate.
  Representative model-backed worker, identity provider mail-edge/bounce,
  live retention, and native multi-service evidence remain open.

The next ledger revision must be generated from a clean frozen commit and an
environment manifest, then include live, recovery, security, and resource
evidence rather than inheriting these static results.
