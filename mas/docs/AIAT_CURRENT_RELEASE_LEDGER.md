# AIAT Current Release Ledger

**Run date:** 2026-08-12
**Base revision:** `4d7a495aebc2a44a05637f5115596047231d7e5e` (aggregate static run revision)
**Working-tree state:** dirty; this ledger is a P0 progress ledger, not a production release certificate  
**Decision:** **NO-RELEASE / P0 INCOMPLETE**

The latest static aggregation at this revision (2026-08-11T21:16:28Z) is
48/48 checks passing, with two pending technical evidence items and a dirty
working tree; the conservative decision remains **NO-RELEASE**. The configured
live profile is retained separately below. The current unconfigured live
attempt is retained at [`provenance/release_ledger_live_current.json`](provenance/release_ledger_live_current.json)
and is not silently substituted for the configured loopback evidence.

This ledger replaces reliance on the historical July snapshot for the current
implementation pass. It records what was actually run and keeps unavailable
live evidence explicit.

The bounded readiness/declaration groups `5553b19`, `adc7b26`, `d9b1262`,
`569231f`, and `84a1c01` were reviewed after the aggregate snapshot above.
Subsequent focused groups `09bdd19`, `22fc21a`, `234adfb`, `541d6e0`,
`e378f40`, `33e0384`, `52de581`, `f445c17`, `d3482ab`, `5f4b0eb`, and
`823fa6d`, `970f09c`, `85596b0`, `a0faf5b`, `280d363`, `3e8a0ea`, `7541b84`,
and `1761429`, `beabb95`, `46eccee`, `651ad11`, `a260e04`, `2860838`, `380daf5`, and
`fb39128`, `8b7d9f1`, `4d7a495`, `26dc138`, `bc80ad5`, `b5098e7`, `f364763`,
and `cb1c665`, `fcb0f4b`, `7828b48`, `6b0413b`, `140af1c`, `89091c1`, `83e39e6`,
`99a19a2`, `93fdfbc`, `da113af`, `993b1cb`, `d320383`, `826b4c5`,
`8ffb5df`, `f4ae7eb`, `543f392`, `40b87dd`, `32f3a76`, `ed5e551`, `bbd6ba3`, and
`1f947a9`, `c07b4a6`, `d928834`, `50cee61`, `485dfd2`, `24be4ba`, `db898e7`,
`0974434`, `29b700c`, `14968d4`, and `888fde3` are
also recorded
in the rows below with separate documentation commits. The focused
worker-registry policy group `d8cafbb` is recorded below as well; aggregate
check counts and the
release decision are intentionally not recomputed from the dirty working tree.

## Evidence index

| Boundary | Evidence type | Result | Command or record |
|---|---|---|---|
| Governance denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `888fde3`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4151 npx playwright test e2e/governance-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied combined reads hide Refresh/Retry and all executive action forms while preserving only last-known read context; native/live ACL and WCAG evidence remain open. |
| Licence metadata evaluator | unit/regression | PASS | Base implementation `cbdcfa6`; certification/rollout and internal-use enforcement hardening `9b84af3`; `uv run --isolated pytest packages/mas-core/tests/test_worker_governance.py packages/mas-core/tests/test_default_shipped_agents.py apps/orchestrator-api/tests/test_workers_test5_lifecycle.py -q` covers metadata-only evaluator verdicts, non-gating steward certification/rollout checks, passed-security activation, and default manifests. `scripts/check_provenance.py --json` validates source/version inventory fields without a licence allowlist or prohibited-component decision |
| Normal adapter availability | static/unit/worker reconciliation | PASS | Default manifests expose TruffleHog, Plane, OpenProject, and Ansible as selectable adapter metadata without `excluded_default_tools`; base-profile choices remain technical packaging defaults and `check_worker_reconciliation.py --json` stays green |
| Bounded security scanner adapters | static/unit/tool-service | PASS | Contract `fc528a8`; implementation hardening `5b830e9`; tools SDK alias/trace group `965ba38`; `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py packages/mas-tools-sdk/tests/test_smoke.py -q`; `uv run --isolated python scripts/check_security_adapters.py --json`; `security.scan` accepts `semgrep`, `skillspector`, and `trufflehog` compatibility aliases, routes each through the configured sandbox adapter with bounded output, and preserves the same audit/grant/rate/approval boundary |
| Document ingest fallback | static/unit/tool-service | PASS | `uv run --isolated pytest apps/tool-service/tests/test_default_shipped_tool_catalog.py -q`; `document.ingest` invokes Docling when installed and otherwise returns usable source text with `available: true`, `configured: true`, `degraded: true`, and `backend: plain_text_fallback`; this does not claim Docling installation or external adapter certification |
| Optional licence metadata stage | unit/regression | PASS | `test_license_metadata_stage_can_be_skipped` proves `SOURCE_REVIEW` can move directly to `SECURITY_REVIEW`; the compatibility `LICENSE_REVIEW` label is optional metadata and cannot block or delay normal internal use |
| Release environment manifest | static/reproducibility | PASS | `uv run --isolated python scripts/check_release_environment.py --json` emits `aiat.release-environment.v1` with the current revision, thirteen release-input hashes covering operator pins, security evidence, worker manifests, and the prior inputs, tool identities, environment-presence flags, and deterministic digest `865fab3f44a0ab8a846483f71cbb0c42f119ea7f3e2655e108b2af7900c11756` without values or credentials; current WSL evidence records 362 changed paths, 5/6 tools available, and 0 configured optional inputs; dirty state remains a release decision reason |
| Native release-host preflight | static host/deployment prerequisite | BLOCKED (current local WSL2 host) | The opt-in `uv run --isolated python scripts/check_release_environment.py --require-native-linux --json` preflight fails closed unless the host is native Linux, Docker/Compose v2 and `runsc` are available, the tree is clean, and all ten deployment image refs are digest-pinned. The current run is retained at [`provenance/native_release_preflight.json`](provenance/native_release_preflight.json): WSL2, no registered `runsc`, dirty tree, and zero configured immutable image refs. This diagnostic does not replace native network, image/SBOM, scan, recovery, or provider evidence; licence metadata remains non-gating |
| Operator runtime/CLI pin contract | static/source | PASS | Checker/test group `dd857ae`; `uv run --isolated python scripts/check_operator_pins.py --json` verifies nine exact production tool/dependency pins and records seven host/optional/deployment capabilities as explicitly unavailable until exact identities are supplied; this technical check is independent of licence metadata |
| Backend and team-runner regression suite | unit/integration | PASS | `uv run --isolated pytest packages/mas-core/tests apps/orchestrator-api/tests apps/tool-service/tests apps/team-runner/tests -q` |
| Bounded metric labels and project-state metric | static/API integration + local live scrape | PASS (static + local scrape; many-project open) | `541d6e0` adds a bounded compatibility fallback for resume-time project-state reconciliation when a lightweight storage double lacks the state-only query. `uv run --isolated pytest packages/mas-core/tests/test_metric_series_budget.py apps/orchestrator-api/tests/test_metrics.py -q`; `check_metric_series_budget.py --json` emits the complete AIAT label inventory, classifies every label as bounded by a protocol/registry/catalogue/bucket contract, rejects `project_id`, passes the synthetic 10,000-project/2,000-total fixture, and the live parser folds Prometheus' synthetic histogram `_created` sample into its declared family; the refreshed local scrape passes at 31 series with no `project_id` label and is retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json), while native many-project scrape remains open |
| HTTP/message/trace-evidence propagation | static/unit/API + local live transport/tool | PASS (committed core; fresh local read-back; broader sources open) | Core contract `77d5494`; router/agent propagation and cleanup `5bc0aae`; fail-closed live probe group `eac83ae`; tool-service HTTP/usage integration `53d38fc`; API/storage trace writer/read-model wiring `84a1c01`; worker trace/span context persistence `ceb7011`; worker source coverage contract `24c2e35`; focused API/core trace, SLO, and propagation tests pass. The fresh 2026-08-11 configured local probes pass: `/health` observes one API-request row plus one native transport span, and `time_now` observes one project-usage row plus one native tool span. Missing configuration remains explicitly blocked. Selected live model-backed worker, identity provider mail-edge, and live retention remain separate evidence boundaries |
| API observation ledger | static/unit/API/fixture | PASS (bounded; API route/storage wiring reviewed) | Core contract `9c39919`; integration group `84a1c01`; focused API observation, trace propagation, trace evidence, and SLO/capacity tests pass; `check_api_observability.py --json` remains the payload-free fixture gate. Migrations `0034_api_request_observations`, `0035_trace_correlation_evidence`, and `0036_native_trace_spans` are checked in, and the orchestrator middleware now records normalized observations with trace headers and operator read-back |
| Secret-safe system diagnostics | static/unit/API | PASS | Commit `2860838`; `uv run --isolated pytest apps/orchestrator-api/tests/test_system.py apps/orchestrator-api/tests/test_test10_ops_scripts.py -q` covers database, router, tool-service, optional object-store, degraded aggregation, no-storage 503, and dependency-payload redaction. `GET /system/diagnostics` is read-only, returns bounded status/latency/connection facts or exception type, and never returns credentials, URLs, dependency payloads, or raw error text |
| Operator control CLI | static/unit | PASS | Commits `380daf5` and `f8df50e`; `uv run --isolated pytest scripts/tests/test_mas_ctl.py -q` passes six deterministic cases, and the focused operational API suite verifies the executable `scripts/mas-ctl` wrapper. `bootstrap` requires healthy `/health` plus `ok` diagnostics; HTTP error bodies are suppressed and `resume`/`shutdown` remain explicit authenticated POST commands |
| Communication-policy sender identity | static/unit/mocked router | PASS | Commit `fb39128`; focused policy, message-router, publish-auth, and orchestrator communication-policy tests cover sender role/team coherence, spoofed worker-to-CEO/admin paths, role-specific message types, and HTTP 403 before dedupe/enqueue. Live external-router and dashboard hierarchy evidence remain separate |
| Hierarchy communication-policy overlay | source type/lint/build + focused live E2E | PASS (focused local live image; release image open) | Implementation `8b7d9f1`; evidence-test wording cleanup `3dc61ad`; selector hardening `d5f596e`; fail-closed staged-context handling `45ee42c`; dashboard `npm run typecheck`, focused ESLint, and `npm run build` pass. `npm run test:e2e -- --workers=1 --grep "system visualization exposes hierarchy"` passes 1/1 against a current `mas/dashboard:overlay` image built from a clean explicit context. The `mas.sh` wrapper excludes all disposable `.tmp*` paths and rejects incomplete staging; direct unwrapped WSL Docker-context and native/release-image evidence remain separate, and the API-only hierarchy suites retain two explicit live-evidence skips |
| Native trace-span contract | static/unit/fixture + local live transport/tool | PASS (committed core; local read-back; model/provider open) | Core contract `77d5494`; tool-service native span persistence `53d38fc`; API/storage native-span writers and trace readers `84a1c01`; worker artifact/usage trace context and payload-free storage assertions `ceb7011`; source-category coverage `24c2e35`; focused trace/SLO tests pass and scalar allow-list/redaction remains enforced. Provider mail-edge/live-retention and selected representative model-backed worker coverage remain open |
| Worker trace source coverage | static/unit/fixture + explicit live boundary | PASS (fixture; live dispatch blocked without operator selection) | Commit `24c2e35`; `uv run --isolated pytest packages/mas-core/tests/test_worker_trace_coverage.py -q` and `uv run --isolated python scripts/check_worker_trace_coverage.py --json --require-integration` require model-usage, worker-artifact, native model, native worker, and optional integration source categories. Read-only live mode requires a selected trace; dispatch requires an active model-backed worker/project/profile and `--confirm-dispatch`, emits only counts/status metadata, and does not auto-select or activate a worker. No live worker-run, mail-edge, retention, sandbox, canary, or rollback evidence is claimed; licence metadata remains informational only |
| Trace retention planner | static/unit/fixture | PASS (non-mutating; live open) | `uv run --isolated pytest packages/mas-core/tests/test_trace_retention.py -q`; `uv run --isolated python scripts/check_trace_retention.py --json` passes deterministic `aiat.trace-retention-plan.v1` retain/archive/delete/invalid classification; no database mutation, live deletion/archive, project narrowing, or restore parity is claimed |
| Signed identity mail SLO/trace projection | static/unit/client/API projection | PASS (bounded optional source) | Commit `f577675`; `PYTHONPATH=apps/identity-service uv run --isolated pytest apps/identity-service/tests/test_identity_service.py -q` and `uv run --isolated pytest apps/orchestrator-api/tests/test_identity_reconciliation.py apps/orchestrator-api/tests/test_slo_capacity.py -q`; identity migration `0002_mail_trace_correlation` persists safe delivery-attempt trace/span IDs, while signed `/v1/dashboard/mail-relay` rows are reduced to outcome/time/trace metadata only and filtered to the requested window; absent identity configuration remains explicit `mail_delivery: no_data` |
| SLO/capacity operational read models | static/unit/API/fixture + local transport evidence | PASS (bounded; API/storage projections integrated; native model/tool/mail evidence open) | Core contract `14b4e4b`; API/storage integration `84a1c01`; focused SLO/capacity, trace, and API contract checks pass; `check_slo_capacity.py --json` remains the deterministic fixture gate and the retained local trace transport evidence is [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json). `aiat.slo-policy.v1`, `aiat.slo-report.v1`, and `aiat.capacity-forecast.v1` cover descriptive API/queue/worker/tool/model/PM/SCM/mail/recovery targets, durable usage cost/token forecasts, confidence, and budget headroom; signed identity mail-attempt, PM/SCM delivery, and worker-recovery projections are bounded and payload-free, while absent native model/tool/mail-edge/full-span sources remain `no_data`/`insufficient_data` and do not block execution |
| LiteLLM/OmniRoute monitoring adapter | unit/fixture | PASS (non-networking) | Commit `525a94b`; `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py -k monitoring_adapter -q`; the configured adapter emits `aiat.monitoring-analytics-plan.v1` endpoint/synthetic-check metadata, rejects credentials embedded in analytics URLs, and never probes or mutates services; Prometheus-compatible output remains optional |
| Third-party inventory policy | static validation | PASS | Implementation `cbdcfa6`; `uv run --isolated python scripts/check_provenance.py` validates 21 components, including the operator-supplied SkillSpector metadata record, while licence/restriction values remain catalogue notices only |
| Documentation authority/index | static validation | PASS | `uv run --isolated python scripts/check_docs_index.py --json`; the canonical target, eleven current feature documents, three ordered plans, maintained links, roadmap references, and metadata-only policy markers reconcile |
| Changed Python paths | static validation | PASS | isolated `compileall` for worker registry, orchestrator API, dashboard policy, image-contract, and provenance paths |
| CEO/service/dashboard ACL | API security | PASS | Commits `d405ccb` and `e9b4da4`; `uv run --isolated pytest apps/orchestrator-api/tests/test_auth_boundary.py packages/mas-core/tests/test_dashboard_access.py -q` covers section authorization, operator-only persistence, deny-by-default principal/section combinations, and recovery invariants |
| Team-runner storage/network boundary | static + API/client contract + local live matrix | PASS (static/API + refreshed WSL2 live) / BLOCKED (native release host) | Boundary base `43bee16`; typed storage-client and fail-closed identity hardening `22fc21a`; the secret-safe WSL2 matrix evidence was refreshed 2026-08-11 in `9bee6bb`; `uv run --isolated pytest apps/orchestrator-api/tests/test_team_runner_storage_boundary.py apps/team-runner/tests/test_storage_client.py apps/orchestrator-api/tests/test_gamma_hardening.py packages/mas-core/tests/test_network_boundary.py -q`; `uv run --isolated python scripts/check_network_boundary.py --json` and `--live --json` pass all 11 current WSL2 runners with gateway allow-list, protected-service/egress denial, storage-health read-back, no forbidden runner environment names, and no Docker sockets; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json) |
| Object-store contract fixture/runner | static/unit + local deployed live run | PASS (fixture + refreshed reconciled local MinIO live; aggregate private-network child pass; external open) | IAM reconciliation helper `5558f3c`; the secret-safe local MinIO evidence was refreshed 2026-08-11 in `22c736d`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_conformance.py packages/mas-core/tests/test_phase7_storage.py -q`; `bash infra/compose/scripts/check-minio-conformance.sh` and the deterministic fixture both pass 8/8 scoped cases after the credential-safe persisted IAM reconciliation; `check_object_store_conformance.py --live --json --compose-local` now invokes the same probe inside the orchestrator container, so the release child no longer times out on the private `minio:9000` alias; secret-safe evidence is retained at [`provenance/object_store_live_conformance.json`](provenance/object_store_live_conformance.json), with the repair helper at [`infra/compose/scripts/reconcile-minio-agent-user.sh`](../infra/compose/scripts/reconcile-minio-agent-user.sh); provider-pair/SeaweedFS, encrypted backup, retention, benchmark, and multi-host evidence remain open |
| Object-store benchmark contract | static/unit + explicit two-provider live boundary | PASS (fixture; live blocked) | `aiat.object-store-benchmark.v1` implementation and CLI are covered by `uv run --isolated pytest packages/mas-core/tests/test_object_store_benchmarks.py -q` (4 passed); `uv run --isolated python scripts/check_object_store_benchmarks.py --json --payload-size 32` passes the deterministic fixture, while `--live --json` exits 2 until both MinIO and SeaweedFS endpoint/credential sets are supplied. Each run verifies checksum read-back and scoped cleanup, emits no credentials, and records `operator_review_required` rather than selecting a provider; reliability/resource/concurrency/large-object/multipart/outage/recovery comparison remains open |
| Object-store copy/parity fixture | static/unit | PASS (fixture only) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_migration.py -q` and `uv run --isolated python scripts/check_object_store_copy.py --json`; `aiat.object-store-copy.v1` copies explicit project-scoped refs, verifies source/target checksum and size after read-back, preserves source data, and cleans failed target copies; provider-pair migration, cutover, backup, restore, and benchmark evidence remain open |
| Object-store live copy/parity runner | live-runner readiness | PASS (live blocked) | `uv run --isolated python scripts/check_object_store_copy.py --live --json` returns exit 2 with `status: blocked` when source/target configuration is absent; the runner inventories source bytes into checksum-bearing refs, refuses empty inventory, and performs non-destructive target read-back parity when configured; provider-pair evidence, cutover, backup, restore, and benchmark remain open |
| Object-store migration workflow | static/unit/fixture; guarded live boundary | PASS (fixture; live blocked) | `uv run --isolated pytest packages/mas-core/tests/test_object_store_rollout.py packages/mas-core/tests/test_object_store_migration_runner.py -q`; `uv run --isolated python scripts/check_object_store_migration.py --json` proves `aiat.object-store-migration.v1` inventory → verified copy → optional dual-write → human-confirmed cutover → human-confirmed rollback, while `--live --json` returns exit 2 without provider-specific routing/retention/rollback evidence |
| Dashboard ACL policy | unit/contract | PASS | Core policy/test group `d405ccb`; enforcement/proxy group `e9b4da4`; `uv run --isolated pytest packages/mas-core/tests/test_dashboard_access.py apps/orchestrator-api/tests/test_auth_boundary.py -q` covers finite sections, deny-by-default unknowns, deterministic persistence, operator recovery invariants, and authenticated principal separation |
| Dashboard operator proxy/type contract | static/typecheck | PASS | Commits `e9b4da4` and `fc4f0fa`; credential and identity proxies use the strict operator key plus bounded section context, and the project-evidence error-state repair restores the workspace `npm run typecheck` contract |
| Dashboard local Compose E2E matrix | authenticated local UI | PASS WITH EXPLICIT SKIP (58/59) | `e378f40` hardens the credential and tools operation selectors against accessible-name/strict-mode drift; `d5f596e` uses the overlay accessible name and explicit trace-control IDs; `514aeeb` keeps hiring evaluation details expanded after evaluation; `npm run test:e2e -- --workers=1` in `apps/mas-dashboard` passes 58/59. The WSL2 Compose run covers operational UI, hierarchy communication-policy/path tracing, CEO chat/hiring, project workspace, schema-driven flow editing, all eight branching/recovery scenarios, hiring-board evaluation details, identity stale-record/retry state, PM integration conflict/stale retry, project-detail stale/retry state, runtime-status panels, shell skip-link/mobile focus recovery, evidence-detail scalar redaction, theme preference, and all stale/recovery read surfaces. Secret-safe evidence is [`provenance/dashboard_e2e_live.json`](provenance/dashboard_e2e_live.json); the live DLQ replay case is intentionally skipped pending an operator-owned safe fixture, and native-Linux/mobile/WCAG evidence remains open |
| Identity-resource stale/retry and table accessibility | static/build/source-built Playwright (preparatory P1) | PASS | Commits `46eccee`, `651ad11`, `a260e04`; `npm run typecheck`, focused ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4142 npx playwright test identity-states.spec.ts --workers=1 --reporter=line` pass (focused browser coverage 1/1). The shared `IdentityResourcePage` loader aborts obsolete requests, ignores late responses by generation, retains rows and the warning while retrying, and clears stale state after recovered data arrives; the table exposes a caption, scoped column headers, explicit row-action names, and 44px action targets without rendering sensitive fields, while the page exposes named main/status/metadata/table regions, explicit busy state, and decorative-icon suppression. Provider/live identity evidence remains open. |
| External-account action policy | static/API/unit | PASS | Commit `f577675`; `PYTHONPATH=apps/identity-service uv run --isolated pytest apps/identity-service/tests/test_identity_service.py -q`; `aiat.external-account-action-policy.v1` exposes category-sensitive signup, rotation/closure human gates, immediate suspension, and governed browser-session rules |
| Production image contract | static + fail-closed live identity | PASS (static + SBOM schema; live blocked) | Contracts `7d69fbd`, `1d373ee`, and `42b03a3`; `uv run --isolated pytest packages/mas-core/tests/test_image_provenance_runner.py -q` (6 passed) and `uv run --isolated python scripts/check_image_provenance.py --json` pass the immutable Compose/Dockerfile contract and verify the checked-in `production-image-lock.example.env` covers every application image input. `--require-sbom` now validates CycloneDX format/version, metadata component, named components, and unique `bom-ref` values without inspecting licence metadata; `--live --require-sbom --json` returns exit 2 because deployment-supplied immutable `*_IMAGE_REF` values and release artifacts are absent. Development-only wrapper defaults are isolated in `b9a77e9` |
| Local runtime identity/release wrapper | static Compose/shell/Docker validation | PASS | Wrapper hardening `fd41874`; runner prompt timezone implementation `c955ac8`; `mas/infra/compose/mas.sh validate`, `check_image_provenance.py --json`, and `check_operator_pins.py --json` pass. The wrapper supplies only development defaults, requires distinct CEO/worker keys, propagates the company IANA timezone into runner prompts, pins its `uv` image bootstrap, and documents identity migration head `0002_mail_trace_correlation`; direct production Compose still requires populated immutable image refs |
| Worker manifest/runtime/provenance reconciliation | static + read-only authenticated local live binding + scan-evidence linkage | PASS (39/39 persisted defaults; technical findings remain open) | Runtime catalogue/checker/test group `80e0ca3`; the secret-safe local live evidence was refreshed 2026-08-11 in `180f9e0`; `uv run --isolated python scripts/check_worker_reconciliation.py --json` plus authenticated local `--live --json`; all 39 checked-in defaults match persisted `/capabilities/workers` rows with zero missing rows or binding mismatches. Secret-safe evidence is [`provenance/worker_reconciliation_live.json`](provenance/worker_reconciliation_live.json). Coding/tester rows link to `security_scan_evidence.yaml`, which records 316 Semgrep findings and 54 engine warnings, so both remain non-activatable; host package availability is advisory and Compose runtime imports are recorded separately |
| Worker registry grant/update policy | static/API fixture | PASS (66 focused cases; adjacent suites pass) | Commit `d8cafbb`; `uv run --isolated pytest apps/orchestrator-api/tests/test_workers_test4_config.py -q` covers the four accepted update-policy values, invalid-policy `422` responses, direct/persisted forbidden-tool denial, and no-write-on-denial behavior. Capability/team authority changes revalidate persisted `required_tools` against the canonical tool policy before storage mutation; licence/restriction metadata remains informational only |
| Default worker implementation bindings | static contract + explicit live boundary | PASS (static; live blocked by operator environment) | `uv run --isolated pytest packages/mas-core/tests/test_default_worker_bindings.py -q`; `uv run --isolated python scripts/check_default_worker_bindings.py --json` reconciles all 15 documented default worker slots across department, runtime, transport, isolation, runtime-catalogue support, runtime/integration adapter entrypoints, capability, adapter configuration, and required tools. `--live --json` is fail-closed and returns exit 2 until an operator selects a live environment; the report never treats licence metadata as a gate |
| Worker-run lifecycle contract | deterministic controller fixture + explicit live boundary | PASS (fixture; live blocked without operator-selected run) | Commit `fe6fb8d`; `uv run --isolated pytest packages/mas-core/tests/test_worker_run_lifecycle.py -q`; `uv run --isolated python scripts/check_worker_run_lifecycle.py --json` drives the real `WorkerRunController`/`NativeWorkerAdapter` through checkpoint persistence, pause/resume, cold cancellation, cold-crash failure normalization, lease-expiry requeue, and artifact/usage-before-terminal ordering; `--live --json` returns exit 2 without mutating a live run, while database/sandbox/canary/rollback evidence remains open |
| Selected worker-run readiness preflight | fixture + authenticated read-only live diagnostic | PASS (fixture); BLOCKED (current selected live state) | Commit `5553b19`; `uv run --isolated pytest packages/mas-core/tests/test_worker_run_readiness.py -q` and `uv run --isolated python scripts/check_worker_run_readiness.py --json` pass the complete snapshot fixture. The live checker requires explicit worker/project UUIDs, reads only bounded worker/project/company/assignment/budget/profile/health records, and never auto-selects, activates, provisions identity, reserves budget, dispatches, or returns payloads. The current local coding-worker/terminal-project selection exits 2 with inactive worker, missing immutable pointers, terminal project, and missing assignment; identity, sandbox runtime, canary, live-run, rollback, and licence metadata remain separate/not-gated |
| Selected steward certification readiness preflight | fixture + authenticated read-only live diagnostic | PASS (fixture); BLOCKED (current selected live state) | Commit `adc7b26`; `uv run --isolated pytest packages/mas-core/tests/test_worker_steward_readiness.py -q` and `uv run --isolated python scripts/check_worker_steward_readiness.py --json` pass the complete candidate fixture. The live checker requires explicit worker/candidate UUIDs, reads only bounded worker/steward/candidate records, and never generates or certifies a candidate, approves, activates, rolls out, dispatches, or returns payloads. The current local coding-worker selection exits 2 with `steward_not_ready`, `security_scan_not_passed`, and `candidate_not_found`; licence metadata remains separate/not-gated |
| Team-runner manifest identity bindings | static/unit | PASS | Commit `d9b1262`; `uv run --isolated pytest packages/mas-core/tests/test_team_worker_manifest_refs.py apps/team-runner/tests/test_team_config.py -q` and `uv run --isolated python scripts/check_team_worker_manifest_refs.py --json` reconcile 11 team files and 39 exact agent→manifest IDs; no registration/activation mutation and licence metadata remains informational |
| Team-runner startup manifest enforcement | unit/startup contract | PASS | Commit `569231f`; `uv run --isolated pytest apps/team-runner/tests/test_team_config.py apps/team-runner/tests/test_shutdown.py -q` verifies mounted-manifest reconciliation, fail-closed missing references, exact `AgentConfig` propagation, and health metadata; startup remains read-only and does not register or activate workers |
| Exact OpenCode security scan | external source + Semgrep evidence | FINDINGS_REVIEW_REQUIRED | Exact commit `10c894bdeef3618f5666fb506ef7f9491bb964d8` (`v1.17.13`) scanned with Semgrep `1.168.0`; 5,062 files, 316 findings, 19 `ERROR`, 54 engine warnings; evidence digest `e960a57d7b397d8af9271025b2025b0f46567967b4c684eaab8652a75d47d3b5` |
| Worker certification matrix | deterministic static/unit declaration | PASS | Commit `a62ddb7` (generator group `4c5fd68`); `uv run --isolated pytest packages/mas-core/tests/test_worker_certification_matrix.py -q` and `uv run --isolated python scripts/generate_worker_certification_matrix.py --check`; 39 rows separate declared runtime/adapter inputs from pending security, canary, and live evidence; the regression checks generated-artifact parity, exact manifest coverage, and metadata-only licence handling |
| Microsoft Agent Framework adapter boundary | unit/fixture | PASS (compatibility boundary; activation open) | Commit `fc528a8`; `uv run --isolated pytest packages/mas-core/tests/test_microsoft_agent_framework_adapter.py -q`; fake-module coverage proves bounded Agent/ChatAgent construction, async run/invoke translation, shutdown, and missing package/instructions fail-closed paths; locked package/MCP compatibility and activation are tracked separately |
| Microsoft Agent Framework/MCP compatibility lock | static contract + preflight | PASS (activation blocked) | Commit `fc528a8`; `uv run --isolated pytest packages/mas-core/tests/test_microsoft_agent_framework_compatibility.py packages/mas-core/tests/test_microsoft_agent_framework_adapter.py -q`; `uv run --isolated python scripts/check_runtime_compatibility.py --json` records `agent-framework==1.13.0`, MCP `>=1.27,<2`, current workspace MCP `1.23.3`, and missing optional MAF as explicit blockers; no installation or network mutation occurs |
| Code-review adapter catalogue/default | static/unit/fixture | PASS (external candidates blocked) | Catalogue `fc528a8`; implementation `5b830e9`; `uv run --isolated pytest apps/tool-service/tests/test_real_adapter_backends.py apps/tool-service/tests/test_default_shipped_tool_catalog.py apps/tool-service/tests/test_review_tool_contract.py -q`; `uv run --isolated python scripts/check_code_review_adapters.py --json` proves the local deterministic diff reviewer is the default and generic external candidates cannot activate without exact source/revision/version evidence. Review submissions preserve structured findings and publish canonical `REVIEW_RESPONSE` envelopes; the bind-mounted project Git initializer uses inline identity flags and avoids config-lock chmod assumptions |
| Worker runtime readiness | static + live import probe | PASS (local Compose image imports; certification remains open) | Commit `4c5fd68`; `uv run --isolated pytest packages/mas-core/tests/test_worker_runtime_readiness.py -q`; `uv run --isolated python scripts/check_worker_runtime_readiness.py --json` reports 39 manifests without certifying them; the aggregate `--live --json --compose-local` probe imports required LangGraph/CrewAI packages inside `mas-orchestrator-api-1` and passes with no required runtime blockers; secret-safe evidence is retained at [`provenance/worker_runtime_readiness_live.json`](provenance/worker_runtime_readiness_live.json), while optional MAF/AutoGen/Letta imports and security, sandbox, canary, live-run, and rollback checks remain separate |
| Default runtime install profile | static source/lock/Dockerfile contract | PASS | Commits `9a10a4b` and tracked workspace lock `2b13d89`; `uv lock --check` and `uv run --isolated python scripts/check_runtime_install_profile.py --json` reconcile the `runtime-default` LangGraph/CrewAI extra, locked versions (`langgraph` 0.6.11 and `crewai` 1.6.1), runtime-catalogue imports, and production Dockerfile install command; package imports and worker certification remain separate |
| Workspace lock reproducibility | clean-archive/static packaging | PASS | Tracked lock commit `2b13d89`; `uv sync --locked --dev --dry-run` resolves 351 packages and would install the declared workspace without lock drift, while a clean Git archive contains `mas/uv.lock` and passes `check_docs_index.py` |
| Default worker steward lifecycle | deterministic domain fixture + restart-safe persistence path | PASS (domain evidence; live open) | Existing steward contract `c80e339`, fixture coverage test `fe6fb8d`, durable compatibility-matrix writer/reader and bounded evidence test `ceb7011`; `uv run --isolated python scripts/check_worker_steward_contract.py --json` plus `uv run --isolated pytest packages/mas-core/tests/test_worker_steward_contract.py apps/orchestrator-api/tests/test_steward_rehydration.py packages/mas-core/tests/test_compatibility_matrix_persistence.py -q` exercises dedicated steward, immutable candidate, compatibility matrix, certification/approval, shadow → read-only canary → promotion, regression blocking, and pre-activation rollback for both externally sourced default workers; certification records the matrix in the same-process steward cache and durable store, while API rehydration restores durable active bundle/adapter IDs plus persisted compatibility-matrix rows, normalizing profile/capability JSON and failing closed on malformed evidence. Evidence is [`provenance/worker_steward_contract.json`](provenance/worker_steward_contract.json). Database reconciliation, security, sandbox, live canary, and worker-run evidence remain open |
| Sandbox runtime readiness | static + live Docker runtime probe | PASS (live blocked) | Commit `a24c554`; `uv run --isolated pytest packages/mas-core/tests/test_sandbox_runtime_readiness.py -q`; `uv run --isolated python scripts/check_sandbox_runtime_readiness.py --json` reconciles 39 manifests and 10 hardened external workers; the Docker CLI is reachable but `--live --json` returns exit 2 because the required gVisor `runsc` runtime is not registered and no `runc` fallback is permitted, while digest-pinned smoke/network, canary, and Firecracker checks remain separate |
| Runtime benchmark readiness | read-only live API probe | PASS (fresh local dependency dry-run; certification boundary remains open) | Base contract `ad31793`; bounded endpoint/test hardening `4d61279`; the secret-safe authenticated local run was refreshed 2026-08-11 in `3f15e28`; `uv run --isolated pytest packages/mas-core/tests/test_runtime_benchmarks.py apps/orchestrator-api/tests/test_epsilon_runtimes.py -q` passes. Third-party imports execute off the API event loop with a capped timeout and explicit `benchmark_timeout`/`benchmark_error` responses; the local `--live --json` probe passes deterministic LangGraph/CrewAI dependency dry-runs and is retained at [`runtime_benchmarks_live.json`](provenance/runtime_benchmarks_live.json). Missing API/package/validation evidence remains exit 2 when encountered; package benchmarks never certify a worker canary/live run/rollback |
| Default runtime adapter conformance | unit/fixture + Compose package/lifecycle and lock-parity probe | PASS (adapter and exact-lock evidence; worker certification open) | Commit `9a10a4b`; `uv run --isolated pytest packages/mas-core/tests/test_runtime_adapter_conformance.py -q`; `docker exec mas-orchestrator-api-1 python /app/scripts/check_runtime_adapter_conformance.py --live --json` passes actual `LangGraphAdapter` and `CrewAIAdapter` classes with locked LangGraph `0.6.11` and CrewAI `1.6.1`, verifying manifest/message translation, bounded completion, health, and shutdown without model/tool/provider/project calls; secret-safe evidence is [`provenance/runtime_adapter_conformance_live.json`](provenance/runtime_adapter_conformance_live.json). Sandbox, canary, live worker-run, and rollback evidence remain open |
| Model override expiry and budget settlement replay | focused unit/storage | PASS (preparatory P1) | Commit `63b2db5`; `PYTHONPATH=mas/packages/mas-api-sdk ./.venv/bin/pytest -q apps/orchestrator-api/tests/test_worker_dispatch_governance.py packages/mas-core/tests/test_phase7_storage.py -k 'model_override_expiry or settle_budget_reservation'`; serialized expiry fails closed and terminal settlement retries are no-ops; provider-specific live recovery and settlement chaos evidence remain open |
| LLM gateway transient failure vocabulary | static/unit (preparatory P1) | PASS | Commit `288996e`; `uv run --isolated pytest packages/mas-core/tests/test_gateway_providers.py -q`; normal/streaming/fallback gateway paths share explicit 408/409/412/425/429/5xx retry classification while permanent client 4xx responses remain non-retryable |
| Model/provider cooldown failover | static/unit/persistence (preparatory P1) | PASS | Commit `288996e`; `uv run --isolated pytest packages/mas-core/tests/test_observability.py packages/mas-core/tests/test_persistence.py packages/mas-core/tests/test_gateway_providers.py -q`; bounded model cooldowns, two-failure provider cooldowns, persisted state, SmartRouter exclusion, and automatic fallback filtering pass; provider-specific live outage/recovery and chaos evidence remain open |
| Model profile/runtime catalogue | static/unit/API/dashboard + local live read-only reconciliation (preparatory P1) | PASS WITH FINDINGS (approved coverage present; reconciliation pending) | Commits `288996e`, `09bdd19`, and `ab0a0fe`; the secret-safe local read-only evidence was refreshed 2026-08-11 in `3e111ac`; `uv run --isolated pytest packages/mas-core/tests/test_model_catalogue.py packages/mas-core/tests/test_model_profile_catalogue_runner.py packages/mas-core/tests/test_model_profile_seeder.py apps/orchestrator-api/tests/test_model_profile_catalogue.py -q`; `uv run --isolated python scripts/check_model_profile_catalogue.py --json`; `--live --json --require-approved` observes 93 registry models, 94 persisted profile versions, 92 approved covered entries, one pending registry model, and two non-registered profile findings; secret-safe evidence is retained at [`provenance/model_profile_catalogue_live.json`](provenance/model_profile_catalogue_live.json). The API/dashboard export and operator proxy remain deterministic; stale/extra profile reconciliation and provider recovery remain open |
| Default model-profile bootstrap | static/unit/API seed path + live persisted evidence | PASS WITH FINDINGS | Commit `09bdd19` (building on `288996e`); `uv run --isolated pytest packages/mas-core/tests/test_model_profile_seeder.py -q`; startup and `/system/seed-default-company` invoke the idempotent `aiat.model-profile-bootstrap.v1` declaration for all 93 registry models plus `opencode-phase0b-coding`/`omniroute-coding`, preserve operator rows, and block conflicts; the live catalogue now confirms 94 persisted versions and 92 approved registry bindings, while one pending model and two non-registered rows remain explicit findings |
| Model profile live verifier | fail-closed runner | PASS (local live; findings retained) | Commit `288996e`; `uv run --isolated python scripts/check_model_profile_catalogue.py --live --json --require-approved` now passes the approval gate against the local API and reports `pass_with_profile_findings`; missing API/auth still exits 2, while the retained report records 92 approved covered entries, one pending model, and two non-registered findings without exposing profile payloads |
| Executive reconciliation and role views | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | Core model/checker `be030ac`; API/dashboard integration `d1b8839`; `uv run --isolated pytest packages/mas-core/tests/test_executive_reconciliation.py packages/mas-core/tests/test_executive_reconciliation_runner.py apps/orchestrator-api/tests/test_model_profile_catalogue.py -q`; deterministic `aiat.executive-reconciliation.v1` aggregates durable project/usage/worker-run/budget/model evidence, `aiat.executive-views.v1` projects bounded CFO/CTO/CEO summaries, and `scripts/check_executive_reconciliation.py --live --json` provides a secret-safe bounded coverage/finding verifier with optional `--require-clean`, returning exit 2 when live API/DB evidence is unavailable. API/dashboard routes, reservation/settlement wiring, and live DB population/provider recovery remain separate integrations |
| Executive write action envelope | static/unit/API/dashboard (preparatory P1) | PASS (bounded surface) | API/dashboard integration `d1b8839`; `uv run --isolated pytest apps/orchestrator-api/tests/test_executive_actions.py apps/orchestrator-api/tests/test_api_contract.py -q`; `aiat.executive-action.v1` routes CFO model overrides through the durable request owner, CTO worker runs through canonical governed dispatch, and CEO privileged actions through the audited gate; worker output/payload secrets are excluded from responses; dashboard proxies and the typed confirmation panel cover all three routes; local profile evidence is partial (92/94 approved covered with one pending and two non-registered findings), while provider/live recovery, broader governance forms, and chaos evidence remain open |
| Guarded self-improvement lifecycle | static/unit/storage/API fixture; explicit live boundary | PASS (fixture/storage/API; live blocked without worker/control-plane integration) | Core contract/fixture commit `4d8dddf`; `uv run --isolated pytest packages/mas-core/tests/test_self_improvement.py -q`; `uv run --isolated python scripts/check_self_improvement_lifecycle.py --json` proves `aiat.self-improvement.v1` owner/risk/budget/evidence project request, independent technical gates, human-only promotion, shadow/canary, exact rollback, bounded terminal outcome/cost/incident/rollback/KPI-learning persistence with idempotent IDs, and frozen five-kind `aiat.self-improvement-artifacts.v1` manifest generated from normalized worker records with canonical artifact-row IDs plus SHA-256/size read-back parity for all five artifacts; authenticated API/storage integration remains a separate review group; `--live --json` returns exit 2 until live worker/control-plane integration is configured; external certified-worker/provider and live issue/worker-run/branch/artifact/budget/deployment evidence remain open |
| Self-improvement candidate detection | static/unit/fixture; live signal boundary | PASS (fixture; live blocked) | Core contract/fixture commit `4d8dddf`; `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` proves `aiat.self-improvement-candidate-detection.v1` normalization for defect/metric/upstream-update/cost/operator-goal signals, exact duplicate collapse, conflicting-ID rejection, deterministic risk/budget mapping, secret-safe metadata, and zero project/budget/credential/deployment side effects; `--live` remains blocked until operator-selected signal sources exist |
| CEO evidence envelope and citation route | static/unit/API/dashboard (preparatory P1) | PASS (bounded deterministic + fallback citation surface) | Commit `f1801bb`; `2ca5f3d` adds operator-authenticated artifact/usage detail authorities and `6c52552` retains the last safe projection through refresh failure; `uv run --isolated pytest apps/orchestrator-api/tests/test_ceo_chat.py apps/orchestrator-api/tests/test_evidence_detail_routes.py -q` and `check_api_contract.py --json` pass. API-owned CEO actions/read responses publish and return `aiat.ceo-evidence.v1` with bounded scalar references and traces, never arbitrary payloads or secret values; the legacy fallback accepts only explicit stripped `AIAT_EVIDENCE: kind=id` markers and labels them `unverified`; dashboard renders owning-section links for project/flow/governance/worker/credential/integration/tool/project-evidence/log surfaces plus dedicated `/evidence/{kind}/{id}` records. The bounded `aiat.evidence-detail.v1` projection now covers fourteen scalar citation kinds through `8fefc8b`/`c8505eb`/`dc50719`/`5357166`/`ec8cf67`/`2ca5f3d`; workspace typecheck/build/lint pass, focused source-built coverage is 9/9, stale-refresh recovery retains safe detail and the canonical link, while broader stale/offline recovery and full golden paths remain open |
| Compose network denial/allow matrix | live integration | PASS (refreshed local WSL2 matrix); BLOCKED (native release host) | The `43bee16` Compose boundary was recreated without removing volumes. `check_network_boundary.py --live --json` passes all 11 runners with named gateway access, protected-service and unapproved-egress denial, control-plane storage health, no forbidden runner environment names, and no Docker sockets; secret-safe evidence is [`provenance/network_boundary_live.json`](provenance/network_boundary_live.json). Native-Linux recreation, provisioned-key dashboard ACL coverage, and post-fix closure of `DEF-2026-07-14-036` remain open. |
| CEO service identity and section ACL | static + API security | PASS (static/API + authenticated local matrix); native UI/release matrix open | Commits `d405ccb` and `e9b4da4`; distinct CEO/worker principals, persisted `system_config` ACL, operator-only update, fail-closed dashboard section middleware, strict operator-key proxies, and human/CEO/service/worker matrix pass. The refreshed local endpoint matrix is retained at [`provenance/dashboard_acl_live.json`](provenance/dashboard_acl_live.json); native-Linux deployment/UI denial evidence remains open |
| Production image digest inventory | supply-chain/release | PASS (static contract + SBOM schema); live blocked/open | `1d373ee` adds the non-secret `infra/compose/production-image-lock.example.env` template and coverage regression; `42b03a3` makes `check_image_provenance.py --require-sbom` reject malformed CycloneDX artifacts; `docs/provenance/production_images.yaml` matches Compose image variables; fixed infrastructure refs and Dockerfile bases carry digests; application/gateway refs require deployment-supplied immutable `*_IMAGE_REF` values; `check_image_provenance.py --live --require-sbom --json` remains fail-closed without deployment refs/artifacts; development `mas.sh` fallbacks are isolated to the dev overlay; clean pull/source/lock/scan reconciliation remains open |
| Object-store backup/restore manifest | static/unit fixture + local same-provider live rehearsal | PASS (fixture + refreshed local MinIO rehearsal; provider-diverse open) | Restore safety group `93bf755`; the secret-safe local rehearsal was refreshed 2026-08-11 in `22c736d`; `uv run --isolated pytest packages/mas-core/tests/test_object_store_backup.py -q` includes a non-empty-target regression; `uv run --isolated python scripts/check_object_store_backup_restore.py --json` proves `aiat.object-store-backup.v1` source → backup → empty-target preflight → clean-target restore with exact key/checksum parity and `clean_target_verified: true`; `bash infra/compose/scripts/check-minio-backup-restore.sh` passes two disposable objects with manifest/read-back parity and scoped cleanup, retained at [`provenance/object_store_backup_restore_live.json`](provenance/object_store_backup_restore_live.json); the explicit three-provider `--live --json` runner still blocks without separate providers, encryption, retention, provider durability, and clean-environment disaster-recovery evidence |
| Metric-series budget | static + local live scrape | PASS (static + refreshed local scrape; native many-project open) | `541d6e0` preserves bounded resume-time reconciliation for legacy storage doubles; the secret-safe local scrape evidence was refreshed 2026-08-11 in `76a32c0`; `uv run --isolated python scripts/check_metric_series_budget.py --json` exercises 10,000 synthetic projects with 2,000-total/32-project-state budgets and no `project_id` label; `--live --json` passes the refreshed local orchestrator scrape at 31 bounded series after normalizing the histogram `_created` sample, retained at [`provenance/metric_series_live.json`](provenance/metric_series_live.json); native many-project scrape evidence is still open |
| Tool-service image budget and dependency profile | static contract + local build/resource probe | PASS (static + local Linux probe; native release open) | Contract `b24ca0c`; dependency/profile boundary `e6ee8b8`; `apps/tool-service/pyproject.toml` keeps Playwright in the opt-in `browser` extra while the core gateway remains browser-free, and `Dockerfile.tool-service` keeps browser/Docling/Semgrep in the separately budgeted `extensions` profile with exact uv `0.4.30`; `uv run --isolated pytest packages/mas-core/tests/test_image_budgets.py apps/tool-service/tests/test_default_shipped_tool_catalog.py -q` and `uv run --isolated python scripts/check_image_budgets.py --json` pass the profile ceilings. Retained local measurements are core `267957904` bytes and extensions `4155668123` bytes, with bounded loopback `/health` probes at 26,836 ms/112.3 MiB and 29,913 ms/137.7 MiB respectively. Evidence is [`provenance/image_budgets_live.json`](provenance/image_budgets_live.json); compressed archive size, clean native-Linux build/pull, and vulnerability/SBOM evidence remain open |
| API/protocol contract export | static contract (preparatory P1) | PASS | Commit `2860838` regenerates the checked-in artifacts; `uv run --isolated python scripts/check_api_contract.py --json`; 236 OpenAPI paths/130 schemas, `aiat.v1`, and generated dashboard/SDK types match runtime/provenance hashes, including the read-only system diagnostics route, native trace-evidence, SLO/capacity, evidence-package, self-improvement outcome-action, artifact-bundle/read-back, and bounded artifact/usage evidence reads (`OpenAPI 0997e6642aeaeb0df7cf22f46c99a7ad46174494dae1487f6b5be3f134bfc9a8`, TypeScript `ae4c90396c29d5cfffda4f638d5cdb8a55ca872ebf28f56418868c56510fb539`, Python `88811f84c9d934dab6048cba36f27fb798ac0995218bc7602036a11419292d0e`) |
| Built-in PM/SCM adapter declarations | static/unit/fixture; provider live boundary | PASS | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_declarations.py -q`; `uv run --isolated python scripts/check_provider_adapter_declarations.py --json` reconciles the real YouTrack adapter plus GitHub `pm`/`delivery`/`checks` profiles and bounded path guards with zero provider HTTP calls or mutations; provider-specific mock/live/outage/restore evidence remains open |
| Built-in PM/SCM mocked HTTP conformance | static/unit/fixture; provider live boundary | PASS | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_provider_adapter_http_conformance.py -q`; `uv run --isolated python scripts/check_provider_adapter_http_conformance.py --json` drives eight real-adapter YouTrack/GitHub cases through local mocked responses for health/configuration, projection/read-back, cursors, deactivation, comments/links, GitHub source-control paths, webhook handling, and retryable/permanent failures; no external HTTP or provider mutation occurs |
| PM inbound canary replay and ACTIVE activation scope | static/unit/API/storage (live provider boundary) | PASS (bounded governance; live open) | Commit `72e1aef`; `uv run --isolated pytest apps/orchestrator-api/tests/test_pm_control_plane.py packages/mas-core/tests/test_pm_integrations.py packages/mas-core/tests/test_phase7_storage.py -q` plus `check_api_contract.py --json` pass. ACTIVE inbound mutations persist sanitized actor/command/projection evidence atomically; verified terminal-conflict replay is digest/expiry/arming/connection/one-command gated; ACTIVE lifecycle plans snapshot trusted actor scope/blast radius/default-deny policy and transactional approval evidence; evidence-only reconciliation observations remain non-blocking while actionable conflicts stay blocking. Operator-owned verified replay fixtures and provider/native live canary, rollback, and restore evidence remain open; licence metadata is informational only |
| External-account lifecycle fixture | static/unit/fixture; provider live boundary | PASS | `uv run --isolated pytest packages/mas-core/tests/test_external_account_lifecycle.py -q`; `uv run --isolated python scripts/check_external_account_lifecycle.py --json` drives the actual `IdentityService` through eight in-memory cases for category approval/idempotency, one-use browser leases, credential rotation/session revocation, closure approval, immediate suspension, fail-closed unknown categories, and secret-safe output without external account/provider calls |
| Machine-readable release ledger | static aggregation; live boundary | PASS (static + current live aggregation); BLOCKED/NO-RELEASE (live profiles) | `4d7a495` adds the native release-host preflight as the `release_environment:live` child while preserving the 48-check static profile; `uv run --isolated pytest packages/mas-core/tests/test_release_ledger.py -q` passes. The current unconfigured local 65-check `--live --json` run (2026-08-11T21:15:26Z) records 51 passes, zero failures, 14 externally blocked probes, and four pending evidence items; the safe summary is retained at [`provenance/release_ledger_live_current.json`](provenance/release_ledger_live_current.json). The configured loopback 64-check evidence remains retained at [`provenance/release_ledger_live.json`](provenance/release_ledger_live.json) with 59 passes and five blocked probes. Native release-host, operator-selected worker certification, immutable image identity, gVisor, provider/mail, and self-improvement source remain separate open gates; licence metadata remains non-gating. |
| Project evidence package | static/unit/API/dashboard (preparatory P1) | PASS (committed core/API/dashboard surfaces; project-page/live preparatory) | Commits `a44a1aa`, `d0472af`, `cbf00d9`, `33e0384`, `82bbaeb`, `1112d5e`, and dashboard error-state repair `fc4f0fa`; the deterministic `EvidencePackage` builder, scope-policy resolver, isolated package/policy API router boundary, operator-only idempotent snapshot upsert, project workspace coverage, and API/UI evidence-package categories pass focused tests. `aiat.project-evidence-package.v1` groups repository, documents, tests, security, deployment, cost, approvals, flow, worker, artifact, and audit sources, while licence/restriction values remain metadata-only notices; live durable snapshot/provider/worker generation and native recovery remain open |
| Project evidence package stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `bc80ad5`; `npm run typecheck`, targeted ESLint, full dashboard `npm run lint` (two unrelated hook warnings), `npm run build`, and the source-built `project-evidence-states.spec.ts` pass 1/1. The page uses `cache: "no-store"`, retains the last successful package when a refresh fails, labels it as last known, and exposes a keyboard-visible Retry action that clears after successful recovery; initial failures remain explicit. Full project-page composition and live provider/worker evidence remain open |
| Project evidence package accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `89091c1`; targeted ESLint, `npm run typecheck`, `npm run build`, and the source-built `project-evidence-states.spec.ts` pass 1/1. The page exposes named main/package sections, labeled 44px back/refresh actions, and a captioned evidence table with scoped column headers; the Impeccable detector returned no warnings for the changed page/test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live provider/worker evidence remain open |
| Dashboard contract generation | static/typecheck (preparatory P1) | PASS | Commit `2860838`; `uv run --isolated python scripts/generate_typescript_api.py --check`; `npm run typecheck` in `apps/mas-dashboard` passes after the project-evidence `ErrorBanner` prop repair `fc4f0fa`; 130 models and 269 operations generated deterministically |
| Dashboard theme preference foundation | static/typecheck/lint/build/source-built Playwright (preparatory P1) | PASS (foundation; full page parity/native visual open) | Commit `5e3cc13`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4101 npm run test:e2e -- dashboard-theme.spec.ts` pass 2/2 for persisted explicit light/dark selection and system media switching. `ThemeProvider` applies a no-flash `data-theme` bootstrap, the sidebar/mobile controls expose `system`/`light`/`dark`, the legacy slate palette has a bounded light migration layer, and reduced-motion preferences disable authored animation/transition duration; complete page-by-page contrast/visual/mobile parity remains open. |
| Evidence detail read model | static/build/source-built Playwright (preparatory P1) | PASS (fourteen canonical kinds; evidence-detail stale refresh covered) | Commits `8fefc8b`, `c8505eb`, `dc50719`, `5357166`, `ec8cf67`, `2ca5f3d`, and `6c52552`; `npm run typecheck`, `npm run lint`, `npm run build`, `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4112 npx playwright test dashboard-evidence-detail.spec.ts --reporter=line`, and the focused API evidence suite pass; dashboard source-built coverage is 9/9. `aiat.evidence-detail.v1` reads project, flow, flow-instance, worker, worker-run, credential, dead-letter, runtime, trace, model, integration, tool, artifact, and usage records through allow-listed backend paths and scalar fields only; model catalogue/integration list/tool manifest selection is bounded by the citation ID, artifact/usage reads are operator-authenticated, strings/numbers are bounded, nested payloads/secrets/metadata/pricing/resource/configuration/profile bindings/tool schemas/credential requirements/trace items are omitted, temporary detail failure preserves citation identity, the last successful scalar projection remains visible through a failed refresh with an explicit Refresh control, obsolete requests are aborted on navigation, and unsupported kinds remain identity-only. Broader stale/offline recovery remains open. |
| Evidence detail accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `32f3a76`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4136 npx playwright test dashboard-evidence-detail.spec.ts --workers=1 --reporter=line` pass 9/9. The page exposes a named page/canonical-citation region, semantic bounded-detail region with an `aria-busy` refresh state, decorative-icon suppression, and 44px CEO-chat/canonical-link/Refresh targets; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and broader evidence-detail recovery remain open. |
| System visualisation accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `ed5e551`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4138 npx playwright test app-operations.spec.ts --workers=1 --grep "system visualization exposes hierarchy, permissions, orchestration, and path tracing" --reporter=line` pass 1/1. The page exposes named loading/error/ready landmarks, horizontal visualization tabs with semantic tab/tabpanel links, and 44px breadcrumb, refresh, Mermaid-copy, path-trace, graph/detail, policy, retry, and back-link targets; deterministic fixtures cover the four system-visualisation API reads, and contrast classes identified by the Impeccable detector were corrected before commit. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live control-plane evidence remain open. |
| System visualisation denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `db898e7`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4147 npx playwright test e2e/app-operations.spec.ts --workers=1 --grep "system visualization" --reporter=line` pass 4/4 for healthy, partial, offline, and 403 hierarchy states. A denied hierarchy read renders a named access-status region, operator guidance, and a 44px dashboard return link without Retry; partial notices identify each failed source. Full WCAG/native-Linux visual certification and live ACL evidence remain open. |
| Shared identity-resource denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `0974434`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4148 npx playwright test e2e/identity-states.spec.ts --workers=1 --reporter=line` pass 2/2 for stale/retry preservation and a 403 first-load denial. The shared page removes Refresh/Retry while denied, exposes a named access-status region and 44px dashboard return link, and preserves already loaded metadata-only rows if authorization is lost after a successful read; provider/live identity certification remains open. |
| Shared ErrorBanner decorative-icon semantics | static/build/source-built Playwright (preparatory P1) | PASS | Commit `29b700c`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4149 npx playwright test e2e/app-operations.spec.ts --workers=1 --grep "system visualization" --reporter=line` pass 4/4, including the partial-state assertion that the shared warning icon has `aria-hidden="true"`. Full WCAG/native-Linux visual certification remains open. |
| PM integrations accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `bbd6ba3`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4139 npx playwright test app-operations.spec.ts --workers=1 --grep "PM integrations preserve conflicts and expose a stale refresh retry" --reporter=line` pass 1/1. The page exposes a named busy main landmark, explicit summary/connections/reconciliation/lifecycle regions, labeled lifecycle inputs, and 44px refresh/retry/generation/approval/apply controls; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and provider-owned evidence remain open. |
| System Overview accessibility baseline | static/build/source-built Playwright with local fixtures (preparatory P1) | PASS | Commit `c07b4a6`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4140 E2E_ORCHESTRATOR_URL=http://127.0.0.1:9999 npx playwright test first-run-states.spec.ts --workers=1 --reporter=line` plus the same test at port `4141` against a `not_seeded` fixture pass 1/1 each. The home page exposes a named main and hero/status surface, explicit health/overview-metrics/first-run/company-project-state/Quick Links regions, decorative-icon suppression, and 44px graph/Quick Links/seed controls; the Impeccable detector returned no warnings. The direct live backend was unavailable during this run, so full control-plane, WCAG, native-Linux, and visual certification remain open. |
| System Overview source-status recovery | static/build/source-built Playwright with deterministic fixtures (preparatory P1) | PASS | Commits `50cee61`, `24be4ba`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4144 EXPECTED_OVERVIEW_STATE=offline npx playwright test system-overview-recovery.spec.ts --workers=1 --reporter=line` plus the same test with `EXPECTED_OVERVIEW_STATE=partial` pass 1/1 each. The page classifies seven independent control-plane/metrics reads as healthy, partial, or offline, names failed sources, avoids inferring unavailable values, exposes a 44px GET retry, and hides decorative EmptyState icons from assistive technology; full retained stale history, WCAG, native-Linux, and live control-plane evidence remain open. |
| Governance read-surface stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `52de581`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4113 npx playwright test governance-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The combined model-profile, runtime catalogue, WorkerRun, and external-steward reads use `cache: "no-store"`; if any source fails after a successful load, the page retains the last known data, labels it stale, exposes header Refresh and banner Retry controls, and clears the warning after a successful retry. Provider/live governance evidence remains open. |
| Governance accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f4ae7eb`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4133 npx playwright test governance-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named main/read-surface structure, explicit executive/model-profile/WorkerRun/steward/catalogue regions, a captioned/scoped WorkerRun table, accessible catalogue status, and 44px refresh, retry, executive-form, and confirmation controls; the Impeccable detector returned no warnings for the changed page, action panel, or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live governance evidence remain open. |
| System Control status stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f445c17`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4114 npx playwright test system-status-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The canonical `/api/system/status` read uses `cache: "no-store"`; after a successful load, a failed refresh retains the runtime status, labels it as the last known system status, and exposes header Refresh plus banner Retry controls. The implementation keeps the initial loading state explicit and does not claim native/live runtime evidence. |
| System Control accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `543f392`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4134 npx playwright test system-status-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named main/loading state, explicit runtime-status/schedule/control/dialog regions, scheduled-event semantics, and 44px refresh, retry, shutdown/resume, schedule-input/save, and confirmation controls; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live System Control evidence remain open. |
| System Control denial-state recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `14968d4`; targeted ESLint, `npm run typecheck`, `npm run build`, the Impeccable detector (`[]`), and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4150 npx playwright test e2e/system-status-states.spec.ts --workers=1 --reporter=line` pass 3/3 for stale recovery, first-load 403 denial, and authorization loss after a successful read. Denied status reads hide Refresh/Retry and shutdown/resume/schedule mutations while preserving only last-known read context; native/live ACL and WCAG evidence remain open. |
| Projects list stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `d3482ab`, `7828b48`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test projects-states.spec.ts --workers=1 --reporter=line` pass 1/1. The paired `/api/projects` and `/api/flows?is_active=true` reads use `cache: "no-store"`; either source failing after a successful load retains the last known project/flow data, labels the list stale, separates load errors from create/action errors, and exposes header Refresh plus banner Retry controls. The table exposes a caption, scoped headers, explicit description disclosure, responsive overflow, and 44px selection/filter/sort/link/action targets. Native/live project evidence remains open. |
| Tools catalogue stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `5f4b0eb`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4116 npx playwright test tools-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/tools` catalogue read uses `cache: "no-store"`; after a successful load, a failed refresh retains tool definitions and circuit-breaker summaries, keeps them visible, labels the catalogue stale, and exposes header Refresh plus banner Retry controls. Native/live tool evidence remains open. |
| Tools catalogue accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `83e39e6`; targeted ESLint, `npm run typecheck`, `npm run build`, and the source-built `tools-states.spec.ts` pass 1/1. The page exposes named main/search/group regions, captioned/scoped group tables, keyboard-visible tool expansion, and 44px refresh/group/search/copy/retry/empty-state targets; the Impeccable detector returned no warnings for the changed page/test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live tool evidence remain open |
| Dead-letter queue stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `823fa6d`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4117 npx playwright test dlq-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/dlq` read uses `cache: "no-store"`; after a successful load, a failed refresh retains the message set and replay context, keeps cards visible, labels the queue stale, and exposes header Refresh plus banner Retry controls. Native/live DLQ evidence remains open. |
| Dead-letter queue accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `99a19a2`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test dlq-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/summary/filter/list/disclosure regions, `aria-pressed` severity filters, keyboard-visible envelope inspection, and 44px refresh/retry/selection/replay/filter/inspection targets; the Impeccable detector returned no warnings for the changed page and filter component. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live DLQ evidence remain open. |
| Credentials metadata stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `970f09c`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4118 npx playwright test credentials-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The `/api/credentials` read uses `cache: "no-store"`; after a successful load, a failed refresh retains only redacted credential metadata (names, placeholders, policy, usage), keeps rows visible, labels the list stale, and exposes header Refresh plus banner Retry controls. The fixture contains no secret value; native/live expiry, revocation, and provider evidence remain open. |
| Credentials accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `93fdfbc`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test credentials-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/security/data regions, a captioned/scoped credentials table, a labeled creation dialog with explicit field associations, and 44px refresh/audit/selection/copy/delete/dialog targets; the Impeccable detector returned no warnings for the changed page and bulk-action component. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live credential evidence remain open. |
| Metrics partial/stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `85596b0`; `npm run typecheck`, `npm run lint`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4119 npx playwright test metrics-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The six `/api/metrics` Prometheus query families use `cache: "no-store"`; partial query failures retain successful series, label the surface stale, preserve the last-successful refresh timestamp, and expose header Refresh plus banner Retry controls. Native/live telemetry and Prometheus evidence remain open. |
| Metrics accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `da113af`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4127 npx playwright test metrics-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/summary/chart regions, a semantic time-range control, and 44px range/refresh/retry/empty-state targets; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live metrics evidence remain open. |
| Container Logs accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `993b1cb`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4128 npx playwright test logs-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/filter/legend/output/status regions, 44px stream/filter/recovery targets, and an `aria-busy` log output; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live container-log evidence remain open. |
| Flows list stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `a0faf5b`; `npm run typecheck` and `npm run build` pass, the focused Flows file/test lint passes, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4120 npx playwright test flows-states.spec.ts --reporter=line` passes 1/1. The `/api/flows?limit=1000` (and status-filtered) read uses `cache: "no-store"`; after a successful load, a failed refresh retains flow definitions, keeps rows visible, labels the view as showing last-known flows, and exposes header Refresh plus banner Retry controls. Native/live flow evidence remains open. |
| Flows list stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `a0faf5b`, `6b0413b`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test flows-states.spec.ts --workers=1 --reporter=line` pass 1/1. The `/api/flows?limit=1000` (and status-filtered) read retains last-known definitions through refresh failure and exposes stale/retry recovery; the table now has an accessible name/caption, scoped headers, responsive overflow, and 44px refresh/create/search/filter/selection/link/delete targets. Native/live flow evidence remains open. |
| Flow editor load/stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `b5098e7`; `npm run typecheck`, full `npm run lint` (two unrelated hook warnings), `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4111 npx playwright test flow-editor-states.spec.ts --workers=1 --reporter=line` pass 1/1. Existing-flow first load exposes an explicit unavailable state with Retry; refresh failures retain the last-known flow/canvas, show stale labeling, preserve backend error detail, and recover through Retry. New-flow creation remains available without the refresh control. Native/live flow/runtime recovery remains open. |
| Flow editor load/stale/retry and accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commits `b5098e7`, `140af1c`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4115 npx playwright test flow-editor-states.spec.ts --workers=1 --reporter=line` pass 1/1. Existing-flow first load remains explicit and retryable; refresh failures retain the last-known canvas, and the editor exposes semantic header/main/palette/canvas/config landmarks plus 44px toolbar/palette/config/generated-form targets. Native/live flow/runtime recovery remains open. |
| Project detail first-load/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `f364763`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4112 npx playwright test project-detail-states.spec.ts --workers=1 --reporter=line` pass 1/1. A failed first project read now shows Project unavailable with backend error detail and keyboard-visible Retry; a successful retry renders the project workspace. Existing-project stale/retry recovery remains covered by the workspace path; full project-page composition and live provider/worker generation remain open. |
| Project detail accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `40b87dd`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4135 npx playwright test project-detail-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes a named page/loading state, explicit project status, 44px refresh/retry/back and primary project-view tab targets, semantic project/workspace tab-panel relationships, and an `aria-busy` workspace panel; the Impeccable detector returned no warnings for the changed page or test. This is a focused page-level baseline; full project composition, WCAG/native-Linux visual certification, and live provider/worker evidence remain open. |
| Project workspace stale/retry recovery and nested tab semantics | static/build/source-built Playwright (preparatory P1) | PASS | Commits `cb1c665`, `fcb0f4b`; `npm run typecheck`, targeted ESLint, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4114 npx playwright test project-workspace-states.spec.ts --workers=1 --reporter=line` pass 1/1. A failed workspace refresh retains activity/resources/cost data, preserves the last repository snapshot across partial failure, labels the surface as showing last known workspace data, and recovers through Retry. Activity/Resources/Cost tabs expose semantic tab/tabpanel relationships, roving `tabIndex`, Arrow/Home/End navigation, and 44px targets. First-load handling is explicit; live workspace/provider/worker generation remains open. |
| Agent Streams reconnect/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `3e8a0ea`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4123 npx playwright test streams-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The stream page reads bounded history with `cache: "no-store"`, guards obsolete reconnect generations, retains history/messages across failed SSE or history refresh, labels last-known data, and exposes Reconnect plus Retry. Native/live Redis/router stream evidence remains open. |
| Agent Streams accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `d320383`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4129 npx playwright test streams-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/filter/feed/status regions, a captioned message table, keyboard-accessible expandable rows, 44px stream/filter/action targets, and an `aria-busy` feed state; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live stream evidence remain open. |
| Hiring Board stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `7541b84`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4124 npx playwright test workers-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The worker catalogue retains its last successful rows after a failed refresh, labels the view as showing last-known workers, keeps rows visible, and exposes Retry; first-load failures show an unavailable state. Native/live worker certification evidence remains open. |
| Hiring Board accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `826b4c5`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4131 npx playwright test workers-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/policy/summary/filter/table regions, integration/runtime status landmarks, a captioned/scoped worker table, keyboard-expandable rows, associated registration-dialog fields, and 44px refresh/register/filter/selection/row-action/dialog targets; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live worker evidence remain open. |
| CEO Live Feed reconnect/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `1761429`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4125 npx playwright test ceo-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The CEO feed retains bounded history/messages across failed reconnect or history refresh, guards obsolete generations, labels last-known data, and exposes Reconnect plus Retry without changing the governed composer. Native/live Redis/router CEO evidence remains open. |
| CEO Live Feed accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `1f947a9`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4130 npx playwright test ceo-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/composer/summary/filter/feed/status regions, 44px stream/composer/filter/recovery targets, a busy feed state, and keyboard-expandable messages; the Impeccable detector returned no warnings for the changed page and test. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live CEO feed evidence remain open. |
| CEO Command Center chat stream/history recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `beabb95`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4126 npx playwright test ceo-chat-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The chat retains bounded history through live-stream failure, guards obsolete history/live callbacks, keeps the transcript labelled last known, exposes a keyboard-visible Retry action, and keeps history and stream failures independent so a late history response cannot hide a stream outage. Native/live Redis/router CEO evidence remains open. |
| CEO Command Center chat accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `8ffb5df`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4132 npx playwright test ceo-chat-states.spec.ts --workers=1 --reporter=line` pass 1/1. The page exposes named main/workspace/transcript/composer regions, a live transcript log with busy state, 44px navigation/composer/quick-command/recovery targets, explicit chat guidance regions, and a mobile-safe accessible activity link. The Impeccable detector was run once before the final class corrections; its three gray-on-color findings were corrected in the implementation, and no detector rerun was used. This is a focused page-level baseline; full WCAG/native-Linux visual certification and live CEO chat evidence remain open |
| Credentials refresh render-state repair | static/lint/build/source-built Playwright (preparatory P1) | PASS | Commit `e6e6980`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4121 npx playwright test credentials-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The stale credentials table uses state, rather than reading a ref during render, to distinguish initial loading from retained metadata; full dashboard lint now reports zero errors and two unrelated hook warnings. |
| Container Logs stale/retry recovery | static/build/source-built Playwright (preparatory P1) | PASS | Commit `280d363`; `npm run lint`, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4122 npx playwright test logs-states.spec.ts --reporter=line` pass (focused browser coverage 1/1). The SSE route retains the last log buffer after an error payload, labels it as last known, exposes Retry, and replaces the retained buffer on the first successful event after retry; native/live container log evidence remains open. |
| Python SDK contract generation | static/unit (preparatory P1) | PASS | Commit `2860838`; `uv run --isolated python scripts/generate_python_api.py --check`; `uv run --isolated pytest packages/mas-api-sdk/tests -q`; 130 models and 269 operations match the OpenAPI artifact |
| Company manifest policy fields | static/contract (preparatory P1) | PASS | Typed manifest group `e0f0aee`; `uv run --isolated pytest packages/mas-core/tests/test_company_manifest.py -q`; optional timezone, retention (including trace days/sample rate), privacy, evidence, model, and deployment blocks validate, and legacy manifests remain compatible |
| Company timezone propagation | static/unit/tool-service/dashboard (preparatory P1) | PASS (tool/dashboard; Compose follow-up) | Commits `8bcff1a` and `ee1361f`; `uv run --isolated pytest apps/tool-service/tests/test_time.py apps/orchestrator-api/tests/test_system.py -k 'time or schedule' -q` plus `npm run typecheck` in `apps/mas-dashboard` prove valid IANA-zone reads, manifest-derived scheduler defaults, and invalid-zone UTC fallback while durable records remain UTC. Compose/native display evidence remains separate |
| Prompt/tool and review contract reconciliation | static/unit (preparatory P1) | PASS | Contract `20f0499`; implementation `5b830e9`; `uv run --isolated python scripts/check_prompt_tool_reconciliation.py --json`; all 11 shipped prompts resolve to 114 concrete, policy-allowed tools; review submissions use canonical `REVIEW_RESPONSE` envelopes with signed caller context and CEO privileged requests use `/ceo/privileged-action` |
| PM/SCM provider conformance fixture | static/unit/CLI (preparatory P1) | PASS (fixture; live blocked) | Commit `7f6bfc5`; `uv run --isolated pytest packages/mas-core/tests/test_pm_integrations.py packages/mas-core/tests/test_provider_conformance_runner.py -q`; `uv run --isolated python scripts/check_provider_conformance.py --json` passes 10 fake-provider cases; `--live --json` returns exit 2 until provider-specific sandbox/mock HTTP/outage/restore evidence is supplied |
| Flow node-schema contract | static/unit (preparatory P1) | PASS | `uv run --isolated python scripts/generate_flow_node_schemas.py --check`; 9 node types at v1.0 match backend validation, JSON artifact, `/flows/node-schemas`, and generated dashboard metadata |
| Dashboard node-schema editor | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; both flow editors render the generated contract plus editable typed form, governed worker/profile selectors, CSV/JSON fields, deprecated `team_id`/`action` fields only in collapsed compatibility controls, and the dashboard store/proxy exposes explicit saved-definition migration |
| Evidence-policy contract | static/unit (preparatory P1) | PASS | Evidence tests cover required artifact kinds; `/evidence-policies` publishes built-ins and policy dry-run validation includes worker-run/repository resources |
| Evidence-policy selection | static/API/unit (preparatory P1) | PASS | Company defaults persist through the active manifest; `PUT /projects/{project_id}/evidence-policy` persists project defaults and milestone overrides; `resolve_evidence_policy_selection` and `check_evidence_policy_resolution.py --json` cover project-milestone → project → flow → company-milestone → company → manual precedence without using licence metadata as a gate; live transition proof remains open |
| Evidence-policy scope resolution fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | `uv run --isolated pytest packages/mas-core/tests/test_evidence_policy_resolution.py -q`; `uv run --isolated python scripts/check_evidence_policy_resolution.py --json` passes seven deterministic precedence/fallback cases and reports `licence_metadata_is_gate: false`; `--live` is explicitly blocked without an authenticated API scenario |
| External-account action policy fixture | static/unit (preparatory P1) | PASS (live scenario blocked) | Commit `f577675`; `uv run --isolated pytest packages/mas-core/tests/test_external_account_action_policy.py -q`; `uv run --isolated python scripts/check_external_account_action_policy.py --json` reconciles all five action rules, four category dispositions, and fail-closed unknown action/category behavior without mutating identity/provider state; `--live` is blocked until a provider-specific sandbox/outage scenario exists |
| Outbound-mail lifecycle fixture | static/unit/fixture (relay live open) | PASS | Commit `f577675`; `uv run --isolated pytest packages/mas-core/tests/test_outbound_mail_lifecycle.py -q`; `uv run --isolated python scripts/check_outbound_mail_lifecycle.py --json` drives the actual `IdentityService` through approval pause, request/submission idempotency, definitive provider-failure retry, ambiguous-outage reconciliation hold, and secret-safe output without external relay calls |
| Self-improvement candidate detection | static/unit/fixture (live signal sources open) | PASS | Commit `4d8dddf`; `uv run --isolated pytest packages/mas-core/tests/test_improvement_candidates.py -q`; `uv run --isolated python scripts/check_self_improvement_candidates.py --json` reconciles defect, metric, upstream-update, cost, and operator-goal signals with deterministic deduplication/risk/budget mapping, conflicting-ID rejection, secret-safe metadata, and zero project/budget/credential/deployment side effects |
| Self-improvement lifecycle persistence | static/unit/storage/API (live worker/provider boundary) | PASS (live boundary blocked) | Commit `64218ab`; `PYTHONPATH=packages/mas-api-sdk uv run --isolated pytest apps/orchestrator-api/tests/test_self_improvement_api.py packages/mas-core/tests/test_phase7_storage.py -k self_improvement -q` passes 31 focused tests; authenticated project creation, revisioned read/reference/action paths, compare-and-set lifecycle updates, outcome idempotency, and project-history persistence are covered. Live certified-worker, provider, deployment, and control-plane reconciliation remain open |
| Reusable flow templates | static/unit/API (preparatory P1) | PASS | Commit `234adfb`; six canonical templates validate through `validate_flow`; `/flow-templates` and `/flows/from-template` are covered by `test_flow_templates.py` and `test_flows.py`, with import paths using the package-level workflow exports |
| Dashboard canonical template consumption | static/typecheck (preparatory P1) | PASS | `npm run typecheck` in `apps/mas-dashboard`; new-flow starter cards fetch `/api/flow-templates`, preserve template configs/evidence metadata, remap branch references, and retain a blank-canvas fallback |
| Flow definition lifecycle | static/API (preparatory P1) | PASS | Commit `234adfb`; new and updated definitions persist the explicit node-schema version, and export/hash, deterministic diff, import, publish, deprecate, compatible migration, and explicitly mapped active-node graph-rewrite endpoints are covered by `apps/orchestrator-api/tests/test_flows.py`; live recovery remains open |
| Flow-instance recovery boundary | static + guarded live API probe | PASS (blocked without instance/API) | `uv run --isolated pytest packages/mas-core/tests/test_flow_instance_recovery.py -q`; `scripts/check_flow_instance_recovery.py` is read-only by default, requires `--confirm` for state-changing actions, validates post-action state/execution history, and returns exit 2 when URL/instance/live evidence is absent; full worker/project/UI recovery remains open |
| Flow control-topology contract | static/unit/fixture (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py -q`; `uv run --isolated python scripts/check_flow_topology.py --json` reconciles parallel branch declarations, join fan-in, and switch case targets with persisted edges; no worker dispatch/storage mutation occurs and live fan-out/recovery remains open |
| Flow execution semantics contract | static/unit/fixture (preparatory P1) | PASS (deterministic traversal; live open) | `uv run --isolated pytest packages/mas-core/tests/test_flow_execution_semantics.py -q`; `uv run --isolated python scripts/check_flow_execution_semantics.py --json` drives the real traversal engine through fan-out, one-branch join waiting, single join scheduling, selected switch routing, and unknown-case blocking; duplicate join scheduling and completed-join reactivation are prevented, while live fan-out/join/watchdog/recovery evidence remains open |
| Governed asynchronous flow-task binding | static/unit/fixture (preparatory P1) | PASS (binding contract; live open) | Commit `234adfb`; `uv run --isolated pytest packages/mas-core/tests/test_flow_worker_binding.py packages/mas-core/tests/test_flow_retry_persistence.py apps/orchestrator-api/tests/test_flows.py -q`; `uv run --isolated python scripts/check_flow_worker_binding.py --json` proves queued/claimed/running Worker Runs keep their task active, terminal states settle, parallel bindings remain copy-on-write, safe retry re-enters governed dispatch, and unknown states fail closed; both API retry paths preserve prior executions as `SUPERSEDED`, and the API now fails closed when evidence-preserving retry storage is absent; live worker canary/recovery remains open |
| Watchdog and safe-retry recovery semantics | static/unit/fixture (preparatory P1) | PASS (controller contract; live open) | `uv run --isolated pytest packages/mas-core/tests/test_workflow_watchdog_recovery.py -q`; `uv run --isolated python scripts/check_workflow_watchdog_recovery.py --json` proves boot grace, downtime-aware timeout, watchdog failure transition, recorded-safe-state retry, and terminal-state exclusion without storage/worker mutation; native watchdog/cold-recovery remains open |
| Flow compatibility-alias audit | static/API/unit (preparatory P1) | PASS | `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_dry_run.py -q`; dry-run returns deterministic `compatibility_aliases` findings and concrete worker-binding recommendations without mutating saved definitions |
| Saved-definition worker migration | static/API/unit (preparatory P1) | PASS | Commit `54ad710`; `uv run --isolated pytest packages/mas-core/tests/test_flow_node_schema.py apps/orchestrator-api/tests/test_flow_legacy_migration.py apps/orchestrator-api/tests/test_flow_dry_run.py -k 'legacy or dry_run' -q`; `POST /flows/{flow_id}/migrate-legacy-tasks` previews missing/unknown bindings and creates an immutable worker-bound version with `aiat.flow-legacy-task-migration.v1` metadata, with the dashboard proxy/store preserving the operator boundary; live worker canary/recovery remains open |
| Sprint retrospective/profile lineage | focused API/unit (preparatory P1) | PASS | Commit `1112d5e`; `uv run --isolated pytest apps/orchestrator-api/tests/test_test7_operator_ui.py -k 'completed_issue_updates_agent_profile_once or completed_sprint_persists_retrospective_lineage' -q`; terminal issue completion observes the assigned profile exactly once and persists a `sprint_retrospective` KPI snapshot whose raw data names source/completed issue IDs and assigned-agent lineage; live transition/recovery remains open |

| Operator sign-in accessibility baseline | static/build/source-built Playwright (preparatory P1) | PASS | Commit `d928834`; targeted ESLint, `npm run typecheck`, `npm run build`, and `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4143 npx playwright test login-accessibility.spec.ts --workers=1 --reporter=line` pass 1/1. The unauthenticated route exposes a named main/operator-sign-in structure, explicit busy/status announcements, labeled credential fields, password-visibility state, and 44px password/sign-in targets; the Impeccable detector returned no warnings for the changed page/test. Full WCAG/native-Linux visual certification remains open |
| Shared identity-resource route matrix | source-built Playwright (preparatory P1) | PASS | Commit `485dfd2`; `PLAYWRIGHT_BASE_URL=http://127.0.0.1:4145 npx playwright test identity-resource-matrix.spec.ts --workers=1 --reporter=line` passes 9/9 with safe metadata-only fixtures across identities, approvals, audit, sessions, external accounts, domains, relay, mailboxes, and outbound mail; provider/live identity certification remains open |
## Licence metadata assertion

The evaluator and steward tests deliberately include missing, restricted, and
unclassified licence metadata. Those values are retained in the report and
provenance record as notices, while technical source/version, security,
sandbox, compatibility, adapter, model, budget, and human gates remain
independent blockers.
The historical `LICENSE_REVIEW` label is also non-blocking; the steward test
suite rejects a direct `LICENSE_REVIEW` → `BLOCKED` transition.

## Carry-forward blockers

- `DEF-2026-07-14-036`: the refreshed local WSL2 negative network matrix passes and is retained at `provenance/network_boundary_live.json`; a post-fix native-Linux negative test is still required for closure.
- The team-runner storage boundary is statically/API-tested; native DNS/TCP/HTTP
  denial and positive control-plane storage evidence are still required to
  close the historical network defect.
- Coding/tester security evidence has been executed but is not clean: the
  exact-source Semgrep report has 316 findings and 54 engine warnings. Both
  manifests remain `findings_review_required` and cannot be activated solely
  because OpenCode interface evidence exists.
- Production images and dependency/runtime records still need deployment-supplied
  immutable digest, source, lock, generated SBOM, and scan reconciliation. The
  `42b03a3` checker now rejects malformed CycloneDX artifacts when required, but
  the clean native-Linux artifact set is not present in this workspace.
- CEO-vs-human dashboard/API section separation is covered by static/API tests;
  native deployment/network evidence remains open.
- Application image digests, SBOMs, vulnerability scans, and core/extension
  resource measurements remain open: Docker is reachable, but deployment image
  references, native build/scan inputs, and clean measurement evidence are not
  available in this working environment.
- The object-store conformance runner has a retained local deployed MinIO
  result (8/8 scoped cases) and a secret-safe IAM reconciliation helper. The
  R6 exit gate still requires provider-pair/SeaweedFS comparison, encrypted
  backup, clean-host restore, benchmark, and multi-host storage evidence; the
  bounded empty-target restore preflight is covered by `93bf755`.
- The refreshed local orchestrator and rebuilt tool service are running the
  current source with database migration head `0036_native_trace_spans`. The
  bounded live trace probe observes one API-request row and one native
  `/health` transport span; the separate pure `time_now` probe observes one
  project-usage row and one native `tool_service` span. Retained evidence is
  [`provenance/trace_observability_live.json`](provenance/trace_observability_live.json)
  plus [`provenance/tool_trace_live.json`](provenance/tool_trace_live.json).
  The host-side tool probe now resolves the Compose-only `tool-service:8002`
  alias to loopback only for a local orchestrator; remote configured URLs are
  left unchanged, and both live trace children pass in the corrected aggregate.
  Commit `24c2e35` adds explicit native model/worker source-category coverage
  and a fail-closed selected-worker checker; the fixture passes, but no live
  dispatch is claimed. Representative model-backed worker, identity provider
  mail-edge/bounce, live retention, and native multi-service evidence remain
  open.

The next ledger revision must be generated from a clean frozen commit and an
environment manifest, then include live, recovery, security, and resource
evidence rather than inheriting these static results.
