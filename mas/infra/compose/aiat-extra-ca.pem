# Optional additional trust roots for OmniRoute.
#
# Keep this placeholder certificate-free. Set AIAT_EXTRA_CA_CERT to a host
# path containing a trusted PEM root when a managed TLS proxy or endpoint
# security product re-signs outbound HTTPS traffic.
