import { IdentityResourcePage } from "@/components/identity/IdentityResourcePage";
export default function Page() { return <IdentityResourcePage resource="auth-sessions" title="Auth sessions" description="Opaque local browser session metadata and revocation state." />; }
