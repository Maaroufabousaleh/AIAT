{
  "$schema": "https://opencode.ai/config.json",
  "agent": {
    "build": {
      "permission": {
        "bash": {
          "*": "allow",
          "git push": "ask",
          "git reset --hard *": "ask",
          "rm -rf *": "ask"
        },
        "edit": {
          "*": "allow"
        },
        "read": "allow"
      }
    }
  }
}